# 앱 API 2단계-a: 워치 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `GET/POST/DELETE /v1/me/watch` 3개 엔드포인트를 구현하고, 1단계에서 `false`로 고정해뒀던 `TrendDetailResponse.watched`를 실데이터로 교체한다.

**Architecture:** `Vote`/`Endorsement` 엔티티와 같은 스타일로 `Watch` 엔티티 + `WatchRepository`를 새로 만들고(신규 마이그레이션 V19), `SubmissionService`/`MeService`와 같은 독립 서비스 패턴으로 `WatchService`를 만든다. 매칭은 기존 `NameNormalizer.normalize()`(제보 완전일치 병합에 쓰는 순수 함수) 재사용, stage 계산은 `TrendQueryService.toSummary()`가 이미 하는 것과 같은 조합(`SubmissionRepository.findDistinctPlatforms` + `StageEvaluator.fromReach`)을 `WatchService`에서 독립적으로 호출한다(서비스 간 의존 없이 — 이 프로젝트의 기존 서비스들도 서로 안 부르고 각자 리포지토리만 쓴다).

**Tech Stack:** Spring Boot 3.3.5 / Java 17, Spring Data JPA, PostgreSQL/Flyway. `api-public` 모듈 관례(1단계와 동일)를 따라 컨트롤러/서비스 단위 테스트는 작성하지 않는다 — 빌드 컴파일 + curl 수동 검증으로 확인한다.

참고 스펙: [docs/superpowers/specs/2026-08-19-app-watch-design.md](../specs/2026-08-19-app-watch-design.md)

**스펙과 다른 점 하나**: 스펙 초안에 "이미 워치 중이면 200(멱등)"이라고 썼는데, OpenAPI 계약(`POST /v1/me/watch`)엔 `201`만 정의돼 있다. 굳이 상태 코드를 분기할 이유가 없으므로(프론트도 상태 코드 값을 안 씀 — `app/src/api/hooks.ts`의 `useToggleWatch`는 응답 바디/코드를 안 씀) **이미 있어도 항상 201**로 통일한다. 아래 태스크는 이 수정된 방침을 따른다.

---

### Task 1: `watches` 테이블 + `Watch` 엔티티 + `WatchRepository`

**Files:**
- Create: `backend/persistence/src/main/resources/db/migration/V19__watches.sql`
- Create: `backend/persistence/src/main/java/kr/trendstage/persistence/entity/Watch.java`
- Create: `backend/persistence/src/main/java/kr/trendstage/persistence/repo/WatchRepository.java`

- [ ] **Step 1: 마이그레이션**

```sql
-- V19 · 워치(관심 키워드 추적). merge_queue(V15)와 같은 스타일.
CREATE TABLE watches (
    id              UUID          PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID          NOT NULL REFERENCES users(id),
    keyword         VARCHAR(120)  NOT NULL,
    normalized_key  VARCHAR(160)  NOT NULL,
    created_at      TIMESTAMPTZ   NOT NULL DEFAULT now(),
    UNIQUE (user_id, normalized_key)
);
```

- [ ] **Step 2: `Watch` 엔티티** (`Endorsement.java` 패턴 그대로)

```java
package kr.trendstage.persistence.entity;

import jakarta.persistence.*;

import java.time.Instant;
import java.util.UUID;

/** 워치(관심 키워드 추적). 유저 단위 dedup(정규화 키 유니크). */
@Entity
@Table(name = "watches", uniqueConstraints = @UniqueConstraint(columnNames = {"user_id", "normalized_key"}))
public class Watch {

    @Id @GeneratedValue(strategy = GenerationType.UUID)
    private UUID id;

    @Column(name = "user_id", nullable = false)
    private UUID userId;

    @Column(nullable = false, length = 120)
    private String keyword;

    @Column(name = "normalized_key", nullable = false, length = 160)
    private String normalizedKey;

    @Column(name = "created_at", nullable = false, updatable = false)
    private Instant createdAt = Instant.now();

    protected Watch() {}

    public Watch(UUID userId, String keyword, String normalizedKey) {
        this.userId = userId;
        this.keyword = keyword;
        this.normalizedKey = normalizedKey;
    }

    public UUID getId() { return id; }
    public UUID getUserId() { return userId; }
    public String getKeyword() { return keyword; }
    public String getNormalizedKey() { return normalizedKey; }
}
```

- [ ] **Step 3: `WatchRepository`**

```java
package kr.trendstage.persistence.repo;

import kr.trendstage.persistence.entity.Watch;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;
import java.util.UUID;

public interface WatchRepository extends JpaRepository<Watch, UUID> {
    List<Watch> findByUserIdOrderByCreatedAtDesc(UUID userId);
    boolean existsByUserIdAndNormalizedKey(UUID userId, String normalizedKey);
    void deleteByUserIdAndNormalizedKey(UUID userId, String normalizedKey);
}
```

- [ ] **Step 4: 컴파일 확인**

```bash
cd backend && ./gradlew :persistence:compileJava
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: 커밋**

```bash
git add backend/persistence/src/main/resources/db/migration/V19__watches.sql backend/persistence/src/main/java/kr/trendstage/persistence/entity/Watch.java backend/persistence/src/main/java/kr/trendstage/persistence/repo/WatchRepository.java
git commit -m "feat(persistence): watches table + Watch entity/repo"
```

---

### Task 2: `WatchService` + 응답 레코드

**Files:**
- Create: `backend/api-public/src/main/java/kr/trendstage/apipublic/web/WatchRequest.java`
- Create: `backend/api-public/src/main/java/kr/trendstage/apipublic/web/WatchItemResponse.java`
- Create: `backend/api-public/src/main/java/kr/trendstage/apipublic/service/WatchService.java`

- [ ] **Step 1: 요청/응답 레코드**

```java
package kr.trendstage.apipublic.web;

import jakarta.validation.constraints.NotBlank;

public record WatchRequest(@NotBlank String keyword) {}
```

```java
package kr.trendstage.apipublic.web;

public record WatchItemResponse(String keyword, String stage, String note) {}
```

- [ ] **Step 2: `WatchService` 작성**

```java
package kr.trendstage.apipublic.service;

import kr.trendstage.apipublic.web.WatchItemResponse;
import kr.trendstage.domain.trend.DisplayStage;
import kr.trendstage.domain.trend.NameNormalizer;
import kr.trendstage.domain.trend.StageEvaluator;
import kr.trendstage.persistence.entity.TrendItem;
import kr.trendstage.persistence.entity.Watch;
import kr.trendstage.persistence.repo.SubmissionRepository;
import kr.trendstage.persistence.repo.TrendItemRepository;
import kr.trendstage.persistence.repo.WatchRepository;
import kr.trendstage.persistence.type.SubmissionResult;
import kr.trendstage.persistence.type.TrendState;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;
import java.util.UUID;

/** 워치(관심 키워드 추적) CRUD. 실제 푸시 발송은 스코프 밖(2단계-a 스펙 참고). */
@Service
public class WatchService {

    private final WatchRepository watches;
    private final TrendItemRepository trends;
    private final SubmissionRepository submissions;

    public WatchService(WatchRepository watches, TrendItemRepository trends, SubmissionRepository submissions) {
        this.watches = watches; this.trends = trends; this.submissions = submissions;
    }

    @Transactional
    public void add(UUID userId, String keyword) {
        String normalized = NameNormalizer.normalize(keyword);
        if (!watches.existsByUserIdAndNormalizedKey(userId, normalized)) {
            watches.save(new Watch(userId, keyword, normalized));
        }
    }

    @Transactional
    public void remove(UUID userId, String keyword) {
        watches.deleteByUserIdAndNormalizedKey(userId, NameNormalizer.normalize(keyword));
    }

    @Transactional(readOnly = true)
    public List<WatchItemResponse> list(UUID userId) {
        return watches.findByUserIdOrderByCreatedAtDesc(userId).stream()
                .map(this::toResponse)
                .toList();
    }

    private WatchItemResponse toResponse(Watch w) {
        TrendItem item = trends.findByNormalizedKey(w.getNormalizedKey()).orElse(null);
        if (item != null && item.getState() == TrendState.MERGED && item.getMergedInto() != null) {
            item = trends.findById(item.getMergedInto()).orElse(item);
        }
        if (item == null) {
            return new WatchItemResponse(w.getKeyword(), null, "아직 관측되지 않음");
        }
        List<String> platforms = submissions.findDistinctPlatforms(item.getId(), SubmissionResult.VOID);
        boolean resolvedFading = item.getState() == TrendState.RESOLVED;
        DisplayStage stage = StageEvaluator.fromReach(platforms.size(), resolvedFading);
        return new WatchItemResponse(w.getKeyword(), stage.name(), null);
    }
}
```

- [ ] **Step 3: 컴파일 확인**

```bash
cd backend && ./gradlew :api-public:compileJava
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 커밋**

```bash
git add backend/api-public/src/main/java/kr/trendstage/apipublic/web/WatchRequest.java backend/api-public/src/main/java/kr/trendstage/apipublic/web/WatchItemResponse.java backend/api-public/src/main/java/kr/trendstage/apipublic/service/WatchService.java
git commit -m "feat(app-api): WatchService — add/remove/list"
```

---

### Task 3: `WatchController` 라우팅 + 수동 검증

**Files:**
- Create: `backend/api-public/src/main/java/kr/trendstage/apipublic/web/WatchController.java`

- [ ] **Step 1: 컨트롤러 작성** (`MeController` 패턴)

```java
package kr.trendstage.apipublic.web;

import jakarta.validation.Valid;
import kr.trendstage.apipublic.service.WatchService;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.core.Authentication;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.UUID;

/** OpenAPI /v1/me/watch* 대응. 인증 필요(PublicSecurityConfig). */
@RestController
@RequestMapping("/v1/me/watch")
public class WatchController {

    private final WatchService service;

    public WatchController(WatchService service) { this.service = service; }

    @GetMapping
    public List<WatchItemResponse> list(Authentication auth) {
        return service.list(userId(auth));
    }

    @PostMapping
    public ResponseEntity<Void> add(Authentication auth, @Valid @RequestBody WatchRequest req) {
        service.add(userId(auth), req.keyword());
        return ResponseEntity.status(HttpStatus.CREATED).build();
    }

    @DeleteMapping("/{keyword}")
    public ResponseEntity<Void> remove(Authentication auth, @PathVariable String keyword) {
        service.remove(userId(auth), keyword);
        return ResponseEntity.noContent().build();
    }

    private UUID userId(Authentication auth) {
        return (UUID) auth.getPrincipal();
    }
}
```

(`DELETE /v1/me/watch/{keyword}`의 `keyword`는 앱이 `encodeURIComponent`로 URL 인코딩해서 보낸다 — Spring MVC가 `@PathVariable`에서 자동으로 디코딩하므로 별도 처리 불필요.)

- [ ] **Step 2: 컴파일**

```bash
cd backend && ./gradlew :api-public:compileJava :app:compileJava
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 백엔드 재기동 후 curl 검증**

포트 8080에 떠 있는 프로세스가 있으면 종료 후 재기동(`./gradlew :app:bootRun`, 백그라운드), `curl -s http://localhost:8080/v1/trends?daily=true`가 응답할 때까지 폴링.

인증 없이:
```bash
curl -s -w "\n%{http_code}\n" http://localhost:8080/v1/me/watch
```
Expected: 401 (모든 `/v1/me/watch*`는 인증 필요 — `PublicSecurityConfig`의 `.requestMatchers(HttpMethod.GET, "/v1/trends", "/v1/trends/*").permitAll()` 목록에 `/v1/me/**`는 없으므로 자동으로 `anyRequest().authenticated()`에 걸림).

Firebase ID 토큰을 확보할 수 있으면(1단계 Task 6/9와 동일한 방법 — 어려우면 이 부분은 스킵):
```bash
# 워치 추가
curl -s -w "\n%{http_code}\n" -X POST http://localhost:8080/v1/me/watch \
  -H "Authorization: Bearer <ID_TOKEN>" -H 'Content-Type: application/json' \
  -d '{"keyword":"제로슈거 첼로 챌린지"}'
```
Expected: 201

```bash
# 목록 조회 — stage가 채워지는지 확인
curl -s http://localhost:8080/v1/me/watch -H "Authorization: Bearer <ID_TOKEN>"
```
Expected: `[{"keyword":"제로슈거 첼로 챌린지","stage":"RISING","note":null}]` 형태(테스트 데이터에 이 항목이 있다는 전제 — 1단계 Task 3 검증 때 쓴 `c0ffee00-0000-0000-0000-000000000001` "제로슈거 첼로 챌린지"가 여전히 DB에 있으면 이 이름으로 매칭될 것).

```bash
# 존재하지 않는 키워드로 워치 추가 후 목록 확인
curl -s -X POST http://localhost:8080/v1/me/watch -H "Authorization: Bearer <ID_TOKEN>" -H 'Content-Type: application/json' -d '{"keyword":"존재안하는키워드zzz"}'
curl -s http://localhost:8080/v1/me/watch -H "Authorization: Bearer <ID_TOKEN>"
```
Expected: 두 번째 응답에 `{"keyword":"존재안하는키워드zzz","stage":null,"note":"아직 관측되지 않음"}` 포함.

```bash
# 같은 키워드 두 번 추가 — 멱등 확인
curl -s -X POST http://localhost:8080/v1/me/watch -H "Authorization: Bearer <ID_TOKEN>" -H 'Content-Type: application/json' -d '{"keyword":"제로슈거 첼로 챌린지"}'
```
Expected: 201(에러 아님). DB에서 `select count(*) from watches` 확인 시 같은 유저·같은 키워드로 행이 2개가 아니라 1개여야 함.

```bash
# 해제
curl -s -w "\n%{http_code}\n" -X DELETE "http://localhost:8080/v1/me/watch/$(python3 -c "import urllib.parse; print(urllib.parse.quote('제로슈거 첼로 챌린지'))")" -H "Authorization: Bearer <ID_TOKEN>"
```
Expected: 204

토큰 확보가 어려우면 401 확인까지만으로 충분(핵심 로직은 컴파일 타임 + 코드 리뷰로 검증됨).

- [ ] **Step 4: 커밋**

```bash
git add backend/api-public/src/main/java/kr/trendstage/apipublic/web/WatchController.java
git commit -m "feat(app-api): GET/POST/DELETE /v1/me/watch 라우팅"
```

---

### Task 4: `TrendDetailResponse.watched` 실데이터화

**Files:**
- Modify: `backend/api-public/src/main/java/kr/trendstage/apipublic/service/TrendQueryService.java`
- Modify: `backend/api-public/src/main/java/kr/trendstage/apipublic/web/TrendController.java`

- [ ] **Step 1: `TrendQueryService`에 `WatchRepository` 주입 + `detail()` 시그니처 변경**

생성자에 `WatchRepository watches` 추가(기존 `trends`/`submissions`/`verdicts`/`votes`는 유지). `detail(UUID id)`를 `detail(UUID id, UUID viewerId)`로 바꾸고, 마지막 `new TrendDetailResponse(...)` 호출의 `watched` 인자(현재 하드코딩된 `false`)를 다음으로 교체:

```java
        boolean watched = viewerId != null
                && watches.existsByUserIdAndNormalizedKey(viewerId, item.getNormalizedKey());
```

`import kr.trendstage.persistence.repo.WatchRepository;` 추가.

- [ ] **Step 2: `TrendController.detail()`에서 nullable `Authentication` 전달**

```java
    @GetMapping("/{id}")
    public TrendDetailResponse detail(@PathVariable UUID id, Authentication auth) {
        UUID viewerId = auth != null ? (UUID) auth.getPrincipal() : null;
        return service.detail(id, viewerId);
    }
```

(Spring MVC는 `Authentication` 파라미터가 있고 인증이 안 됐으면 `null`을 주입한다 — 별도 `required=false` 설정 불필요. `import org.springframework.security.core.Authentication;`은 이미 파일에 있음 — 없으면 추가.)

- [ ] **Step 3: 컴파일 확인**

```bash
cd backend && ./gradlew :api-public:compileJava
```
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 재기동 후 curl로 로그인 유무에 따른 `watched` 차이 확인**

인증 없이:
```bash
curl -s http://localhost:8080/v1/trends/c0ffee00-0000-0000-0000-000000000001 | grep -o '"watched":[a-z]*'
```
Expected: `"watched":false`

Task 3에서 이 항목을 워치에 추가한 토큰이 있으면, 같은 요청을 그 토큰으로:
```bash
curl -s http://localhost:8080/v1/trends/c0ffee00-0000-0000-0000-000000000001 -H "Authorization: Bearer <ID_TOKEN>" | grep -o '"watched":[a-z]*'
```
Expected: `"watched":true` (단, Task 3 마지막에 해제(DELETE)했다면 다시 추가해서 확인).

토큰이 없으면 비로그인 케이스(`watched:false`)만 확인하고 넘어간다.

- [ ] **Step 5: 커밋**

```bash
git add backend/api-public/src/main/java/kr/trendstage/apipublic/service/TrendQueryService.java backend/api-public/src/main/java/kr/trendstage/apipublic/web/TrendController.java
git commit -m "feat(app-api): TrendDetailResponse.watched 실데이터화"
```

---

## Out of Scope (다음 단계)

- 실제 푸시 알림 발송(디바이스 토큰 등록, 단계 전환 감지 배치)
- `PUT /v1/me/preferences` (다음 하위 기능)
- 일일 다이제스트 스냅샷 + 읽음 스트릭 + `GET /v1/me/summary` (그다음 하위 기능들)
