# SP0 원칙·문서 정비 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CLAUDE.md와 설계 문서 01~05를 2026-08-13 판정 모델 피벗(외부 지표 → 제보 시계열)과 2026-09-17 설계 재검토 결정(스펙 `docs/superpowers/specs/2026-09-17-design-review-design.md` §2)에 맞춰 정합화한다. 코드 변경은 주석 1곳뿐.

**Architecture:** 문서 6개 + Java 주석 1곳을 순차 편집. 각 태스크는 파일 하나를 다루며 "찾기 → 바꾸기" 쌍으로 정확한 교체 문구를 싣는다. 검증은 `grep`으로 피벗 이전 용어가 0건임을 확인. 작업 브랜치 `docs/design-review`(스펙이 이미 커밋돼 있음) → PR로 `main` 병합.

**Tech Stack:** Markdown, Git, ripgrep(`rg`) 또는 `grep`. 문서 위치는 저장소 루트(`D:\project\TRD\`) — `docs/`가 아니다.

**전제:**
- 현재 브랜치가 `docs/design-review`인지 확인: `git branch --show-current` → `docs/design-review`. 아니면 `git checkout docs/design-review`.
- 작업 트리에 미커밋 변경(`.gitignore`, `README.md`, `backend/app/src/main/resources/application.yml`)이 있다. **이 파일들은 건드리지 말고 커밋에 포함시키지 말 것.** `git add`는 항상 파일명을 명시한다.
- 편집은 Edit 도구(정확 문자열 교체)로 한다. 파일을 통째로 Write하지 않는다.

**용어 (스펙 §2 참조):** P1~P10 = 정책 결정, SP0~SP4 = 서브프로젝트, O8~O10 = 신규 열린 결정, R4/R5 = 개정 원칙.

---

## 파일 구조

| 파일 | 책임 | 태스크 |
|---|---|---|
| `CLAUDE.md` | 원칙 R1~R5·공식·배치 표·열린 결정·문서 경로 | 1 |
| `01-system-design.md` | 도메인·판정 엔진·점수·등급·배치 | 2 |
| `03-merge-clustering.md` | 병합 트랜잭션·SLA 근거·동시성·실패 모드 | 3 |
| `04-development-plan.md` | 원칙→가드 매핑·모듈·스키마·배치·로드맵·테스트 | 4 |
| `02-admin-console.md` | 콘솔 원칙·SLA·ADM-100/200/410/500/600/800·ADM-900 신설 | 5 |
| `05-screen-endpoint-map.md` | 엔드포인트 상태·권한·ADM-900 | 6 |
| `backend/api-admin/.../report/ReportAdminService.java` | 클래스 주석 1곳 | 7 |
| PR | `docs/design-review` → `main` | 8 |

---

### Task 1: CLAUDE.md — 원칙·공식·배치·경로

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: R4 교체**

찾기:
```
### R4. 자동은 플래그까지, 결정은 사람이

어뷰징 탐지·병합 후보 추출은 자동. 제재 확정·병합 확정은 사람.
오탐으로 인한 자동 강등은 절대 구현하지 않는다.
```
바꾸기:
```
### R4. 자동은 플래그와 가역적 보전 조치까지, 결정은 사람이

어뷰징 탐지·병합 후보 추출은 자동. 제재 확정·병합 확정·비공개 **확정**은 사람.
임시 비공개(`TEMP_HIDDEN`)는 되돌릴 수 있는 보전 조치이므로 신고 큐 SLA(4h) 초과 시 **자동 적용을 허용**한다(`sla_watch`). 사람이 확인해 해제하거나 확정한다.
오탐으로 인한 자동 강등·자동 제재 확정은 절대 구현하지 않는다.
```

- [ ] **Step 2: R5 교체**

찾기:
```
### R5. 지표는 누적치가 아니라 증가분으로 쓴다

해시태그 총 게시물 수, 갤러리 누적 글 수 같은 누적값은 유행의 증거가 아니다.
모든 지표는 `Δ(일별 증가분)` 또는 `기준선 대비 배수`로 변환 후 사용.
```
바꾸기:
```
### R5. 판정 입력은 제보 데이터뿐이며, 제보는 누적이 아니라 시간 분포로 본다

외부 사이트 지표(해시태그 게시물 수, 갤러리 글 수, 검색량 등)는 어떤 형태로도 판정에 들어가지 않는다.
"2주 안에 N명이 제보했다"는 단순 누적 카운트는 유행의 증거로 불충분하다. 서로 다른 제보자 수에 더해 **제보의 시간 분포(지속성)**, **플랫폼 다양성**, **제보자 독립성**을 신호로 쓴다.
시딩(`is_seed=true`)과 VOID 제보는 신호에서 제외한다.
(현행 코드는 제보자 수 축만 구현. 나머지 축은 SP4에서 Phase 0 백테스트와 함께 도입.)
```

- [ ] **Step 3: 도메인 개념 표의 T 행 보강**

찾기:
```
| **T**                 | 서로 다른 후속 제보자 수 / 목표치. 판정 점수 (0~1, clip) |
```
바꾸기:
```
| **T**                 | 서로 다른 후속 제보자 수 / 목표치. 판정 점수 (0~1, clip). 시딩·VOID 제외 |
```

- [ ] **Step 4: 점수 공식 — d를 AS로 이동**

찾기:
```
```
HIT :  Δ = + c × w_order × (1 + m) × d
MISS:  Δ = − c × 0.5 × d
VOID:  Δ = 0

c        확신도 (10 / 30 / 50)
w_order  선점 가중 (1위 1.0 / 2위 0.6 / 3위 0.4 / 4위↓ 0.2)
m        확산배수 (L1 0.2 / L2 0.5 / L3 1.0 / L4 1.5)
d        시간감쇠 = 0.5^(경과일/90)
```

**실패 페널티가 이득의 절반인 것은 의도된 설계다.**
```
바꾸기:
```
```
HIT :  Δ = + c × w_order × (1 + m)
MISS:  Δ = − c × 0.5
VOID:  Δ = 0
AS   = Σ Δ_i × 0.5^(원장 기록일로부터 경과일 / 90)

c        확신도 (10 / 30 / 50)
w_order  선점 가중 (1위 1.0 / 2위 0.6 / 3위 0.4 / 4위↓ 0.2)
m        확산배수 (L1 0.2 / L2 0.5 / L3 1.0 / L4 1.5)
```

**원장(`score_ledger`)에는 감쇠 없는 원값을 기록한다.** 반감기 90일 감쇠는 AS를 계산하는 조회 시점에만 적용한다. 원장에 감쇠를 미리 곱해 두면 "그때 얼마를 받았나"가 불변이 아니게 되고(R2), 반감기를 바꾸려면 과거 원장을 UPDATE해야 해 사실상 변경이 불가능해진다. 조회 시점에도 곱하면 이중 적용이다.
(현행 코드는 `ScoreEngine`이 Δ에 감쇠를 곱해 기록하고 `ActiveScore`가 다시 곱하는 **이중 적용** 상태다. SP1에서 원값 기록으로 전환.)

> ⚠ 조회 시점 감쇠는 반대급부가 있다 — 반감기를 바꾸면 **과거 AS가 전부 재계산된다.** ADM-600의 "기본값은 비소급 예약 적용"과 충돌하므로, 조회에 쓸 반감기를 전역 현재값으로 할지 원장 기록 시점의 파라미터 버전으로 할지는 O11(미결, SP1에서 결정).

**실패 페널티가 이득의 절반인 것은 의도된 설계다.**
```

- [ ] **Step 5: first_seen_at 경고 교체**

찾기:
```
### ⚠ first_seen_at 이 바뀌면 baseline 재계산 필수

baseline은 "최초 제보 시각 직전 14일". 병합으로 `first_seen_at`이 앞당겨지면 기존 `r_i`가 전부 무효가 된다.
**이걸 빼먹으면 에러도 안 나고 T값만 조용히 틀린다.** 반드시 테스트 케이스로 걸어둘 것.
```
바꾸기:
```
### ⚠ first_seen_at 이 바뀌면 order_rank 재계산, 판정 후면 병합 금지

병합으로 `first_seen_at`이 앞당겨지면 `order_rank`와 (SP4 이후) 시간 분포 신호가 바뀐다.
판정 전(PENDING)이면 병합 트랜잭션 안에서 재계산한다. 판정 후(RESOLVED)면 **병합 자체를 거부(409)**한다 — 흡수된 제보가 다음 판정에서 원장에 다시 실려 이중 점수가 된다. 판정 후 병합(ADJ 상쇄 경로)은 Phase 2(O9).
```

- [ ] **Step 6: 배치 잡 표 교체**

찾기:
```
| 주기             | 잡                 | 비고                                 |
| ---------------- | ------------------ | ------------------------------------ |
| 일 1회           | `cluster_merge`  | 임베딩 병합 + 관리자 큐 적재         |
| 일 1회           | `verdict_runner` | D+14 판정 → 원장 기록               |
| 일 1회           | `abuse_scan`     | 플래그만 생성, 제재는 안 함          |
| 주 1회(월 00:00) | `grade_recalc`   | 등급 재계산 + 제보권 리필(이월 없음) |
| 월 1회           | `l4_quota`       | L4 정원 재산정                       |
```
바꾸기:
```
| 주기             | 잡                 | 비고                                 |
| ---------------- | ------------------ | ------------------------------------ |
| 일 1회           | `cluster_merge`  | 임베딩 병합 + 관리자 큐 적재. ADM-900에서 수동 실행 가능 |
| 일 1회           | `verdict_runner` | D+14 판정 → 원장 기록. 판정 로직은 `JudgeService` 하나를 배치·재판정·시뮬이 공유(SP1) |
| 시간당           | `sla_watch`      | 신고 4h → 자동 임시 비공개 / 병합 24h → 판정 유예 연장 / 90일 미접속 관리자 비활성화 (SP3) |
| 주 1회(월 00:00) | `grade_recalc`   | 등급 재계산 + 제보권 리필(이월 없음) |
| 일 1회           | `abuse_scan`     | **미구현(Phase 2)**. 플래그만 생성, 제재는 안 함 |
| 월 1회           | `l4_quota`       | **미구현(Phase 3)**. L4 정원 재산정  |
```

- [ ] **Step 7: 2인 승인 대상에 관리자 계정 추가**

찾기:
```
**2인 승인 대상**: 유저 제재 확정 / 등급 수동 조정 / 파라미터 적용 / 100점 초과 상쇄 원장
```
바꾸기:
```
**2인 승인 대상**: 유저 제재 확정 / 등급 수동 조정 / 파라미터 적용 / 100점 초과 상쇄 원장 / 관리자 계정 생성·권한 변경(신규 계정은 생성 후 7일간 승인권 없음)

**통제는 서버 한 곳(`ApprovalGate`)에서.** 프론트 `CAN` 게이트는 표시용이며 통제가 아니다. 재판정(rejudge)이 만드는 ADJ 합계가 100점을 넘으면 같은 게이트를 지난다.

**ADM-900 배치 관리**: `cluster_merge` 수동 실행(OPERATOR/ADMIN). 실행 중이면 409.
```

- [ ] **Step 8: Phase 1에 쿼터 선행 조건 추가**

찾기:
```
운영진 시딩 주 20건 + 초대 유저 50~100명.
시딩분은 `is_seed=true`로 **점수 원장에 반영하지 않는다.**
```
바꾸기:
```
운영진 시딩 주 20건 + 초대 유저 50~100명.
시딩분은 `is_seed=true`로 **점수 원장에 반영하지 않고, 판정 신호(T)에도 넣지 않는다.** 시딩은 클러스터 앵커 역할만 한다.
**선행 조건: 제보권 쿼터·리필·VOID 반환 구현(SP1).** 쿼터 없는 베팅 구조는 난사를 보상한다.
```

- [ ] **Step 9: 열린 결정 표 갱신**

찾기:
```
| O1 | submitterTarget(목표 제보자 수)·판정 임계값 | **Phase 0 백테스트로 보정 필수**      |
```
바꾸기:
```
| O1 | submitterTarget(목표 제보자 수)·판정 임계값 | **상대값 방향 확정**(`max(하한, 활성 제보자 × 비율)`). 값은 SP4 백테스트 |
```

찾기:
```
| O7 | 임베딩 임계값 0.85 / 0.75                    | 한국어 트렌드 데이터로 튜닝 필요            |
```
바꾸기:
```
| O7 | 임베딩 임계값 0.85 / 0.75                    | 한국어 트렌드 데이터로 튜닝 필요            |
| O8 | 시간 분포·플랫폼 다양성·제보자 독립성 축의 가중치 | SP4                                    |
| O9 | 판정 후 병합(ADJ 상쇄 경로) 도입 시점        | Phase 2. 그 전까지 RESOLVED 병합은 409      |
| O10 | SLA 통보 채널(메신저 웹훅)                  | Phase 2. Phase 1은 로그                     |
```

- [ ] **Step 10: 코드 규약에 판정 규칙 추가**

찾기:
```
- 배치는 재실행 가능해야 한다(idempotent). `verdict_runner` 재실행이 점수를 두 번 주면 안 됨.
```
바꾸기:
```
- 배치는 재실행 가능해야 한다(idempotent). `verdict_runner` 재실행이 점수를 두 번 주면 안 됨. 멱등성은 코드 가드가 아니라 DB 제약(`verdicts` 부분 UNIQUE, `score_ledger(verdict_id, submission_id)` UNIQUE)으로 보장.
- 판정 로직은 `JudgeService` 하나. **배치 클래스 안에서 같은 빈의 `@Transactional` 메서드를 직접 호출하지 말 것** — 프록시를 타지 않아 트랜잭션이 무효가 되고 dirty-check 변경이 flush되지 않는다.
- 제약(UNIQUE·CHECK)은 코드 가드 대신 DB에 둔다.
```

- [ ] **Step 11: 상세 문서 경로 수정**

찾기:
```
- `docs/01-system-design.md` — 시스템 설계서 (도메인 모델, 점수 알고리즘, 등급 체계, 어뷰징 방어)
- `docs/02-admin-console.md` — 관리자 콘솔 설계서 (15개 화면, 권한 매트릭스, 와이어프레임)
- `docs/03-merge-clustering.md` — 병합·클러스터링 상세 (정규화 규칙, 트랜잭션 절차, 실패 모드)
```
바꾸기:
```
설계 문서는 저장소 **루트**에 있다(`docs/`가 아님).

- `01-system-design.md` — 시스템 설계서 (도메인 모델, 판정 신호, 점수 알고리즘, 등급 체계)
- `02-admin-console.md` — 관리자 콘솔 설계서 (16개 화면, 권한 매트릭스, 와이어프레임)
- `03-merge-clustering.md` — 병합·클러스터링 상세 (정규화 규칙, 트랜잭션 절차, 동시성, 실패 모드)
- `04-development-plan.md` — 개발 설계서 (모듈 구조, 스키마, 배치, 로드맵)
- `05-screen-endpoint-map.md` — 화면 ↔ 엔드포인트 매핑
- `docs/superpowers/specs/2026-09-17-design-review-design.md` — 설계 재검토 진단·결정·서브프로젝트(SP0~SP4)
```

- [ ] **Step 12: 검증**

Run:
```bash
grep -n "baseline\|기준선\|누적치\|docs/0" CLAUDE.md
```
Expected: 출력 없음(exit 1).

Run:
```bash
grep -c "sla_watch\|JudgeService\|ApprovalGate\|O9" CLAUDE.md
```
Expected: `4` 이상.

- [ ] **Step 13: 커밋**

```bash
git add CLAUDE.md
git commit -m "docs(CLAUDE): R4·R5 개정, 감쇠를 AS로 이동, sla_watch·JudgeService·ApprovalGate 반영

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: 01-system-design.md — 판정 엔진 재서술

**Files:**
- Modify: `01-system-design.md`

- [ ] **Step 1: 헤더**

찾기:
```
# 트렌드 제보 · 유저 등급 시스템 설계서 (v0.1)

> **문서 성격**: 초안. 임계값·가중치 등 수치 파라미터는 시딩 기간 데이터로 재보정하는 것을 전제로 함.
> **작성 기준일**: 2026-08-06
```
바꾸기:
```
# 트렌드 제보 · 유저 등급 시스템 설계서 (v0.2)

> **문서 성격**: 초안. 임계값 등 수치 파라미터는 Phase 0 백테스트로 재보정하는 것을 전제로 함.
> **작성 기준일**: 2026-08-06 · **개정**: 2026-09-17 (판정 모델 피벗 2026-08-13 반영 — 외부 지표 판정 → 제보 시계열 판정. 설계 재검토 결정은 `docs/superpowers/specs/2026-09-17-design-review-design.md`)
```

- [ ] **Step 2: 전제 A1**

찾기:
```
| A1 | 유저 제보는 **조기경보 신호**이며, 유행 여부의 최종 판정은 **자동 수집 지표**가 한다 | 유저 투표로 유행을 결정하지 않음 |
```
바꾸기:
```
| A1 | 유저 제보는 **조기경보 신호이자 판정 데이터**다. 유행 여부의 최종 판정은 **제보 시계열 집계**(4장)가 한다. 외부 사이트 지표는 쓰지 않는다 | 투표·좋아요·동의는 판정에 입력되지 않음 |
```

- [ ] **Step 3: 개요 다이어그램**

찾기:
```
                                    [지표 수집기 · 시간당 배치]
                                              ↓
                                    [D+14 자동 판정 엔진]
```
바꾸기:
```
                                    [제보 신호 집계 (시딩·VOID 제외)]
                                              ↓
                                    [D+14 자동 판정 엔진 · JudgeService]
```

- [ ] **Step 4: 엔티티 표 — metric_snapshots 제거**

찾기:
```
| `endorsements` | 중복 제보의 동의 처리분 | submission_id, user_id, created_at |
| `metric_snapshots` | 지표 시계열 | trend_item_id, source, metric, value, captured_at |
| `verdicts` | 판정 결과 | trend_item_id, result, reach_level, score_T, judged_at, evidence_json |
```
바꾸기:
```
| `endorsements` | 중복 제보의 동의 처리분(현행: 저장만, 점수·판정 미반영) | submission_id, user_id, created_at |
| `merge_queue` | 회색지대 병합 후보 큐 | new_trend_item_id, target_trend_item_id, status, assigned_to, decision_key |
| `verdicts` | 판정 결과(append-only, `supersedes`로 재판정) | trend_item_id, result, reach_level, score_T, judged_at, evidence_json |
```

- [ ] **Step 5: 중복 제보자 처리 — 상태 명시**

찾기:
```
**중복 제보자 처리**: 제보권을 소모하지 않고 `endorsements`로 전환. 적중 시 소액 점수(선점 4위 이하 가중치 적용), 실패 시 감점 없음. → *"남이 올린 걸 따라 올려 손해 볼까"* 하는 위축을 막으면서 데이터는 확보.
```
바꾸기:
```
**중복 제보자 처리**: 제보권을 소모하지 않고 `endorsements`로 전환. 적중 시 소액 점수(선점 4위 이하 가중치 적용), 실패 시 감점 없음. → *"남이 올린 걸 따라 올려 손해 볼까"* 하는 위축을 막으면서 데이터는 확보.
> 현행: `endorsements`는 저장만 되고 점수·판정에 반영되지 않는다. 반영 여부와 방식은 SP4(판정 신호 재설계) 백테스트와 함께 결정한다.
```

- [ ] **Step 6: 제보권 — 선행 조건 명시**

찾기:
```
매주 월요일 00:00 리필. **이월 없음**(비축 후 일괄 투척 방지).
```
바꾸기:
```
매주 월요일 00:00 리필. **이월 없음**(비축 후 일괄 투척 방지).

> **Phase 1 선행 조건(P3).** 쿼터가 없으면 "MISS 페널티 = HIT의 절반"이라는 의도된 비대칭이 역효과를 낸다 — 적중률 ~30%만 넘으면 난사가 이득. 현행 미구현(차감·리필·VOID 반환 모두 TODO). SP1에서 판정과 같은 트랜잭션 경계로 구현.
```

- [ ] **Step 7: §4 자동 판정 엔진 전체 교체**

`## 4. 자동 판정 엔진` 헤딩부터 `## 5. 점수 알고리즘` 직전의 `---` 까지(4.1 수집 지표 ~ 4.4 수집 관련 주의)를 아래로 교체한다. Edit 도구로 하려면 `### 4.1 수집 지표` 부터 `- **인스타 Graph API 해시태그 검색은 7일 창·계정당 30개 제한** 등 제약이 크다. S4는 보조 지표로만 취급.` 까지를 old_string으로 잡는다.

바꾸기(새 §4 본문, `## 4. 자동 판정 엔진` 헤딩 아래):
```
판정 입력은 **제보 데이터뿐**이다(R5). 외부 사이트 지표는 수집하지 않는다.

### 4.1 판정 신호 (TrendSignal)

D+14 도달 시 항목의 제보를 집계한다. 집계 대상에서 **시딩(`is_seed=true`)과 VOID 제보는 제외**한다.

| 축 | 정의 | 상태 |
|---|---|---|
| 서로 다른 제보자 수 `n` | 유효 제보의 distinct user 수 | 구현 |
| 시간 분포 | 2주 중 후속 제보가 들어온 날 수, 첫 48h 쏠림 비율 — 이미 뜬 것을 몰아서 적는 행동을 감점 | SP4 |
| 플랫폼 다양성 | 유효 제보의 distinct 플랫폼 수(플랫폼은 enum) | 집계만 구현, 미사용 → SP4 |
| 제보자 독립성 | 가입일·디바이스·IP 군집이 같은 제보자를 1명으로 압축 | SP4 |

`TrendSignal`은 SP1에서 확장 가능한 형태(제보 시각 목록·플랫폼·제보자 메타 포함)로 정의해 SP4가 파이프라인을 다시 열지 않게 한다.

### 4.2 종합 점수 T

```
T = clip( n / submitterTarget , 0, 1 )        # 현행
```

`submitterTarget`(기본 20)은 절대값이라 유저 규모와 무관하다 — Phase 1 유저 50~100명이 카테고리별로 흩어지면 도달 불가능해 전원 MISS로 수렴하고, TI 하락 → 등급 정체 → 이탈의 디플레 루프가 생긴다. 반대로 계정 4개면 HIT(0.20), 15개면 L4다.

**SP4에서 상대값으로 전환한다(P9):**
```
submitterTarget = max( 하한 , 최근 N일 활성 제보자 수 × 비율 )
```
하한·비율·기간은 파라미터 스튜디오(ADM-600)에서 관리하고 값은 Phase 0 백테스트로 정한다(O1). 다축 신호의 가중치는 O8.

### 4.3 판정 규칙

| 조건 | 결과 | reach_level | 확산배수 m |
|---|---|---|---|
| T < 0.20 | MISS | — | — |
| 0.20 ≤ T < 0.35 | HIT | L1 국지 | 0.2 |
| 0.35 ≤ T < 0.55 | HIT | L2 플랫폼 | 0.5 |
| 0.55 ≤ T < 0.75 | HIT | L3 크로스플랫폼 | 1.0 |
| T ≥ 0.75 | HIT | L4 매스 | 1.5 |

판정은 `JudgeService` 하나가 수행하며 배치(`verdict_runner`)·관리자 재판정·파라미터 시뮬레이션이 같은 서비스와 같은 파라미터 소스(`ParameterSetProvider`)를 쓴다(P6). 항목은 `PENDING → JUDGING → RESOLVED`로 전이하고, 판정·원장 기록·제보 result·항목 상태 변경이 한 트랜잭션이다.

### 4.4 VOID — 판정 공식의 출력이 아니라 사건의 결과

VOID는 `verdict_runner`가 계산하는 값이 아니다(P5). 아래 사건이 발생하면 **그 시점에** 제보가 VOID 처리되고 제보권이 반환된다. 판정 배치는 VOID 제보를 집계에서 제외할 뿐이다.

- 항목이 VOID됨(관리자 VOID 처리, 병합 시 흡수) → 해당 항목의 모든 제보
- 병합 후 같은 유저의 제보가 중복됨 → 늦은 쪽 (03 §4.4)
- 규정 위반(허위 URL, 미고지 이해관계)으로 관리자가 개별 제보를 VOID

판정 시점에 유효 제보가 0건이면 항목 자체가 VOID.
```

- [ ] **Step 8: §5.1 건별 점수 — d 제거**

찾기:
```
```
HIT :  Δ = + c × w_order × (1 + m) × d
MISS:  Δ = − c × 0.5 × d
VOID:  Δ = 0
```

| 기호 | 의미 | 값 |
|---|---|---|
| `c` | 확신도 | 10 / 30 / 50 |
| `w_order` | 선점 가중치 | 1위 1.0 / 2위 0.6 / 3위 0.4 / 4위 이하 0.2 |
| `m` | 확산배수 | 4.3 표 참조 |
| `d` | 시간감쇠 | `0.5 ^ (경과일수 / 90)` — 반감기 90일 |
```
바꾸기:
```
```
HIT :  Δ = + c × w_order × (1 + m)
MISS:  Δ = − c × 0.5
VOID:  Δ = 0
```

| 기호 | 의미 | 값 |
|---|---|---|
| `c` | 확신도 | 10 / 30 / 50 |
| `w_order` | 선점 가중치 | 1위 1.0 / 2위 0.6 / 3위 0.4 / 4위 이하 0.2 |
| `m` | 확산배수 | 4.3 표 참조 |

> **원장에는 감쇠 없는 원값을 기록한다.** 시간감쇠는 5.3 AS 계산에서만 적용. 원장에 감쇠를 미리 곱해 두면 "그때 얼마를 받았나"가 불변이 아니게 되고(R2) 반감기를 바꾸려면 과거 원장을 UPDATE해야 한다. 양쪽에 걸면 이중 적용(현행 코드 상태 — SP1에서 전환). 조회 시점 감쇠에 쓸 반감기 버전은 O11(미결).
```

- [ ] **Step 9: §5.3 AS — 감쇠 정의**

찾기:
```
```
AS = Σ Δ  (score_ledger 전체, 시간감쇠 반영 후 합)
```
```
바꾸기:
```
```
AS = Σ Δ_i × d_i ,    d_i = 0.5 ^ (원장 기록일로부터 경과일 / 90)     # 반감기 90일, 조회 시점 계산
```
```

- [ ] **Step 10: §6 등급 — 미구현 상태 명시**

찾기:
```
### 6.1 강등 규칙
```
바꾸기:
```
> 현행: L4는 `GradePolicy`에 없다(Phase 3). 6.1 강등 규칙은 미구현(Phase 2) — 구현 전까지 강등은 발생하지 않는다.

### 6.1 강등 규칙
```

- [ ] **Step 11: §7 어뷰징 표 — 외부 지표 행 정리**

찾기:
```
| 자가 유행 조작 (직접 확산시키고 적중 주장) | 제보자 계정과 확산 계정의 연결성 검사, 확산 초기 게시물의 계정 다양성 지표(1인당 게시물 수) 감시 |
| 뒷광고·대행사 제보 | 이해관계 고지 필수 + 미고지 적발 시 **AS 전액 소멸 + 3단계 강등** |
| 지표 조작 (총공) | 참여 계정 수 대비 게시물 수 비율 이상 시 해당 지표 가중치 자동 하향 |
```
바꾸기:
```
| 자가 유행 조작 (다중 계정으로 후속 제보 생성) | **제보자 독립성 압축**(4.1, SP4) — 같은 군집의 제보자는 판정 입력에서 1명으로 센다. 사후 제재가 아니라 판정 입력 단계에서 방어 |
| 뒷광고·대행사 제보 | 이해관계 고지 필수 + 미고지 적발 시 **AS 전액 소멸 + 3단계 강등** |
```

찾기:
```
**공통 원칙**: 자동 탐지는 **플래그까지만**, 계정 제재는 사람이 결정한다. 오탐으로 인한 강제 강등은 이의 제기 폭증과 신뢰 붕괴로 직결된다.
```
바꾸기:
```
**공통 원칙**: 자동 탐지는 **플래그까지만**, 계정 제재는 사람이 결정한다(R4). 오탐으로 인한 강제 강등은 이의 제기 폭증과 신뢰 붕괴로 직결된다. `abuse_scan` 잡은 미구현(Phase 2).
```

- [ ] **Step 12: §8 콜드 스타트 — Phase 0/1**

찾기:
```
| Phase 0 | ~4주 | 지표 수집기만 가동. 유저 노출 없음. 판정 임계값 보정용 데이터 축적 | 과거 사례 30건으로 판정 엔진 백테스트 |
| Phase 1 | 4~12주 | 운영진 시딩 (주 20건 이상). 초대 기반 클로즈드 베타 50~100명 | 주간 활성 제보자 30명 |
```
바꾸기:
```
| Phase 0 | ~4주 | 판정 엔진 가동. 과거 사례 30건을 제보 데이터로 재생해 백테스트. 유저 노출 없음 | submitterTarget·임계값 보정(O1) |
| Phase 1 | 4~12주 | 운영진 시딩 (주 20건 이상, T 집계 제외). 초대 기반 클로즈드 베타 50~100명. **선행 조건: 제보권 쿼터(SP1)** | 주간 활성 제보자 30명 |
```

찾기:
```
> **Phase 0 백테스트는 생략하면 안 된다.** 4.2의 가중치와 4.3의 임계값은 근거 없는 초기 추정치이며, 실데이터 보정 없이 공개하면 판정 시비가 서비스 초기에 신뢰를 무너뜨린다.
```
바꾸기:
```
> **Phase 0 백테스트는 생략하면 안 된다.** 4.2의 submitterTarget과 4.3의 임계값은 근거 없는 초기 추정치이며, 실데이터 보정 없이 공개하면 판정 시비가 서비스 초기에 신뢰를 무너뜨린다. SP4의 다축 신호도 같은 백테스트로 정당화한다.
```

- [ ] **Step 13: §9 API — metrics 제거**

찾기:
```
GET    /v1/trends/{id}/metrics      지표 시계열 (L2 이상)
```
바꾸기:
```
GET    /v1/trends/{id}              상세 (제보 시계열 요약, 잠정 T)
```

- [ ] **Step 14: §10 배치 표 교체**

찾기:
```
| 1시간 | `metric_collector` | 관측 중 항목 전체 지표 수집 → `metric_snapshots` |
| 일 1회 | `cluster_merge` | 신규 제보 임베딩 병합, 운영자 큐 적재 |
| 일 1회 | `verdict_runner` | D+14 도달 항목 판정 → `verdicts` + `score_ledger` |
| 일 1회 | `abuse_scan` | 어뷰징 룰 실행 → `abuse_flags` |
| 주 1회(월 00:00) | `grade_recalc` | AS·TI 재계산, 승급/강등, 제보권 리필 |
| 월 1회 | `l4_quota` | L4 정원 재산정 |
```
바꾸기:
```
| 일 1회 | `cluster_merge` | 신규 제보 임베딩 병합, 운영자 큐 적재. ADM-900 수동 실행 가능 |
| 일 1회 | `verdict_runner` | D+14 도달 항목 판정(`JudgeService`) → `verdicts` + `score_ledger` + 제보 result + 항목 RESOLVED |
| 1시간 | `sla_watch` | 신고 4h → 자동 임시 비공개 / 병합 24h → 판정 유예 연장 / 90일 미접속 관리자 비활성화 (SP3) |
| 주 1회(월 00:00) | `grade_recalc` | AS·TI 재계산, 승급, 제보권 리필 |
| 일 1회 | `abuse_scan` | **미구현(Phase 2)**. 어뷰징 룰 실행 → `abuse_flags` |
| 월 1회 | `l4_quota` | **미구현(Phase 3)**. L4 정원 재산정 |
```

- [ ] **Step 15: §11 법무 — 외부 데이터 항목 제거**

찾기:
```
- [ ] **외부 데이터 수집의 적법성**: 각 플랫폼 이용약관 및 저작권 검토 (4.4 참조). 공식 API 우선 원칙.
```
바꾸기:
```
- [ ] **신고 콘텐츠 자동 임시 비공개(4h)**의 절차·해제 기준을 약관에 명시 (P1)
```

- [ ] **Step 16: §12 미결 사항**

찾기:
```
| O1 | 4.2 지표 가중치, 4.3 임계값 | Phase 0 백테스트로 보정 필수 |
| O2 | 판정 유예기간 14일의 적정성 | 카테고리별(밈 vs 상품) 차등 검토 |
| O3 | 디시 데이터 수집 방식 확정 | 직접 수집 vs 상용 API 구매 — 법무 검토 후 결정 |
```
바꾸기:
```
| O1 | 4.2 submitterTarget(상대값 하한·비율·기간), 4.3 임계값 | 상대값 방향 확정(P9). 값은 SP4 백테스트 |
| O2 | 판정 유예기간 14일의 적정성 | 카테고리별(밈 vs 상품) 차등 검토. `sla_watch`가 병합 지연 시 연장 |
```

찾기:
```
| O6 | 독립 서비스 / 기존 커머스 부가모듈 여부 | 미확정. 후자일 경우 인증·회원 모듈 교체 및 과업 범위 협의 필요 |
```
바꾸기:
```
| O6 | 독립 서비스 / 기존 커머스 부가모듈 여부 | 미확정. 후자일 경우 인증·회원 모듈 교체 및 과업 범위 협의 필요 |
| O8 | 시간 분포·플랫폼 다양성·제보자 독립성 축의 가중치 | SP4 |
| O9 | 판정 후 병합(ADJ 상쇄 경로) 도입 시점 | Phase 2. 그 전까지 RESOLVED 병합은 409 |
| O10 | SLA 통보 채널 | Phase 2 메신저 웹훅. Phase 1은 로그 |
```

- [ ] **Step 17: 검증**

Run:
```bash
grep -n "metric_\|baseline\|기준선\|S1\b\|S3\b\|S4\b\|S5\b\|데이터랩\|Graph API\|r_i\|수집 지표\|× d" 01-system-design.md
```
Expected: 출력 없음.

- [ ] **Step 18: 커밋**

```bash
git add 01-system-design.md
git commit -m "docs(01): 판정 엔진을 제보 신호 기반으로 재서술, 외부 지표·metric_collector 제거

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: 03-merge-clustering.md — 트랜잭션·SLA·동시성

**Files:**
- Modify: `03-merge-clustering.md`

- [ ] **Step 1: 헤더**

찾기:
```
# 병합 · 클러스터링 상세 설계 (v0.1)
```
바꾸기:
```
# 병합 · 클러스터링 상세 설계 (v0.2)

> 개정 2026-09-17: 외부 지표(baseline·소급 재수집) 서술 제거, RESOLVED 병합 금지(P4), 클레임·멱등키·부분 UNIQUE 명시. 근거: `docs/superpowers/specs/2026-09-17-design-review-design.md` §2.3.
```

- [ ] **Step 2: §1 세 가지 붕괴**

찾기:
```
1. **지표가 쪼개진다** — 항목마다 따로 baseline을 잡고 T를 계산하니, 실제로는 크게 터진 유행인데 개별로는 전부 임계값(0.20) 미달로 MISS. 제보자 7명 전원 감점.
2. **선점 순위를 매길 수 없다** — 항목이 갈라지면 모두가 자기 항목의 1위가 된다.
3. **수집 쿼리가 누락된다** — "두바이 초콜릿"만 검색하면 "두바이초콜릿" 표기 게시물이 안 잡힌다.
```
바꾸기:
```
1. **제보자 수가 쪼개진다** — 항목마다 따로 T를 계산하니, 실제로는 크게 터진 유행인데 개별로는 전부 임계값(0.20) 미달로 MISS. 제보자 7명 전원 감점.
2. **선점 순위를 매길 수 없다** — 항목이 갈라지면 모두가 자기 항목의 1위가 된다.
3. **중복 감지가 누락된다** — 앱의 중복 감지(`GET /v1/trends?q=`)가 다른 표기를 못 잡아 같은 유행이 또 신규 항목으로 생긴다.
```

- [ ] **Step 3: §3 트랜잭션 절차**

찾기:
```
BEGIN TRANSACTION
  1. SELECT ... FROM trend_items WHERE id = :target FOR UPDATE
  2. submissions.trend_item_id = :target
  3. aliases[] 에 신규 정규화 키 추가
  4. canonical_name 재결정
  5. first_seen_at = MIN(기존, 신규)
  6. order_rank 전체 재계산
  7. 제보권 반환 (endorsement 전환 시)
  8. admin_audit_log 기록
COMMIT
  9. 수집 쿼리 재생성 + 소급 재수집 잡 큐잉   ← 트랜잭션 밖
```
바꾸기:
```
BEGIN TRANSACTION
  0. 멱등키 검사: merge_queue.decision_key = :Idempotency-Key 가 이미 있으면 이전 결과 반환
  1. SELECT ... FROM trend_items WHERE id IN (:target, :source) ORDER BY id FOR UPDATE   ← id 오름차순(데드락 회피)
  2. 상태 가드: 두 항목 모두 PENDING. JUDGING → 409(재시도), RESOLVED → 409(병합 불가, §3.2)
  3. submissions.trend_item_id = :target
  4. aliases[] 에 신규 정규화 키 추가
  5. canonical_name 재결정
  6. first_seen_at = MIN(기존, 신규)
  7. 같은 유저 중복 제보 → 늦은 쪽 VOID + 제보권 반환 (§4.4)
  8. order_rank 전체 재계산 (§3.1)
  9. merge_queue 상태 전이 + decision_key 기록
 10. admin_audit_log 기록 (사유·전후 스냅샷을 detail 에)
COMMIT
```

- [ ] **Step 4: §3.1 번호 참조**

찾기:
```
### 3.1 order_rank 재계산 (6번)
```
바꾸기:
```
### 3.1 order_rank 재계산 (8번)
```

- [ ] **Step 5: §3.2 first_seen_at 교체**

찾기:
```
### 3.2 first_seen_at 변경의 파급 (5번)

baseline은 "최초 제보 시각 직전 14일". `first_seen_at`이 앞당겨지면 기준선 구간이 통째로 이동하고 계산된 `r_i`가 전부 무효가 된다.

**→ `first_seen_at` 변경 시 baseline 재계산 잡을 반드시 트리거할 것.**
누락하면 에러 없이 T값만 조용히 틀린다. 로그에도 안 남는다. 테스트 케이스 필수.
```
바꾸기:
```
### 3.2 first_seen_at 변경의 파급 (6번)

`first_seen_at`이 앞당겨지면 두 가지가 바뀐다.
- `order_rank` — 8번에서 재계산되므로 트랜잭션 안에서 해결된다.
- 시간 분포 신호(SP4 이후) — 첫 제보 시각 기준의 활동일·쏠림 비율이 이동한다. 판정 시점에 재집계하므로 판정 전이면 문제 없다.

**판정 후(RESOLVED) 항목은 병합하지 않는다(2번 가드, P4).** 흡수된 제보가 다음 판정에서 원장에 다시 실려 이중 점수가 되기 때문이다. 판정 후 병합이 필요한 경우(ADJ 상쇄 경로)는 Phase 2(O9)에서 다룬다.
```

- [ ] **Step 6: §3.3 소급 재수집 → SLA 근거로 교체**

찾기:
```
### 3.3 소급 재수집 (9번)

alias가 늘었다는 건 그동안 그 표기의 게시물을 놓치고 있었다는 뜻.

| 소스 | 소급 수집 | 비고 |
|---|:--:|---|
| X | O | `since:`/`until:`. 조회 한도 소모 |
| 네이버 데이터랩 | O | 자유 |
| 디시 | O | 날짜 필터 |
| **인스타 Graph API** | **X** | 조회 창 제한 → **영구 결손** |

인스타는 소급이 불가능하다. 병합이 지연되면 그 기간 S4는 복구할 수 없다.
**→ 병합 검수 SLA 24시간의 실질적 근거.** 단순히 "빨리 처리하자"가 아니라 데이터가 사라지기 때문.
```
바꾸기:
```
### 3.3 병합 검수 SLA 24시간의 근거

병합이 늦어져 D+14가 병합 전에 도달하면, 항목은 제보자 수·order_rank가 미완 상태로 판정된다. 판정 후에는 병합할 수 없으므로(§3.2) 이 시점을 놓치면 되돌릴 수 없다.

**→ `sla_watch`(시간당)가 24h 초과 큐 항목의 대상 항목 `grace_until`을 +24h 연장한다.** 연장은 판정 *시점*만 미루며 결과에 개입하지 않는다(R1). 현행은 배너 문구만 있고 연장은 사람이 수동 호출해야 한다 — SP3에서 자동화.
```

- [ ] **Step 7: §4.3 순위 뒤집힘 — RESOLVED 행**

찾기:
```
| 양쪽 모두 PENDING | 문제 없음. 판정 전이라 점수 미발생 |
| 하나라도 RESOLVED | **`ADJ` 상쇄 원장 추가** (원칙 R2). 기존 원장 수정 금지 |

영향받은 유저에게 **자동 통보 필수.** 점수가 소리 없이 바뀌면 반드시 분쟁이 된다.
```
바꾸기:
```
| 양쪽 모두 PENDING | 문제 없음. 판정 전이라 점수 미발생 |
| 하나라도 JUDGING | 409. 판정 트랜잭션이 끝난 뒤 재시도 |
| 하나라도 RESOLVED | **병합 거부(409)** (P4). 판정 후 병합(`ADJ` 상쇄 경로, R2)은 Phase 2(O9) |

ADJ 경로를 도입할 때는 영향받은 유저에게 **자동 통보 필수.** 점수가 소리 없이 바뀌면 반드시 분쟁이 된다.
```

- [ ] **Step 8: §4.5 삭제**

찾기:
```
### 4.5 metric_snapshots 합산 금지
두 클러스터의 지표를 더하면 검색어가 겹치는 구간에서 같은 게시물이 이중 계수된다.
→ 통합 쿼리로 **전 구간 재수집**. 재수집 불가 소스는 결측 처리 후 해당 지표 가중치를 나머지에 재배분.

---
```
바꾸기:
```
---
```

- [ ] **Step 9: §6 동시성 제어 교체**

찾기:
```
검수자가 여럿일 때 같은 제보를 서로 다른 클러스터로 동시에 병합하는 상황이 실제로 발생한다.

| 장치 | 내용 |
|---|---|
| 큐 항목 클레임 | 카드 오픈 시 5~10분 소프트 락. 만료 시 큐 복귀 |
| 낙관적 락 | `trend_items.version` 으로 커밋 시점 충돌 감지 → "다른 검수자가 이미 처리했습니다" 후 최신 상태 재표시 |
| 멱등성 키 | 네트워크 재시도로 인한 이중 실행 방지 |
```
바꾸기:
```
검수자가 여럿일 때 같은 제보를 서로 다른 클러스터로 동시에 병합하는 상황이 실제로 발생한다. 검수자 A "병합" + B "분리"는 서로 다른 행을 건드려 낙관적 락만으로는 잡히지 않는다 — **큐 항목 자체를 잠가야 한다.**

| 장치 | 내용 |
|---|---|
| 큐 항목 클레임 | 카드 오픈 시 `merge_queue.assigned_to`·`claimed_at` 기록, 15분 만료 후 큐 복귀. 클레임 중인 항목은 다른 검수자에게 읽기 전용 |
| 큐 상태 모델 | `PENDING → CLAIMED → (MERGED \| SEPARATED \| VOIDED \| HELD)`. `HELD` 3회 → `ESCALATED`(OPERATOR 큐). 보류와 분리는 다른 상태 |
| 행 잠금 | 병합 트랜잭션에서 두 항목을 id 오름차순 `FOR UPDATE`(§3 1번) |
| 낙관적 락 | `trend_items.version` 으로 커밋 시점 충돌 감지 → "다른 검수자가 이미 처리했습니다" 후 최신 상태 재표시 |
| 멱등성 키 | `Idempotency-Key` 헤더 → `merge_queue.decision_key UNIQUE`. 같은 키 재요청은 이전 결과 반환 |
| 완전일치 유일성 | `trend_items(normalized_key) WHERE state <> 'MERGED'` 부분 UNIQUE. 제보 삽입이 충돌하면 기존 항목에 합류(조회-후-삽입 레이스 제거) |

현행은 트랜잭션만 구현돼 있다(잠금·클레임·멱등키·부분 UNIQUE 없음). SP2에서 구현.
```

- [ ] **Step 10: §7 실패 모드 표와 복구 비용**

찾기:
```
| **과병합** (over-merge) | 별개 유행을 합침. 뒤 제보자가 부당하게 4위 이하로 밀림. 지표 혼입으로 T 왜곡 | 중 |
```
바꾸기:
```
| **과병합** (over-merge) | 별개 유행을 합침. 뒤 제보자가 부당하게 4위 이하로 밀림. 제보자 수 혼입으로 T 왜곡 | 중 |
```

찾기:
```
### 복구 비용 순위
1. **인스타 데이터 결손** — 복구 불가
2. **확정 점수 정정** — 상쇄 원장 + 통보 + 이의 대응
3. **baseline 재계산 누락** — 에러 없이 조용히 틀림. **발견이 가장 늦다**

3번이 특히 위험하므로 `first_seen_at` 변경 → baseline 재계산 트리거에 반드시 테스트를 건다.
```
바꾸기:
```
### 복구 비용 순위
1. **확정 점수 정정** — 상쇄 원장 + 통보 + 이의 대응. 판정 후 병합을 막는 이유(§3.2)
2. **과분할로 인한 전원 MISS** — 판정 전에 발견하면 병합으로 복구, 판정 후면 1번
3. **과병합** — 분리(5장)로 복구. 판정 전이면 점수 영향 없음

RESOLVED 항목 병합 시도 → 409 는 반드시 테스트로 건다.
```

- [ ] **Step 11: §8 M5**

찾기:
```
| M5 | 소프트 락 5~10분 | 검수 평균 소요시간 측정 후 확정 |
```
바꾸기:
```
| M5 | 클레임 만료 15분 | 검수 평균 소요시간 측정 후 확정 |
```

- [ ] **Step 12: 검증**

Run:
```bash
grep -n "baseline\|기준선\|소급 재수집\|metric_snapshots\|인스타\|데이터랩\|S4\|수집 쿼리" 03-merge-clustering.md
```
Expected: 출력 없음.

- [ ] **Step 13: 커밋**

```bash
git add 03-merge-clustering.md
git commit -m "docs(03): baseline·소급 재수집 제거, RESOLVED 병합 금지, 클레임·멱등키·부분 UNIQUE 명시

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: 04-development-plan.md — 가드 매핑·모듈·배치·로드맵

**Files:**
- Modify: `04-development-plan.md`

- [ ] **Step 1: 헤더**

찾기:
```
> **작성 기준일**: 2026-08-06
> **확정 스택**: 앱 = React Native(Expo) + Java 백엔드 / 관리자 = React(웹) + Java 백엔드
```
바꾸기:
```
> **작성 기준일**: 2026-08-06 · **개정**: 2026-09-17 (외부 수집기 제거, `sla_watch`·`JudgeService`·`ApprovalGate` 반영. 근거: `docs/superpowers/specs/2026-09-17-design-review-design.md`)
> **확정 스택**: 앱 = React Native(Expo) + Java 백엔드 / 관리자 = React(웹) + Java 백엔드
```

- [ ] **Step 2: §0 표**

찾기:
```
| 정한다 | 정하지 않는다 (미결로 이관) |
|---|---|
| 기술 스택, 리포 구조, 모듈 경계 | 지표 가중치·임계값 (O1 — Phase 0 백테스트) |
| 백엔드 레이어링과 도메인 모듈 | 임베딩 모델 선정 (M2), 디시 수집 방식 (O3) |
```
바꾸기:
```
| 정한다 | 정하지 않는다 (미결로 이관) |
|---|---|
| 기술 스택, 리포 구조, 모듈 경계 | submitterTarget 상대값·임계값 (O1 — SP4 백테스트) |
| 백엔드 레이어링과 도메인 모듈 | 임베딩 모델 선정 (M2), 다축 신호 가중치 (O8) |
```

- [ ] **Step 3: §1 R4·R5 가드**

찾기:
```
| **R4** 자동은 플래그까지 | `abuse_scan`은 `abuse_flags` INSERT만. 제재는 `sanctions` 상신→2인 승인 상태머신. 자동 강등 코드 경로 부재. |
| **R5** 지표는 증가분(Δ)·배수 | `metric_snapshots`는 원시값 저장, `T` 계산은 항상 baseline 대비 `r_i`로 변환하는 `ScoreEngine`을 경유. 누적값 직접 참조 금지(정적 분석 규칙 후보). |
```
바꾸기:
```
| **R4** 자동은 플래그와 가역적 보전 조치까지 | `abuse_scan`은 `abuse_flags` INSERT만. 제재는 `sanctions` 상신→2인 승인 상태머신. 자동 강등 코드 경로 부재. `sla_watch`가 적용하는 `TEMP_HIDDEN`은 가역적이며 확정은 사람(ADM-410). |
| **R5** 판정 입력은 제보뿐 | `VerdictEngine`은 `TrendSignal`(제보 집계)만 입력받는다. 외부 지표 테이블·수집기는 존재하지 않는다. 시딩·VOID는 집계에서 제외. 시간 분포·플랫폼·독립성 축은 SP4. |
```

- [ ] **Step 4: §2.1 배치 행**

찾기:
```
| 배치 | **Spring Scheduler + ShedLock(JDBC 백엔드)** → 규모 커지면 Spring Batch | 잡 6종(§6) 멱등·재실행 필수. ShedLock JDBC로 다중 인스턴스 단일 실행(초기 Redis 불필요) |
```
바꾸기:
```
| 배치 | **Spring Scheduler + ShedLock(JDBC 백엔드)** → 규모 커지면 Spring Batch | 잡 4종(§6: `cluster_merge`·`verdict_runner`·`grade_recalc`·`sla_watch`) + Phase 2/3 `abuse_scan`·`l4_quota`. 멱등·재실행 필수. ShedLock JDBC로 다중 인스턴스 단일 실행 |
```

- [ ] **Step 5: §3 아키텍처 다이어그램**

찾기:
```
        [PostgreSQL+pgvector] [Redis]   [외부수집: X/네이버DL/인스타/디시]
                                                │
                                          [Embedding 서비스] (M2)
```
바꾸기:
```
        [PostgreSQL+pgvector] [Redis(Phase 2)]   [Embedding 서비스 (TEI, KURE-v1)]
```

찾기:
```
배치 실행: 스케줄러가 `domain-core`의 서비스를 호출. 잡은 전부 멱등(§6). 외부 수집기는 어댑터 패턴(`MetricSource` 인터페이스)으로 소스별 구현 — 디시(O3)·인스타 제약(소급 불가)을 어댑터 단에서 격리.
```
바꾸기:
```
배치 실행: 스케줄러가 `merge`·`JudgeService` 등 서비스 빈을 호출한다. 잡은 전부 멱등(§6). **배치 클래스 안에서 같은 빈의 `@Transactional` 메서드를 직접 호출하지 않는다** — 프록시를 타지 않아 트랜잭션이 무효가 된다. 판정·병합처럼 트랜잭션이 필요한 작업은 별도 서비스 빈으로 두고 배치는 목록 순회만 한다.
```

- [ ] **Step 6: §4 모듈 구조**

찾기:
```
├─ api-admin/          # /admin/** 콘솔 API + 세션·RBAC·2FA
├─ collectors/         # MetricSource 어댑터 (X, 네이버DL, 인스타, 디시)
├─ scheduler/          # 배치 잡 6종 + ShedLock
```
바꾸기:
```
├─ api-admin/          # /admin/** 콘솔 API + 세션·RBAC·2FA · ApprovalGate
├─ merge/              # 클러스터 병합 후보 추출·병합 트랜잭션 (배치·관리자 공용)
├─ scheduler/          # 배치 잡 + ShedLock (목록 순회만, 트랜잭션은 서비스 빈)
├─ api-spec/           # OpenAPI (앱/콘솔 타입 코드생성 원본)
```

찾기:
```
│   ├─ verdict/        VerdictEngine (T 종합점수, reach_level 판정)
```
바꾸기:
```
│   ├─ verdict/        VerdictEngine (TrendSignal → T, reach_level 판정)
```

- [ ] **Step 7: §5.1 스키마 표**

찾기:
```
| `endorsements` | 유저 단위 dedup |
| `metric_snapshots` | 원시값. `(trend_item_id, source, metric, captured_at)` 인덱스 |
| `verdicts` | **append-only**. `supersedes`, `evidence_json`(order_rank·baseline 스냅샷 동결) |
| `score_ledger` | **append-only**. `delta`, `reason`, `kind`(HIT/MISS/VOID/ADJ) |
```
바꾸기:
```
| `endorsements` | 유저 단위 dedup. 현행 저장만(점수·판정 미반영) |
| `merge_queue` | `status`, `assigned_to`·`claimed_at`(클레임), `hold_count`, `decision_key UNIQUE`(멱등키) — SP2 |
| `verdicts` | **append-only**. `supersedes`, `evidence_json`(order_rank·TrendSignal 스냅샷 동결). `(trend_item_id) WHERE supersedes IS NULL` 부분 UNIQUE — SP1 |
| `score_ledger` | **append-only**. `delta`(감쇠 없는 원값), `reason`, `kind`(HIT/MISS/VOID/ADJ), `approved_by_admin_id`. `(verdict_id, submission_id)` UNIQUE — SP1 |
```

찾기:
```
| `trend_items` | `canonical_name`, `aliases text[]`, `first_seen_at`, `state`, `version`(낙관적 락), `merged_into`(tombstone) |
```
바꾸기:
```
| `trend_items` | `canonical_name`, `aliases text[]`, `first_seen_at`, `state`, `version`(낙관적 락), `merged_into`(tombstone). `(normalized_key) WHERE state <> 'MERGED'` 부분 UNIQUE — SP2 |
```

- [ ] **Step 8: §5.2 공식**

찾기:
```
HIT :  Δ = + c × w_order × (1 + m) × d
MISS:  Δ = − c × 0.5 × d          (실패=이득의 절반, 의도된 비대칭 — 변경 금지)
VOID:  Δ = 0
d = 0.5^(경과일/90)   TI = (HIT+2)/(HIT+MISS+5)  (180일, α2 β3, 초기 0.4)
```
바꾸기:
```
HIT :  Δ = + c × w_order × (1 + m)
MISS:  Δ = − c × 0.5              (실패=이득의 절반, 의도된 비대칭 — 변경 금지)
VOID:  Δ = 0
AS   = Σ Δ_i × 0.5^(원장 경과일_i/90)   ← 감쇠는 조회 시점에만. 원장은 원값
TI   = (HIT+2)/(HIT+MISS+5)  (180일, α2 β3, 초기 0.4)
```

- [ ] **Step 9: §5.4 회귀 케이스**

찾기:
```
**반드시 테스트로 거는 회귀 케이스**(에러 없이 조용히 틀리는 유형):
1. `first_seen_at` 변경 → **baseline 재계산 잡 트리거**(docs/03 §3.2). 누락 시 T만 틀림.
2. 클러스터 병합 시 `metric_snapshots` **합산 금지**, 통합쿼리 재수집(docs/03 §4.5).
3. 같은 유저 A·B 제보 병합 → 늦은 쪽 **VOID + 제보권 반환**(docs/03 §4.4).
4. RESOLVED 상태 병합/분리 → **ADJ 상쇄원장 + 유저 통보**(docs/03 §4.3).

병합 트랜잭션: 단일 트랜잭션 + `FOR UPDATE` 행잠금 + 멱등성 키 + `version` 낙관적 락(docs/03 §6). 소급 재수집은 트랜잭션 밖 큐잉.
```
바꾸기:
```
**반드시 테스트로 거는 회귀 케이스**(에러 없이 조용히 틀리는 유형):
1. `first_seen_at` 변경 → **order_rank 재계산**이 같은 트랜잭션에서 일어남(03 §3.2).
2. 같은 유저 A·B 제보 병합 → 늦은 쪽 **VOID + 제보권 반환**(03 §4.4).
3. RESOLVED 상태 병합 시도 → **409**(03 §4.3, P4). ADJ 상쇄 경로는 Phase 2(O9).
4. 판정 배치 1회 실행 후 `submissions.result`·`trend_items.state`가 **DB에 반영**되어 있음(self-invocation 회귀, SP1).

병합 트랜잭션: 단일 트랜잭션 + 두 항목 id 오름차순 `FOR UPDATE` + `Idempotency-Key`→`decision_key UNIQUE` + `version` 낙관적 락 + 큐 클레임(03 §6).
```

- [ ] **Step 10: §6 배치 표**

찾기:
```
| 1시간 | `metric_collector` | `(item,source,captured_at)` upsert. 소스장애→VOID 위험 알림 |
| 일 1회 | `cluster_merge` | 처리済 마킹 + 멱등키. 회색지대는 큐 적재만 |
| 일 1회 | `verdict_runner` | **재실행이 점수 두 번 주면 안 됨** — `verdicts` 존재검사 후 skip. D+14 도달분만 |
| 일 1회 | `abuse_scan` | 플래그 INSERT만(R4) |
| 주 1회(월 00:00) | `grade_recalc` | AS/TI 재계산·승강급·제보권 리필(**이월 없음**) |
| 월 1회 | `l4_quota` | 정원 재산정, 초과분 L3 이동(페널티 아님 표기) |
```
바꾸기:
```
| 일 1회 | `cluster_merge` | 처리済 마킹 + 멱등키. 회색지대는 큐 적재만. ADM-900 수동 실행(같은 ShedLock 이름, 실행 중 409) |
| 일 1회 | `verdict_runner` | `JudgeService.judge()` 순회. **재실행이 점수 두 번 주면 안 됨** — `verdicts` 부분 UNIQUE + `score_ledger(verdict_id, submission_id)` UNIQUE로 DB가 보장 |
| 1시간 | `sla_watch` | 신고 4h→`TEMP_HIDDEN`+에스컬레이션 / 병합 24h→`grace_until` +24h / 90일 미접속 관리자 비활성화. 통보는 `SlaNotifier`(Phase 1 로그) — SP3 |
| 주 1회(월 00:00) | `grade_recalc` | AS/TI 재계산·승급·제보권 리필(**이월 없음**) |
| 일 1회 | `abuse_scan` | **미구현(Phase 2)**. 플래그 INSERT만(R4) |
| 월 1회 | `l4_quota` | **미구현(Phase 3)**. 정원 재산정, 초과분 L3 이동(페널티 아님 표기) |
```

- [ ] **Step 11: §7 API**

찾기:
```
GET  /v1/trends/{id}/metrics    지표 시계열(L2+)
```
바꾸기:
```
```
(해당 줄 삭제 — old_string에 줄바꿈까지 포함해 빈 문자열로 교체)

찾기:
```
큐(병합/어뷰징/이의/신고), 트렌드 상세·판정관리(예외만), 수집모니터, 유저원장(ADJ 추가), 파라미터 스튜디오(드래프트→시뮬→2인승인), 감사로그. **판정결과 직접변경·T 수동입력·verdict 삭제 엔드포인트는 존재하지 않음**(ADM-200 금지기능).
```
바꾸기:
```
큐(병합/어뷰징/이의/신고), 트렌드 상세·판정관리(예외만), 유저원장(ADJ 추가), 파라미터 스튜디오(드래프트→시뮬→2인승인), 배치 관리(ADM-900), 감사로그. **판정결과 직접변경·T 수동입력·verdict 삭제 엔드포인트는 존재하지 않음**(ADM-200 금지기능). 재판정은 `JudgeService`를 승인된 파라미터로 다시 돌리며, ADJ 합계 100점 초과는 `ApprovalGate`를 지난다.
```

- [ ] **Step 12: §8 횡단 관심사**

찾기:
```
| 2인 승인(4-eyes) | 유저제재·등급수동조정·파라미터적용·상쇄원장 100점 초과. `approval_requests` 상태머신(요청→1인→2인→실행), 요청자≠승인자 검증 |
| 감사 로그 | **Phase 0부터 필수**(ADM-700, 소급 불가). 상태변경·원장추가·정책변경·**조회행위**·인증 기록. 해시체인/WORM |
```
바꾸기:
```
| 2인 승인(4-eyes) | 유저제재·등급수동조정·파라미터적용·상쇄원장 100점 초과·**관리자 계정 생성/권한 변경**. 서버 한 곳 `ApprovalGate.require(kind, amount)`가 강제, `approval_requests` 상태머신(요청→1인→2인→실행), 요청자≠승인자 검증, 신규 계정 7일 승인권 유예(P8) |
| 감사 로그 | **Phase 0부터 필수**(ADM-700, 소급 불가). 상태변경·원장추가·정책변경·**조회행위**(`PII_VIEW`는 서버 엔드포인트에서 기록, 클라 토글 금지)·인증 기록. API는 `detail` 포함. 해시체인/WORM |
```

찾기:
```
| 알림 | 앱: expo-notifications(아침 정시 1건 + 급상승 진입). 콘솔: SLA초과·수집장애 사내 웹훅 |
```
바꾸기:
```
| 알림 | 앱: expo-notifications(아침 정시 1건 + 급상승 진입). 콘솔: SLA초과 `SlaNotifier`(Phase 1 로그, Phase 2 사내 웹훅 — O10) |
```

- [ ] **Step 13: §9.2 콘솔**

찾기:
```
- 레이아웃: 좌측 다크 사이드바(네비 그룹+큐 배지) + 상단바(화면ID·역할·"모든 조회는 감사됨") + 본문. **데스크톱 전용**.
- 진입 = **ADM-010 오늘의 작업**(큐 카운트·SLA초과·수집장애), 대시보드 아님(P1).
```
바꾸기:
```
- 레이아웃: 좌측 다크 사이드바(네비 그룹+큐 배지) + 상단바(화면ID·역할) + 본문. **데스크톱 전용**. 큐 배지와 ADM-010 타일은 단일 `GET /admin/queues/summary`에서 그린다(P10) — 하드코딩·픽스처 금지, 미구현 큐는 "미구현" 표시.
- 진입 = **ADM-010 오늘의 작업**(큐 카운트·SLA초과), 대시보드 아님(P1).
- "감사 로그에 기록됩니다" 문구는 서버가 실제로 기록하는 곳에만 둔다(SP3 전까지 표시 금지).
```

- [ ] **Step 14: §10 로드맵**

찾기:
```
- **백엔드 우선**: domain-core(ScoreEngine·VerdictEngine·MergeService) + persistence + append-only 트리거 + 배치(metric_collector·verdict_runner) + audit(ADM-700).
```
바꾸기:
```
- **백엔드 우선**: domain-core(ScoreEngine·VerdictEngine·MergeService) + persistence + append-only 트리거 + 배치(verdict_runner→`JudgeService`) + audit(ADM-700).
```

찾기:
```
- 백엔드: 제보 파이프라인·cluster_merge·endorsement·제보권.
```
바꾸기:
```
- 백엔드: 제보 파이프라인·cluster_merge·endorsement·**제보권 쿼터(선행 조건, SP1)**·`sla_watch`(SP3).
- 선행 서브프로젝트: SP0.5(보안 즉시 수정) → SP1(판정 파이프라인) → SP2(병합 무결성) → SP3(콘솔 통제·SLA). 상세는 `docs/superpowers/specs/2026-09-17-design-review-design.md` §3.
```

찾기:
```
- 콘솔 추가: ADM-300/310/311/320/400/**410(신고 큐, 공개 전 필수·법적리스크)**/610.
```
바꾸기:
```
- 콘솔 추가: ADM-300/310/320/400/610. (ADM-311·410은 Phase 1로 앞당김 — 311은 이의 대응 근거 화면, 410은 이미 구현됨)
- SP4(판정 신호 재설계) + Phase 0 백테스트 30건.
```

- [ ] **Step 15: §11 테스트 전략**

찾기:
```
| 회귀(조용한 버그) | **first_seen_at 변경→baseline 재계산 트리거**(docs/03 §7 3번, 발견 가장 늦음). metric 합산 금지. 같은유저 dedup→VOID |
| 멱등 | verdict_runner·grade_recalc 재실행 시 점수 불변 |
```
바꾸기:
```
| 회귀(조용한 버그) | first_seen_at 변경→order_rank 재계산. RESOLVED 병합→409. 같은유저 dedup→VOID+제보권 반환. **판정 배치 후 submissions.result·trend_items.state DB 반영**(self-invocation) |
| 멱등 | verdict_runner·grade_recalc 재실행 시 점수 불변 — DB UNIQUE 제약 위반으로 증명 |
| 동시성(큐) | 검수자 A 병합 + B 분리 동시 → 클레임으로 하나만 진행. 같은 `Idempotency-Key` 재요청 동일 응답 |
```

- [ ] **Step 16: §12 DevOps**

찾기:
```
- 로컬: `infra/docker-compose`(Postgres+pgvector, 백엔드, mock 수집기). Redis는 Phase 2에 추가(§2.5).
```
바꾸기:
```
- 로컬: `infra/docker-compose`(Postgres+pgvector, 임베딩 TEI). Redis는 Phase 2에 추가(§2.5). `application.yml`의 DB 기본 호스트는 `localhost`(사설 IP 금지 — SP0.5).
```

찾기:
```
- 환경 분리: local/staging/prod 프로파일. 외부 API 키(X·인스타·네이버DL)는 시크릿 매니저. **X API 한도·가격 계약 전 재확인, 디시 수집은 법무 승인 게이트(O3)**.
```
바꾸기:
```
- 환경 분리: local/staging/prod 프로파일. Firebase 서비스 계정 키는 시크릿 매니저. 로컬 오버라이드는 `application-local.yml`(gitignore).
```

- [ ] **Step 17: §13 미결**

찾기:
```
| O1 | 가중치·임계값 | 판정 정확도 | Phase 0 백테스트 |
| O3 | 디시 수집 방식 | collectors 디시 어댑터 | 법무 |
```
바꾸기:
```
| O1 | submitterTarget 상대값(하한·비율)·임계값 | 판정 정확도 | SP4 백테스트 |
| O8 | 다축 신호 가중치 | SP4 | SP4 백테스트 |
| O9 | 판정 후 병합(ADJ 경로) | RESOLVED 병합 허용 | Phase 2 |
| O10 | SLA 통보 채널 | 야간 대응 | Phase 2 |
```

- [ ] **Step 18: 검증**

Run:
```bash
grep -n "metric_\|MetricSource\|collectors\|baseline\|네이버DL\|디시 수집\|수집기\|수집장애\|× d\|O3" 04-development-plan.md
```
Expected: 출력 없음.

- [ ] **Step 19: 커밋**

```bash
git add 04-development-plan.md
git commit -m "docs(04): 외부 수집기 제거, sla_watch·JudgeService·ApprovalGate·DB 제약 반영, 로드맵 SP 순서

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: 02-admin-console.md — 정책·큐·ADM-900

**Files:**
- Modify: `02-admin-console.md`

- [ ] **Step 1: 헤더·P3**

찾기:
```
> **작성 기준일**: 2026-08-06
> **문서 성격**: 초안. 화면 ID·권한 매트릭스는 확정, 레이아웃은 와이어프레임 수준.
```
바꾸기:
```
> **작성 기준일**: 2026-08-06 · **개정**: 2026-09-17 (자동 임시 비공개 정책 확정, 클레임·에스컬레이션, ADM-900 추가. 근거: `docs/superpowers/specs/2026-09-17-design-review-design.md`)
> **문서 성격**: 초안. 화면 ID·권한 매트릭스는 확정, 레이아웃은 와이어프레임 수준.
```

찾기:
```
| P3 | **자동은 플래그까지, 결정은 사람** | 어뷰징 탐지·병합 후보는 자동, 제재·병합 확정은 사람. (설계서 7장) |
```
바꾸기:
```
| P3 | **자동은 플래그와 가역적 보전 조치까지, 결정은 사람** | 어뷰징 탐지·병합 후보는 자동, 제재·병합·비공개 확정은 사람. 임시 비공개(가역)는 SLA 초과 시 자동 허용(R4 개정). |
| P7 | **통제는 서버 한 곳** | 2인 승인·역할 검사는 서버 `ApprovalGate`·메서드 시큐리티가 강제. 프론트 `CAN`은 표시용. |
```

- [ ] **Step 2: 권한 매트릭스 — 배치·계정 행**

찾기:
```
| 개인정보 필드 열람 | 마스킹 | 마스킹 | 사유입력 후 | 사유입력 후 |
| 감사 로그 | — | 본인분 | ✔ | ✔ |
```
바꾸기:
```
| 임시 비공개(수동) | — | ✔ | ✔ | 조회 |
| 배치 수동 실행 (ADM-900) | — | ✔ | ✔ | 조회 |
| 관리자 계정 생성·권한 변경 | — | — | ✔(2인) | 조회 |
| 개인정보 필드 열람 | 마스킹 | 마스킹 | 사유입력 후(서버 기록) | 사유입력 후(서버 기록) |
| 감사 로그 | — | 본인분 | ✔ | ✔ |
```

찾기:
```
**2인 승인(4-eyes)** 대상: 유저 제재 확정, 등급 수동 조정, 파라미터 적용, 상쇄 원장 100점 초과.
```
바꾸기:
```
**2인 승인(4-eyes)** 대상: 유저 제재 확정, 등급 수동 조정, 파라미터 적용, 상쇄 원장 100점 초과(재판정이 만드는 ADJ 합계 포함), 관리자 계정 생성·권한 변경. 신규 계정은 생성 후 7일간 승인권 없음(P8) — 계정 하나 더 만들어 자기 요청을 승인하는 경로 차단.
AUDITOR는 **전 영역 읽기**다 — 신고 큐·파라미터 드래프트 포함.
```

- [ ] **Step 3: IA에 ADM-900**

찾기:
```
├─ ⚙️ 정책
│   ├─ 파라미터 스튜디오    ADM-600
│   └─ 등급 정책          ADM-610
```
바꾸기:
```
├─ ⚙️ 정책
│   ├─ 파라미터 스튜디오    ADM-600
│   ├─ 등급 정책          ADM-610
│   └─ 배치 관리          ADM-900
```

- [ ] **Step 4: ADM-010 SLA 표**

찾기:
```
| 신고 콘텐츠 | 4시간 | 자동 임시 비공개 |
| 병합 검수 | 24시간 | 미처리 시 판정 유예 자동 연장 |
| 어뷰징 플래그 | 48시간 | 상위 역할 에스컬레이션 |
| 이의 제기 | 5영업일 | 알림 + 대시보드 적색 |
```
바꾸기:
```
| 신고 콘텐츠 | 4시간 | 자동 임시 비공개 (`sla_watch`, 정책 확정 P1) |
| 병합 검수 | 24시간 | 판정 유예 자동 연장 +24h (`sla_watch`) |
| 어뷰징 플래그 | 48시간 | 상위 역할 에스컬레이션 (Phase 2) |
| 이의 제기 | 5영업일 | 알림 + 대시보드 적색 (Phase 2) |

큐 카운트·최장 대기·뱃지는 전부 `GET /admin/queues/summary` 하나에서 온다. 미구현 큐는 0이 아니라 **"미구현"** 으로 표시한다(P10).
```

- [ ] **Step 5: ADM-100 설계 포인트**

찾기:
```
- **보류(Skip)** 는 큐 뒤로 보내되 3회 보류 시 상위 역할로 에스컬레이션.
```
바꾸기:
```
- **클레임**: 카드를 열면 `assigned_to`·`claimed_at`이 기록되고 15분간 다른 검수자에게 읽기 전용. 만료 시 큐 복귀. "내 담당만" 필터의 근거.
- **상태 모델**: `PENDING → CLAIMED → (MERGED | SEPARATED | VOIDED | HELD)`. **보류(HELD)** 는 큐 뒤로 보내되 3회 보류 시 `ESCALATED`(OPERATOR 큐). 보류와 분리는 다른 상태다.
- **멱등키**: 병합·분리·VOID 요청은 `Idempotency-Key` 헤더 필수. 재시도는 이전 결과를 돌려준다.
- **판정 후 항목은 병합 불가**: 대상이 RESOLVED면 409 + "재판정 경로로" 안내. VOID는 사유 필수.
```

- [ ] **Step 6: ADM-200 재판정**

찾기:
```
재판정이 필요하면 새 verdict 를 `supersedes: 이전ID` 로 추가하고, 점수 차액을 상쇄 원장으로 반영한다.
```
바꾸기:
```
재판정이 필요하면 새 verdict 를 `supersedes: 이전ID` 로 추가하고, 점수 차액을 상쇄 원장으로 반영한다. 재판정은 배치와 **같은 `JudgeService`·같은 승인된 파라미터**로 돌린다(기본값 하드코딩 금지). 차액 ADJ 합계가 100점을 넘으면 2인 승인 게이트(`ApprovalGate`)를 지난다.
```

- [ ] **Step 7: ADM-311 승인자 표시**

찾기:
```
- `ADJ` 추가 시 **사유 필수 + 승인자 표시**. 100점 초과는 2인 승인.
```
바꾸기:
```
- `ADJ` 추가 시 **사유 필수 + 승인자 표시**(`score_ledger.approved_by_admin_id`). 100점 초과는 2인 승인.
- 마스킹 해제는 서버 엔드포인트(`POST /admin/users/{id}/unmask`)를 거쳐 `PII_VIEW`가 기록된다. 클라이언트 토글로 해제하지 않는다.
```

- [ ] **Step 8: ADM-410 정책 확정**

찾기:
```
- 신고자 정보는 처리자에게도 **비식별 처리** (보복 방지)
```
바꾸기:
```
- **정책 확정(2026-09-17, P1)**: 4h 자동 임시 비공개는 `sla_watch` 잡이 수행한다. 임시 비공개는 가역적 보전 조치라 R4와 양립(R4 개정). 확정(복원/영구 비공개)은 사람. 수동 임시 비공개는 OPERATOR/ADMIN(REVIEWER 불가).
- 신고자 정보는 처리자에게도 **비식별 처리** (보복 방지)
```

- [ ] **Step 9: ADM-500 시딩 제외**

찾기:
```
- 시딩 제보는 `is_seed = true`, 점수 원장에 반영되지 않음
```
바꾸기:
```
- 시딩 제보는 `is_seed = true`, 점수 원장에 반영되지 않고 **판정 신호(제보자 수) 집계에서도 제외**(P2). 시딩은 클러스터 앵커 역할만 — 운영진이 HIT를 만들 수 없어야 한다(R1)
```

- [ ] **Step 10: ADM-600 상대 목표치**

찾기:
```
외부 지표 가중치는 더 이상 없다 — 판정 근거가 제보 시계열 하나뿐이라 조정 대상도 단순해졌다.
```
바꾸기:
```
외부 지표 가중치는 더 이상 없다 — 판정 근거가 제보 시계열 하나뿐이라 조정 대상도 단순해졌다.
SP4에서 `submitterTarget`은 절대값이 아니라 **상대값 파라미터(하한·활성 제보자 비율·기간)** 로 바뀌고, 다축 신호 가중치(O8)가 추가된다. 시뮬레이션은 배치와 같은 `JudgeService`를 쓴다.
```

- [ ] **Step 11: ADM-800**

찾기:
```
- 2FA 필수, 세션 타임아웃 30분
- 권한 부여·회수는 2인 승인
```
바꾸기:
```
- 2FA 필수, 세션 타임아웃 30분
- 계정 생성·권한 부여·회수는 2인 승인. 신규 계정은 생성 후 7일간 승인권 없음(P8)
- 부트스트랩 계정은 첫 로그인 시 비밀번호 변경 강제
```

- [ ] **Step 12: ADM-900 섹션 신설 (ADM-800 뒤, §4 앞)**

찾기:
```
- 퇴사자 즉시 회수 체크리스트

---

## 4. 공통 UI 규약
```
바꾸기:
```
- 퇴사자 즉시 회수 체크리스트

---

### ADM-900 · 배치 관리

배치를 스케줄 밖에서 수동 실행한다. 현재 `cluster_merge`만 지원.

```
┌──────────────────────────────────────────────────────────┐
│  배치 관리                                               │
├──────────────────────────────────────────────────────────┤
│  cluster_merge   일 1회 · 마지막 실행 03:00 (성공)        │
│  [ 지금 실행 ]                                           │
│  결과: 후보 12 · 자동 병합 3 · 큐 적재 5 · 분리 4 · 실패 0│
└──────────────────────────────────────────────────────────┘
```

- 권한 OPERATOR/ADMIN. 스케줄 실행과 같은 ShedLock 이름을 쓰므로 **실행 중이면 409**.
- 감사 로그 `CLUSTER_MERGE_MANUAL_TRIGGER` + 결과 요약을 `detail`에.
- 판정(`verdict_runner`) 수동 실행은 두지 않는다 — 판정 시점을 사람이 고르면 R1이 흔들린다. 재판정은 ADM-200.

---

## 4. 공통 UI 규약
```

- [ ] **Step 13: §4 공통 규약 사유 행**

찾기:
```
| 사유 입력 | 제재·VOID·ADJ·소급적용은 **필수**. 병합은 선택 |
```
바꾸기:
```
| 사유 입력 | 제재·VOID·ADJ·소급적용·재판정은 **필수**(서버가 검증). 병합·분리는 선택 |
```

- [ ] **Step 14: §5 화면 목록**

찾기:
```
| ADM-800 | 관리자 계정 | P0 | |
```
바꾸기:
```
| ADM-800 | 관리자 계정 | P0 | |
| ADM-900 | 배치 관리 | P1 | cluster_merge 수동 실행 (구현됨) |
```

찾기:
```
**Phase 0~1 최소 구성**: ADM-010, 100, 110/111, 200, 500, 600, 700, 800
**Phase 2 공개 전 필수 추가**: ADM-300, 310/311, 320, 400, 410, 610
```
바꾸기:
```
**Phase 0~1 최소 구성**: ADM-010, 100, 110/111, 200, 311(원장 — 이의 대응 근거), 410(구현됨), 500, 600, 700, 800, 900
**Phase 2 공개 전 필수 추가**: ADM-300, 310, 320, 400, 610
```

- [ ] **Step 15: §6 A6 종결**

찾기:
```
| A6 | 신고 콘텐츠 야간·주말 대응 | 자동 임시 비공개로 갈음할지, 당직 운영할지 |
```
바꾸기:
```
| A6 | 신고 콘텐츠 야간·주말 대응 | **결정(2026-09-17)**: 자동 임시 비공개(`sla_watch`). 당직 인력 계획 없이 야간 명예훼손 신고가 노출되는 리스크가 더 큼 |
```

- [ ] **Step 16: 검증**

Run:
```bash
grep -c "ADM-900\|sla_watch\|ApprovalGate\|CLAIMED" 02-admin-console.md
```
Expected: `8` 이상.

Run:
```bash
grep -n "자동 임시 비공개로 갈음할지" 02-admin-console.md
```
Expected: 출력 없음.

- [ ] **Step 17: 커밋**

```bash
git add 02-admin-console.md
git commit -m "docs(02): 자동 임시 비공개 확정, 클레임·에스컬레이션·멱등키, ApprovalGate, ADM-900 추가

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 05-screen-endpoint-map.md — 엔드포인트 상태·ADM-900

**Files:**
- Modify: `05-screen-endpoint-map.md`

- [ ] **Step 1: ADM-100 표 — 실제 경로·상태**

찾기:
```
| 병합 / 분리 | `POST /admin/merge-queue/{id}/action` (MERGE·SPLIT) | ✅ | R/O/A · `Idempotency-Key` 필수 |
| VOID(허위/규정위반) | `POST …/action` (VOID) | ✅ | **O/A** (R은 403) |
| 보류(HOLD) | `POST …/action` (HOLD) | ✅ | R/O/A |
```
바꾸기:
```
| 병합 / 분리 | `POST /admin/merge-queue/{id}/merge` · `…/separate` | ✅ | R/O/A · `Idempotency-Key` 필수(🔶 SP2 — 현행 미수신) |
| VOID(허위/규정위반) | `POST /admin/merge-queue/{id}/void` | ✅ | **O/A** (R은 403) · 사유 필수(🔶 SP2) |
| 클레임 / 해제 | `POST /admin/merge-queue/{id}/claim` · `…/release` | 🔶 | R/O/A · 15분 만료 (SP2) |
| 보류(HOLD) | `POST /admin/merge-queue/{id}/hold` | 🔶 | R/O/A · 3회 → ESCALATED (SP2) |
```

- [ ] **Step 2: ADM-200 재판정 비고**

찾기:
```
| VOID / 유예연장 / 재판정 | `POST /admin/trends/{id}/exceptions` | ✅ | **O/A** · 사유코드 필수 |
```
바꾸기:
```
| VOID / 유예연장 / 재판정 | `POST /admin/verdicts/{id}/rejudge` · `…/extend-grace` · `…/void` | ✅ | **O/A** · 사유 필수 · 재판정 ADJ 합계 >100 → `ApprovalGate`(🔶 SP3) |
```

- [ ] **Step 3: ADM-311 unmask**

찾기:
```
| 유저 상세·원장(마스킹) | `GET /admin/users/{id}` | ✅ | 마스킹 R/O · 사유후 A/Au |
```
바꾸기:
```
| 유저 상세·원장(마스킹) | `GET /admin/users/{id}` | 🔶 | 마스킹 R/O · 사유후 A/Au · **컨트롤러 미구현(SP3)** — 현행 화면은 픽스처 |
| 마스킹 해제(사유 → `PII_VIEW` 기록) | `POST /admin/users/{id}/unmask` | 🔶 | A/Au (SP3) |
```

- [ ] **Step 4: ADM-410 권한·자동 비공개**

찾기:
```
| 신고 목록(신고자 비식별) | `GET /admin/reports` | 🔶 | R/O/A |
| 임시비공개/복원/영구비공개 | `POST /admin/reports/{id}/decide` | 🔶 | O/A · 4h 미처리 자동 비공개 |
```
바꾸기:
```
| 신고 목록(신고자 비식별) | `GET /admin/reports` | ✅ | R/O/A/Au |
| 임시비공개 / 소명 요청(48h) / 결정(복원·영구비공개) | `POST /admin/reports/{id}/hide` · `…/request-explanation` · `…/decide` | ✅ | **O/A**(R 불가 — 🔶 현행 `hide`는 R 허용, SP3에서 축소) · 4h 미처리 자동 비공개는 `sla_watch`(🔶 SP3, P1) |
```

- [ ] **Step 5: ADM-800 비고**

찾기:
```
| 권한 부여/회수 | `POST /admin/accounts/{id}/role` | 🔶 | **A(2인)** |
```
바꾸기:
```
| 계정 생성 | `POST /admin/accounts` | ✅ | **A(2인 — 🔶 현행 단독, SP3에서 `ApprovalGate`)** · 신규 계정 7일 승인권 유예 |
| 권한 부여/회수 | `POST /admin/accounts/{id}/role` | 🔶 | **A(2인)** |
| 비밀번호 변경(부트스트랩 강제) | `POST /admin/accounts/me/password` | 🔶 | 본인 (SP3) |
```

- [ ] **Step 6: ADM-900 섹션 신설 (ADM-800 표 뒤, §C 앞)**

찾기:
```
| 권한 부여/회수 | `POST /admin/accounts/{id}/role` | 🔶 | **A(2인)** |
| 비밀번호 변경(부트스트랩 강제) | `POST /admin/accounts/me/password` | 🔶 | 본인 (SP3) |

---

## C. 스펙 확장 대상 (OpenAPI v0.1 미포함 · 🔶)
```
바꾸기:
```
| 권한 부여/회수 | `POST /admin/accounts/{id}/role` | 🔶 | **A(2인)** |
| 비밀번호 변경(부트스트랩 강제) | `POST /admin/accounts/me/password` | 🔶 | 본인 (SP3) |

### ADM-900 · 배치 관리
| 상호작용 | API | 상태 | 권한 |
|---|---|:--:|---|
| cluster_merge 수동 실행 | `POST /admin/batch-jobs/cluster-merge/run` | ✅ | **O/A** · 실행 중 409 · 감사 `CLUSTER_MERGE_MANUAL_TRIGGER` |

---

## C. 스펙 확장 대상 (OpenAPI v0.1 미포함 · 🔶)
```

- [ ] **Step 7: §C 표 갱신**

찾기:
```
| `GET /admin/users`, `POST …/{id}/grade-adjust` | 유저 목록·등급조정 | ADM-310/311 |
```
바꾸기:
```
| `GET /admin/users`, `GET /admin/users/{id}`, `POST …/{id}/unmask`, `POST …/{id}/grade-adjust` | 유저 목록·상세·PII 열람·등급조정 | ADM-310/311 |
| `POST /admin/merge-queue/{id}/claim`·`release`·`hold` | 큐 클레임·보류 | ADM-100 |
```

찾기:
```
| `GET /admin/reports`, `POST …/{id}/decide` | 신고 콘텐츠 큐 | ADM-410 |
```
바꾸기:
```
```
(해당 줄 삭제 — 이미 구현됨)

- [ ] **Step 8: §D 매트릭스**

찾기:
```
| 개인정보 열람 `/admin/users/{id}?unmask` | 마스킹 | 마스킹 | 사유후 | 사유후 |
| 감사 로그 `/admin/audit-log` | — | 본인분 | ✔ | ✔ |
```
바꾸기:
```
| 개인정보 열람 `POST /admin/users/{id}/unmask` | 마스킹 | 마스킹 | 사유후(기록) | 사유후(기록) |
| 임시 비공개 `/admin/reports/{id}/hide` | — | ✔ | ✔ | 조회 |
| 배치 수동 실행 `/admin/batch-jobs/*/run` | — | ✔ | ✔ | 조회 |
| 관리자 계정 생성·권한 `/admin/accounts` | — | — | ✔(2인) | 조회 |
| 감사 로그 `/admin/audit-log` | — | 본인분 | ✔ | ✔ (Au 전 영역 읽기 — 신고·드래프트 포함) |
```

찾기:
```
> **2인 승인(4-eyes)**: 유저제재 확정 · 등급 수동조정 · 파라미터 적용 · 상쇄원장 100점 초과. 요청자≠승인자·승인자1≠승인자2는 `approval_requests` CHECK로 이미 DB 강제(V6).
```
바꾸기:
```
> **2인 승인(4-eyes)**: 유저제재 확정 · 등급 수동조정 · 파라미터 적용 · 상쇄원장 100점 초과(재판정 ADJ 포함) · 관리자 계정 생성·권한 변경. 요청자≠승인자·승인자1≠승인자2는 `approval_requests` CHECK로 DB 강제(V6) — 단 이는 *계정* 수준이라 신규 계정 7일 승인권 유예(P8)로 *사람* 수준을 보강한다. 서버 게이트 `ApprovalGate` 하나가 전 대상을 강제(SP3). 현행 executor는 `PARAM_APPLY`만 존재.
```

- [ ] **Step 9: 검증**

Run:
```bash
grep -c "ADM-900\|batch-jobs\|unmask\|claim" 05-screen-endpoint-map.md
```
Expected: `6` 이상.

Run:
```bash
grep -n "/action\|?unmask" 05-screen-endpoint-map.md
```
Expected: 출력 없음.

- [ ] **Step 10: 커밋**

```bash
git add 05-screen-endpoint-map.md
git commit -m "docs(05): 실제 병합 큐 경로, 클레임·unmask·ADM-900 추가, 자동 비공개·2인 승인 상태 표기

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: ReportAdminService 주석 정정

**Files:**
- Modify: `backend/api-admin/src/main/java/kr/trendstage/apiadmin/report/ReportAdminService.java:26-29`

- [ ] **Step 1: 주석 교체**

찾기:
```
/**
 * ADM-410 신고 콘텐츠 큐. 4h 자동 임시비공개는 의도적으로 없음 — 사람이 명시적으로 판단하기
 * 전까지는 절대 비공개되지 않는다(설계서 §범위). visibility는 순수 표시 계층 — 판정/점수와 분리(R2).
 */
```
바꾸기:
```
/**
 * ADM-410 신고 콘텐츠 큐. 4h SLA 초과 시 자동 임시비공개(TEMP_HIDDEN)는 정책으로 확정됐고(R4 개정, 2026-09-17,
 * P1) SP3의 sla_watch 잡이 수행한다 — 이 서비스는 사람의 결정(수동 임시비공개/복원/영구 비공개)만 다룬다.
 * visibility는 순수 표시 계층 — 판정/점수와 분리(R2).
 */
```

- [ ] **Step 2: 컴파일 확인**

Run:
```bash
cd backend && ./gradlew :api-admin:compileJava -q
```
Expected: 출력 없음, exit 0. (주석만 바뀌었으므로 실패하면 편집 범위를 벗어난 것.)

- [ ] **Step 3: 커밋**

```bash
git add backend/api-admin/src/main/java/kr/trendstage/apiadmin/report/ReportAdminService.java
git commit -m "docs(api-admin): ReportAdminService 주석 — 4h 자동 임시비공개는 sla_watch(SP3)로 확정

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: 전체 검증 + PR

**Files:** 없음 (검증·PR만)

- [ ] **Step 1: 피벗 이전 용어 전수 검사**

Run:
```bash
grep -n "metric_collector\|metric_snapshots\|MetricSource\|baseline\|기준선\|소급 재수집\|데이터랩\|Graph API\|collectors/" CLAUDE.md 01-system-design.md 02-admin-console.md 03-merge-clustering.md 04-development-plan.md 05-screen-endpoint-map.md
```
Expected: 출력 없음.

- [ ] **Step 2: 신규 용어 존재 검사**

Run:
```bash
for f in CLAUDE.md 01-system-design.md 02-admin-console.md 03-merge-clustering.md 04-development-plan.md 05-screen-endpoint-map.md; do echo "$f: $(grep -c 'sla_watch' $f)"; done
```
Expected: 여섯 파일 모두 `1` 이상.

- [ ] **Step 3: 스펙 §6 체크리스트 완료 표시**

`docs/superpowers/specs/2026-09-17-design-review-design.md` §6의 `- [ ]` 7개를 전부 `- [x]`로 바꾼다(마지막 "PR" 항목은 Step 5 후). 커밋:
```bash
git add docs/superpowers/specs/2026-09-17-design-review-design.md
git commit -m "docs(spec): SP0 체크리스트 완료 표시

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 4: 커밋 목록 확인**

Run:
```bash
git log --oneline main..HEAD
```
Expected: 스펙 커밋(`2be7019`) + Task 1~7 커밋 7개 + 체크리스트 커밋 = 9개. 작업 트리에 `.gitignore`/`README.md`/`application.yml` 변경이 **커밋되지 않고** 그대로 남아 있어야 한다: `git status --short` 에 ` M` 세 줄 유지.

- [ ] **Step 5: 푸시 + PR**

```bash
git push -u origin docs/design-review
```

```bash
gh pr create --base main --head docs/design-review --title "docs: 설계 재검토(SP0) — 원칙 R4·R5 개정, 피벗 이전 서술 제거, sla_watch·JudgeService·ApprovalGate 반영" --body "$(cat <<'EOF'
## 요약
- 설계 재검토 스펙 추가: `docs/superpowers/specs/2026-09-17-design-review-design.md` (진단·결정·SP0~SP4 분해)
- CLAUDE.md: R4(가역적 보전 조치 자동 허용)·R5(판정 입력은 제보뿐, 시간 분포) 개정, 감쇠를 원장→AS 조회로 이동, `sla_watch` 추가, 문서 경로 수정
- 01/03/04: 외부 지표·`metric_collector`·baseline·소급 재수집 서술 전부 제거, 제보 신호 기반으로 재서술, RESOLVED 병합 금지(409), DB 제약 기반 멱등성
- 02/05: 신고 4h 자동 임시 비공개 정책 확정(A6 종결), 병합 큐 클레임·HELD/ESCALATED·멱등키, `ApprovalGate`·신규 계정 7일 승인권 유예, ADM-900 배치 관리 추가, 실제 엔드포인트 경로 반영
- `ReportAdminService` 클래스 주석 정정(코드 변경 없음)

## 검증
- `grep`으로 6개 문서에서 피벗 이전 용어 0건, `sla_watch` 전 문서 존재 확인
- `:api-admin:compileJava` 통과

## 다음
SP0.5(보안 즉시 수정) → SP1(판정 파이프라인) 스펙. 이 PR 병합 전에는 SP1 스펙을 시작하지 않는다(스펙 §4.3).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

Expected: PR URL 출력. 병합은 사용자가 GitHub에서 수행한다(`gh pr merge`는 이 환경에서 차단됨).

- [ ] **Step 6: 스펙 §6 마지막 항목 체크 후 푸시**

`- [ ] PR: docs/design-review → main` → `- [x] PR: docs/design-review → main (#N)` (N = 생성된 PR 번호). 커밋 후 `git push`.
