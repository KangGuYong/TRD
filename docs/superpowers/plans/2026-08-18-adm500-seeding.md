# ADM-500 시딩 관리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 운영자가 관리자 콘솔에서 트렌드를 시딩 제보로 직접 등록하고, 담당자별 적중률(TI)을 실시간으로 볼 수 있게 한다.

**Architecture:** 운영자별 전용 합성 `UserAccount`(핸들 `seed_{loginId}`)를 최초 시딩 등록 시 자동 생성해 `admin_accounts.seed_user_id`에 매핑, 그 계정으로 정상 `Submission`(`seed=true`)을 생성한다. 시딩 제보는 `VerdictRunner`가 그대로 판정하므로(HIT/MISS는 이미 `Submission.result`에 반영됨) 적중률은 `submissions.result` 카운트 + 기존 `TrustIndex.compute()`로 매 요청마다 즉석 계산한다. 제보 생성 로직(정규화 매칭·중복 체크)은 `SubmissionService.create()`와 동일 알고리즘을 `api-admin`에 의도적으로 소규모 중복 구현한다(`api-admin → api-public` 모듈 의존 추가 회피, `VerdictAdminService`의 기존 전례와 동일).

**Tech Stack:** Spring Boot 3.3.5 / Java 17, JPA/Hibernate, Flyway, React + TypeScript, TanStack Query.

---

## Task 1: 마이그레이션 + `AdminAccount.seedUserId`

**Files:**
- Create: `backend/persistence/src/main/resources/db/migration/V18__admin_seed_account.sql`
- Modify: `backend/persistence/src/main/java/kr/trendstage/persistence/entity/AdminAccount.java`

- [ ] **Step 1: 마이그레이션 작성**

```sql
-- V18 · ADM-500 운영자별 시딩 전용 합성 유저 계정 매핑
ALTER TABLE admin_accounts ADD COLUMN seed_user_id UUID REFERENCES users(id);
```

- [ ] **Step 2: `AdminAccount`에 필드·getter·세터 추가**

`backend/persistence/src/main/java/kr/trendstage/persistence/entity/AdminAccount.java`의 `disabledAt` 필드 바로 아래에 추가:

```java
    @Column(name = "seed_user_id")
    private UUID seedUserId;
```

`enable()` 메서드 바로 아래에 추가:

```java
    /** ADM-500: 최초 시딩 등록 시 자동 생성된 합성 유저 계정과 연결. */
    public void linkSeedUser(UUID seedUserId) { this.seedUserId = seedUserId; }
```

`getDisabledAt()` getter 바로 아래에 추가:

```java
    public UUID getSeedUserId() { return seedUserId; }
```

- [ ] **Step 3: 컴파일 확인**

Run: `cd backend && ./gradlew :persistence:compileJava`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: 커밋**

```bash
git add backend/persistence/src/main/resources/db/migration/V18__admin_seed_account.sql backend/persistence/src/main/java/kr/trendstage/persistence/entity/AdminAccount.java
git commit -m "feat(persistence): add admin_accounts.seed_user_id for ADM-500"
```

---

## Task 2: `AdminSeedService`

**Files:**
- Create: `backend/api-admin/src/main/java/kr/trendstage/apiadmin/seed/AdminSeedService.java`

기존 코드베이스에는 `api-admin`용 통합 테스트 스위트가 없다(V14~V17 스코프의 서비스들 모두 수동 E2E로만 검증됨). 이 태스크도 같은 전례를 따라 자동 테스트 없이 구현하고, Task 4에서 컴파일만 검증한 뒤 Task 5(전체 E2E)에서 실 DB로 검증한다.

- [ ] **Step 1: 서비스 작성**

```java
package kr.trendstage.apiadmin.seed;

import kr.trendstage.apiadmin.auth.AdminValidationException;
import kr.trendstage.domain.params.ParameterSet;
import kr.trendstage.domain.score.TrustIndex;
import kr.trendstage.domain.trend.NameNormalizer;
import kr.trendstage.persistence.entity.AdminAccount;
import kr.trendstage.persistence.entity.Submission;
import kr.trendstage.persistence.entity.TrendItem;
import kr.trendstage.persistence.entity.UserAccount;
import kr.trendstage.persistence.repo.AdminAccountRepository;
import kr.trendstage.persistence.repo.SubmissionRepository;
import kr.trendstage.persistence.repo.TrendItemRepository;
import kr.trendstage.persistence.repo.UserRepository;
import kr.trendstage.persistence.type.SubmissionResult;
import kr.trendstage.persistence.type.TrendCategory;
import kr.trendstage.persistence.type.TrendState;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.time.Instant;
import java.util.List;
import java.util.Set;
import java.util.UUID;

/**
 * ADM-500 시딩 등록·담당자별 적중률. 제보 생성 로직은 SubmissionService.create()(api-public)와
 * 동일 알고리즘이지만, api-admin → api-public 모듈 의존을 피하기 위해 의도적으로 소규모 중복한다
 * (VerdictAdminService가 VerdictRunner 판정 로직을 같은 이유로 중복하는 것과 동일 전례).
 */
@Service
public class AdminSeedService {

    private static final Set<Integer> VALID_CONFIDENCE = Set.of(10, 30, 50);

    private final AdminAccountRepository adminAccounts;
    private final UserRepository users;
    private final TrendItemRepository trendItems;
    private final SubmissionRepository submissions;

    public AdminSeedService(AdminAccountRepository adminAccounts, UserRepository users,
                            TrendItemRepository trendItems, SubmissionRepository submissions) {
        this.adminAccounts = adminAccounts;
        this.users = users;
        this.trendItems = trendItems;
        this.submissions = submissions;
    }

    public record SeedSubmissionRequest(String name, String category, String platform,
                                         String evidenceUrl, Integer confidence, String oneLine) {}
    public record SeedSubmissionResult(String submissionId, String canonicalName, String trendItemId) {}
    public record SeedAccuracyRow(String operatorName, int hit, int miss, int judged, double trustIndex) {}

    @Transactional
    public SeedSubmissionResult registerSeed(UUID actorId, SeedSubmissionRequest req) {
        if (!VALID_CONFIDENCE.contains(req.confidence())) {
            throw new AdminValidationException("confidence는 10/30/50 중 하나여야 합니다");
        }

        AdminAccount actor = adminAccounts.findById(actorId).orElseThrow();
        UUID seedUserId = actor.getSeedUserId();
        if (seedUserId == null) {
            UserAccount seedUser = users.save(new UserAccount("seed_" + actor.getLoginId()));
            actor.linkSeedUser(seedUser.getId());
            seedUserId = seedUser.getId();
        }

        String normalized = NameNormalizer.normalize(req.name());
        Instant now = Instant.now();

        TrendItem item = trendItems.findByNormalizedKey(normalized).orElse(null);
        if (item != null) {
            boolean dup = submissions.existsByTrendItemIdAndUserIdAndResultNot(
                    item.getId(), seedUserId, SubmissionResult.VOID);
            if (dup) throw new AdminValidationException("이미 이 계정으로 시딩한 항목입니다");
        } else {
            item = trendItems.save(new TrendItem(req.name(), normalized,
                    TrendCategory.valueOf(req.category()), now));
            item.transitionTo(TrendState.PENDING);
        }

        Submission sub = submissions.save(new Submission(
                seedUserId, item.getId(), req.name(), normalized,
                req.confidence().shortValue(), req.platform(), req.evidenceUrl(), req.oneLine(),
                false, true));

        return new SeedSubmissionResult(sub.getId().toString(), item.getCanonicalName(), item.getId().toString());
    }

    @Transactional(readOnly = true)
    public List<SeedAccuracyRow> listAccuracy() {
        ParameterSet p = ParameterSet.defaults();
        return adminAccounts.findAll().stream()
                .filter(a -> a.getSeedUserId() != null)
                .map(a -> {
                    long hit = submissions.countByUserIdAndResult(a.getSeedUserId(), SubmissionResult.HIT);
                    long miss = submissions.countByUserIdAndResult(a.getSeedUserId(), SubmissionResult.MISS);
                    double ti = TrustIndex.compute((int) hit, (int) miss, p);
                    return new SeedAccuracyRow(a.getDisplayName(), (int) hit, (int) miss, (int) (hit + miss), ti);
                })
                .toList();
    }
}
```

- [ ] **Step 2: 컴파일 확인**

Run: `cd backend && ./gradlew :api-admin:compileJava`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 커밋**

```bash
git add backend/api-admin/src/main/java/kr/trendstage/apiadmin/seed/AdminSeedService.java
git commit -m "feat(admin): add AdminSeedService for ADM-500 seed registration"
```

---

## Task 3: `AdminSeedController`

**Files:**
- Create: `backend/api-admin/src/main/java/kr/trendstage/apiadmin/web/AdminSeedController.java`

- [ ] **Step 1: 컨트롤러 작성**

```java
package kr.trendstage.apiadmin.web;

import kr.trendstage.apiadmin.auth.AdminPrincipal;
import kr.trendstage.apiadmin.seed.AdminSeedService;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/** ADM-500. 등록은 OPERATOR 이상, 담당자별 적중률 조회는 4개 역할 전부(MergeQueueController.list()와 동일 권한). */
@RestController
@RequestMapping("/admin/seed")
public class AdminSeedController {

    private final AdminSeedService service;

    public AdminSeedController(AdminSeedService service) {
        this.service = service;
    }

    @PostMapping("/submissions")
    @PreAuthorize("hasAnyRole('OPERATOR', 'ADMIN')")
    public AdminSeedService.SeedSubmissionResult register(@RequestBody AdminSeedService.SeedSubmissionRequest req,
                                                            @AuthenticationPrincipal AdminPrincipal actor) {
        return service.registerSeed(actor.id(), req);
    }

    @GetMapping("/accuracy")
    @PreAuthorize("hasAnyRole('REVIEWER', 'OPERATOR', 'ADMIN', 'AUDITOR')")
    public List<AdminSeedService.SeedAccuracyRow> accuracy() {
        return service.listAccuracy();
    }
}
```

- [ ] **Step 2: 컴파일 확인**

Run: `cd backend && ./gradlew :api-admin:compileJava`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: 커밋**

```bash
git add backend/api-admin/src/main/java/kr/trendstage/apiadmin/web/AdminSeedController.java
git commit -m "feat(admin): add AdminSeedController /admin/seed endpoints"
```

---

## Task 4: 백엔드 전체 빌드 검증

**Files:** 없음 (검증 전용)

- [ ] **Step 1: 전체 빌드**

Run: `cd backend && ./gradlew build -x test`
Expected: BUILD SUCCESSFUL (기존 테스트는 스킵, 이 스코프에서 신규 단위 테스트가 없으므로 컴파일만 확인)

- [ ] **Step 2: 문제 발생 시 수정 후 재실행**

컴파일 에러가 나오면 (1) `TrendCategory` import 경로, (2) `AdminSeedService`의 record 필드명이 컨트롤러와 정확히 일치하는지 확인.

---

## Task 5: 프론트엔드 `types.ts` 추가

**Files:**
- Modify: `admin/src/api/types.ts`

- [ ] **Step 1: 타입 추가**

파일 끝에 추가:

```ts
export interface SeedSubmissionRequest {
  name: string;
  category: "MEME" | "PRODUCT" | "PERSON_CHANNEL" | "CHALLENGE" | "SLANG" | "ETC";
  platform: string;
  evidenceUrl: string;
  confidence: 10 | 30 | 50;
  oneLine: string;
}
export interface SeedSubmissionResult {
  submissionId: string;
  canonicalName: string;
  trendItemId: string;
}
export interface SeedAccuracyRow {
  operatorName: string;
  hit: number;
  miss: number;
  judged: number;
  trustIndex: number;
}
```

- [ ] **Step 2: 커밋**

```bash
git add admin/src/api/types.ts
git commit -m "feat(admin): add ADM-500 seed types"
```

---

## Task 6: 프론트엔드 `fixtures.ts` 추가

**Files:**
- Modify: `admin/src/fixtures.ts`

- [ ] **Step 1: import에 `SeedAccuracyRow` 추가**

파일 상단 `import type { ... } from "./api/types";` 목록에 `SeedAccuracyRow` 추가.

- [ ] **Step 2: 픽스처 데이터 추가**

파일 끝에 추가 (TI 계산은 `(hit+2)/(hit+miss+5)`로 검산):

```ts
export const fxSeedAccuracy: SeedAccuracyRow[] = [
  { operatorName: "김운영", hit: 14, miss: 3, judged: 17, trustIndex: 0.7273 },
  { operatorName: "이시딩", hit: 6, miss: 6, judged: 12, trustIndex: 0.4444 },
  { operatorName: "박담당", hit: 1, miss: 4, judged: 5, trustIndex: 0.3 },
];
```

- [ ] **Step 3: 커밋**

```bash
git add admin/src/fixtures.ts
git commit -m "feat(admin): add fxSeedAccuracy fixture for ADM-500"
```

---

## Task 7: 프론트엔드 `hooks.ts` 추가

**Files:**
- Modify: `admin/src/api/hooks.ts`

- [ ] **Step 1: import에 `SeedAccuracyRow`, `SeedSubmissionRequest`, `SeedSubmissionResult` 추가**

기존 `import type { ... } from "./types";` 목록에 세 타입을 추가.

- [ ] **Step 2: `useMutation`용 `useQueryClient` import 확인**

파일 상단에 `useQueryClient`가 이미 import되어 있는지 확인(`useParamDraft` 근처 mutation 함수들이 쓰는 패턴). 없으면 `import { useQuery, useQueryClient } from "@tanstack/react-query";`로 보강.

- [ ] **Step 3: 훅·뮤테이션 함수 추가**

파일 끝에 추가:

```ts
export const useSeedAccuracy = () =>
  useData<SeedAccuracyRow[]>(["admin", "seed-accuracy"], "/admin/seed/accuracy", fx.fxSeedAccuracy);

export const registerSeed = (req: SeedSubmissionRequest) =>
  api.post<SeedSubmissionResult>("/admin/seed/submissions", req);
```

`registerSeed`는 fixture 모드에서도 실 API를 그대로 호출하지 않고, `ParamStudioScreen`과 달리 mutation 성공 후 화면단(Task 9)에서 `queryClient.invalidateQueries(["admin", "seed-accuracy"])`를 직접 호출해 목록을 갱신한다(별도 `useMutation` 래퍼 없이 `AdminAccountsScreen`의 `createAdminAccount` 호출부와 동일하게 화면에서 직접 호출하는 기존 패턴을 따름).

- [ ] **Step 4: 타입체크**

Run: `cd admin && npx tsc --noEmit`
Expected: 에러 없음 (Task 9에서 `SeedScreen.tsx`를 만들기 전까지는 미사용 export 경고만 있을 수 있음 — 무시)

- [ ] **Step 5: 커밋**

```bash
git add admin/src/api/hooks.ts
git commit -m "feat(admin): add useSeedAccuracy hook + registerSeed for ADM-500"
```

---

## Task 8: `SeedScreen.tsx` + 라우팅

**Files:**
- Create: `admin/src/screens/SeedScreen.tsx`
- Modify: `admin/src/components/Layout.tsx`
- Modify: `admin/src/App.tsx`
- Modify: `admin/src/state/role.tsx`

- [ ] **Step 1: `role.tsx`에 `CAN.seedRegister` 추가**

`admin/src/state/role.tsx`의 `CAN` 객체에 `paramDraft` 항목 바로 아래 추가:

```ts
  seedRegister: (r: Role) => r === "OPERATOR" || r === "ADMIN",
```

- [ ] **Step 2: `Layout.tsx`의 `ScreenId`와 `NAV`에 `ADM-500` 추가**

`ScreenId` 유니온 수정:

```ts
export type ScreenId = "ADM-010" | "ADM-100" | "ADM-200" | "ADM-500" | "ADM-600" | "ADM-311" | "ADM-700" | "ADM-800" | "stub";
```

`NAV`의 "정책" 그룹(`ADM-600` 항목 바로 위)에 추가:

```ts
    { id: "ADM-500", label: "시딩 관리", code: "ADM-500" },
```

- [ ] **Step 3: `App.tsx`에 화면 등록**

`TITLE` 레코드에 추가:

```ts
  "ADM-500": "시딩 관리",
```

import 목록에 추가:

```ts
import SeedScreen from "./screens/SeedScreen";
```

`Console()`의 렌더 분기에 추가:

```tsx
      {screen === "ADM-500" && <SeedScreen />}
```

- [ ] **Step 4: `SeedScreen.tsx` 작성**

```tsx
import React, { useState } from "react";
import { useQueryClient } from "@tanstack/react-query";
import { ApiError } from "../api/client";
import { registerSeed, useSeedAccuracy } from "../api/hooks";
import type { SeedSubmissionRequest } from "../api/types";
import { Card, Btn, StateView } from "../components/ui";
import { C } from "../theme";
import { useRole, CAN } from "../state/role";

const CATEGORIES: SeedSubmissionRequest["category"][] = ["MEME", "PRODUCT", "PERSON_CHANNEL", "CHALLENGE", "SLANG", "ETC"];
const CONFIDENCE_OPTIONS: SeedSubmissionRequest["confidence"][] = [10, 30, 50];

const EMPTY: SeedSubmissionRequest = {
  name: "", category: "MEME", platform: "", evidenceUrl: "", confidence: 10, oneLine: "",
};

export default function SeedScreen() {
  const { role } = useRole();
  const queryClient = useQueryClient();
  const q = useSeedAccuracy();

  const [form, setForm] = useState<SeedSubmissionRequest>(EMPTY);
  const [busy, setBusy] = useState(false);
  const [toast, setToast] = useState<string | null>(null);

  const flash = (m: string) => { setToast(m); setTimeout(() => setToast(null), 2600); };
  const canRegister = CAN.seedRegister(role);
  const canSubmit = canRegister && form.name.trim() !== "" && form.platform.trim() !== ""
    && form.evidenceUrl.trim() !== "" && form.oneLine.trim() !== "" && !busy;

  const submit = async () => {
    setBusy(true);
    try {
      const result = await registerSeed(form);
      queryClient.invalidateQueries({ queryKey: ["admin", "seed-accuracy"] });
      setForm(EMPTY);
      flash(`시딩 등록 완료: ${result.canonicalName}`);
    } catch (e) {
      flash(e instanceof ApiError ? e.message : "등록에 실패했습니다");
    } finally {
      setBusy(false);
    }
  };

  return (
    <div style={{ maxWidth: 1000 }}>
      <Card>
        <b style={{ fontSize: 13 }}>시딩 등록</b>
        <div style={{ font: "500 11.5px Pretendard", color: C.faint, marginTop: 6, lineHeight: 1.6 }}>
          운영진 직접 등록분은 score_ledger에 반영되지 않습니다(is_seed=true) — 초기 데이터 확보용입니다.
        </div>

        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginTop: 16 }}>
          <Field label="트렌드명">
            <input value={form.name} disabled={!canRegister}
              onChange={(e) => setForm({ ...form, name: e.target.value })}
              style={inputStyle} />
          </Field>
          <Field label="카테고리">
            <select value={form.category} disabled={!canRegister}
              onChange={(e) => setForm({ ...form, category: e.target.value as SeedSubmissionRequest["category"] })}
              style={inputStyle}>
              {CATEGORIES.map((c) => <option key={c} value={c}>{c}</option>)}
            </select>
          </Field>
          <Field label="플랫폼">
            <input value={form.platform} disabled={!canRegister}
              onChange={(e) => setForm({ ...form, platform: e.target.value })}
              style={inputStyle} />
          </Field>
          <Field label="근거 URL">
            <input value={form.evidenceUrl} disabled={!canRegister}
              onChange={(e) => setForm({ ...form, evidenceUrl: e.target.value })}
              style={inputStyle} />
          </Field>
          <Field label="확신도">
            <div style={{ display: "flex", gap: 8 }}>
              {CONFIDENCE_OPTIONS.map((c) => (
                <button key={c} disabled={!canRegister} onClick={() => setForm({ ...form, confidence: c })}
                  style={{ ...inputStyle, cursor: canRegister ? "pointer" : "not-allowed",
                    background: form.confidence === c ? C.ink : "#fff", color: form.confidence === c ? "#fff" : C.ink }}>
                  {c}
                </button>
              ))}
            </div>
          </Field>
          <Field label="한 줄 설명">
            <input value={form.oneLine} disabled={!canRegister}
              onChange={(e) => setForm({ ...form, oneLine: e.target.value })}
              style={inputStyle} />
          </Field>
        </div>

        <div style={{ marginTop: 16 }}>
          <Btn tone="primary" disabled={!canSubmit} onClick={submit}>등록</Btn>
          {!canRegister && (
            <span style={{ marginLeft: 10, font: "500 11.5px Pretendard", color: C.faint }}>OPERATOR 이상만 등록할 수 있습니다</span>
          )}
        </div>
      </Card>

      <Card style={{ marginTop: 12 }}>
        <b style={{ fontSize: 13 }}>담당자별 적중률</b>
        <StateView query={q}>
          {(rows) => (
            <table style={{ width: "100%", marginTop: 14, borderCollapse: "collapse" }}>
              <thead>
                <tr style={{ textAlign: "left", borderBottom: `1px solid ${C.line}` }}>
                  {["담당자", "HIT", "MISS", "판정건수", "TI"].map((h) => (
                    <th key={h} style={{ padding: "8px 10px", font: "600 11.5px Pretendard", color: C.faint }}>{h}</th>
                  ))}
                </tr>
              </thead>
              <tbody>
                {rows.map((r) => (
                  <tr key={r.operatorName} style={{ borderBottom: `1px solid ${C.line}` }}>
                    <td style={{ padding: "10px" }}>{r.operatorName}</td>
                    <td style={{ padding: "10px" }}>{r.hit}</td>
                    <td style={{ padding: "10px" }}>{r.miss}</td>
                    <td style={{ padding: "10px" }}>{r.judged}</td>
                    <td style={{ padding: "10px", font: "700 12.5px ui-monospace, monospace",
                      color: r.trustIndex >= 0.5 ? C.rising : r.trustIndex < 0.3 ? C.fading : C.ink }}>
                      {Math.round(r.trustIndex * 100)}%
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
        </StateView>
      </Card>

      {toast && <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: C.ink, color: "#fff", padding: "12px 18px", borderRadius: 10, font: "500 12.5px Pretendard" }}>{toast}</div>}
    </div>
  );
}

const inputStyle: React.CSSProperties = {
  padding: "8px 10px", borderRadius: 8, border: `1px solid ${C.line}`, font: "500 12px Pretendard", width: "100%",
};

function Field({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <label style={{ display: "block" }}>
      <div style={{ font: "600 11.5px Pretendard", color: C.sub, marginBottom: 6 }}>{label}</div>
      {children}
    </label>
  );
}
```

- [ ] **Step 5: 타입체크**

Run: `cd admin && npx tsc --noEmit`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add admin/src/screens/SeedScreen.tsx admin/src/components/Layout.tsx admin/src/App.tsx admin/src/state/role.tsx
git commit -m "feat(admin): add SeedScreen (ADM-500) + routing"
```

---

## Task 9: 프론트엔드 fixture 모드 브라우저 검증

**Files:** 없음 (검증 전용)

- [ ] **Step 1: dev 서버 기동**

fixture 모드(`VITE_USE_FIXTURES` 미설정 = 기본 true)로 `admin/` 디렉터리에서 dev 서버를 띄운다.

- [ ] **Step 2: 브라우저로 ADM-500 진입**

사이드바 "정책" 그룹에서 "시딩 관리" 클릭 → `fxSeedAccuracy` 3행이 테이블에 표시되는지, TI 색상 구분(70%대는 강조색, 30%는 경고색)이 보이는지 확인.

- [ ] **Step 3: 등록 폼 검증**

역할을 OPERATOR로 전환 후 폼 채워서 "등록" 클릭 → fixture 모드이므로 실제 API 호출 없이 토스트("시딩 등록 완료: ...")가 뜨는지, 폼이 리셋되는지 확인. 역할을 REVIEWER로 전환 시 "등록" 버튼이 비활성화되고 안내 문구가 뜨는지 확인.

---

## Task 10: 실 DB 엔드투엔드 검증

**Files:** 없음 (검증 전용)

- [ ] **Step 1: 백엔드 기동**

`trd-db`(5433), `trd-embeddings`(6000) 도커 컨테이너가 떠 있는지 확인 후 `cd backend && ./gradlew :app:bootRun`.

- [ ] **Step 2: 프론트엔드를 실 API 모드로 전환**

`admin/.env.local`에 `VITE_USE_FIXTURES=false` 설정 후 dev 서버 재기동.

- [ ] **Step 3: 로그인 후 시딩 등록**

기존 세션에서 확인된 admin 계정으로 로그인 → ADM-500 화면에서 시딩 1건 등록 → 200 응답과 함께 담당자별 적중률 테이블에 새 행(HIT/MISS 0, judged 0)이 생기는지 확인.

- [ ] **Step 4: DB 직접 확인**

```bash
docker exec trd-db psql -U postgres -d trd -c "SELECT login_id, seed_user_id FROM admin_accounts WHERE seed_user_id IS NOT NULL;"
docker exec trd-db psql -U postgres -d trd -c "SELECT user_id, seed, result FROM submissions WHERE seed = true;"
```

`admin_accounts.seed_user_id`가 채워졌는지, 방금 등록한 `submissions` 행이 `seed=true`이고 `user_id`가 해당 `seed_user_id`와 일치하는지 확인.

- [ ] **Step 5: 중복 등록 확인**

같은 이름으로 다시 등록 시도 → 422 응답 + "이미 이 계정으로 시딩한 항목입니다" 메시지 확인.

- [ ] **Step 6: 판정 반영 확인 (선택)**

`submissions.result`를 수동 SQL로 `HIT`으로 변경한 뒤 ADM-500 화면 새로고침 → 해당 담당자 행의 HIT/판정건수/TI가 갱신되는지 확인 (VerdictRunner를 기다리지 않고 DB 직접 조작으로 판정 반영 시나리오만 검증 — 실제 배치 동작 자체는 기존 VerdictRunner 테스트 범위).

---

## 비범위 (Out of scope)

- ADM-311 담당자별 상세 드릴다운
- 시딩 계정 비활성화/삭제 UI
- 시딩 목표 건수(주 20건) 진행률 표시
