# Trend Radar — 프로젝트 컨텍스트

## 이 프로젝트가 뭔가

유저가 "이거 뜰 것 같다"고 트렌드를 제보하면, 시스템이 2주간 후속 제보 시계열(서로 다른 제보자 수, 이후 시간 분포·플랫폼 다양성까지)을 관측해 실제로 유행했는지 판정하고, 그 적중률로 유저에게 등급을 매기는 서비스. 외부 사이트 지표(X·네이버·인스타·디시 등)는 쓰지 않는다 — 판정은 오직 제보 자체로만 한다.

**한 줄 요약: 유저 제보 = 조기경보 신호이자 판정 데이터 / 배치 집계 = 최종 판정.**

---

## 절대 어기면 안 되는 원칙 5가지

작업 전 반드시 읽을 것. 이 중 하나라도 위반하는 구현이 나오면 되돌리는 비용이 매우 크다.

### R1. 운영자가 임의로 유행을 결정하지 않는다

판정 근거는 외부 지표가 아니라 제보 시계열(서로 다른 후속 제보자 수)이지만, "누가 판정을 내리는가"는 여전히 사람이 아니다.
HIT/MISS 판정은 `verdict_runner` 배치가 정해진 공식으로만 내리고, 관리자도 판정 결과를 직접 바꿀 수 없다.
"이거 더 뜰까요" 투표(voluntary vote)는 판정에 절대 입력되지 않는다 — 판정 입력은 제보(submission) 자체뿐, 좋아요·투표 같은 부가 반응이 아니다.

### R2. 원장(ledger)은 불변이다

`score_ledger`는 **append-only**. UPDATE/DELETE 금지.
정정이 필요하면 `ADJ` 상쇄 행을 추가한다. `verdicts`도 마찬가지로 `supersedes` 필드로 새 행을 쌓는다.
→ 이유: 유저 이의 제기 시 "운영진이 임의로 고쳤다"는 주장을 반박할 근거가 사라진다.

### R3. 등급은 활동량이 아니라 적중률 주축이다

승급 조건은 활동 점수(AS)와 신뢰도(TI)를 **AND**로 요구한다. 제보 건수 기반 등급 금지.

### R4. 확정은 사람이 — 자동은 플래그·명백한 병합·임시 비공개까지

어뷰징 탐지·병합 후보 추출은 자동. 제재 확정·비공개 **확정**은 사람. 병합은 유사도 ≥0.85만 자동 확정하고(되돌리려면 분리), 0.75~0.85는 관리자 큐로 간다.
신고·어뷰징 대응에서 자동 적용이 허용되는 가역적 조치는 **임시 비공개(`TEMP_HIDDEN`) 하나뿐**이다 — 신고 큐 SLA(4h) 초과 시 `sla_watch`가 적용하고, 사람이 확인해 해제하거나 확정한다. 다른 조치를 이 목록에 추가하려면 R4 자체를 개정해야 한다.
오탐으로 인한 자동 강등·자동 제재 확정은 절대 구현하지 않는다.

### R5. 판정 입력은 제보 데이터뿐이며, 제보는 머릿수가 아니라 여러 축으로 본다

외부 사이트 지표(해시태그 게시물 수, 갤러리 글 수, 검색량 등)는 어떤 형태로도 판정에 들어가지 않는다.
"2주 안에 N명이 제보했다"는 단순 누적 카운트는 유행의 증거로 불충분하다. 서로 다른 제보자 수에 더해 **제보의 시간 분포(지속성)**, **플랫폼 다양성**, **제보자 독립성**을 신호 축으로 둔다(SP4에서 도입).
VOID·시딩(`is_seed=true`) 제보는 신호에서 제외한다(T와 선점 순위 모두). 시딩이 제보자 수에 들어가면 시딩 계정 수만큼 HIT가 공짜로 만들어져 R1이 깨진다.
(현행 코드는 제보자 수 축만 구현. 나머지 축은 SP4에서 Phase 0 백테스트와 함께 도입.)

> `SP0~SP4(SP0.5 포함)`는 설계 재검토(2026-09-17)가 나눈 서브프로젝트 실행 단위다 — SP0 문서 정비 / SP0.5 즉시 수정 / SP1 판정 파이프라인 / SP2 병합 무결성 / SP3 콘솔 통제·SLA / SP4 판정 신호 재설계. 이 순서대로 진행하며 Phase 1 시작 전에 SP1까지 끝나야 한다. 상세는 `docs/superpowers/specs/2026-09-17-design-review-design.md`.

---

## 핵심 도메인 개념

| 용어                        | 의미                                                     |
| --------------------------- | -------------------------------------------------------- |
| **TrendItem**         | 병합된 트렌드 항목(클러스터). 판정 단위                  |
| **Submission**        | 개별 제보. 확신도(c)를 베팅하듯 건다                     |
| **order_rank**        | 클러스터 내 제보 순서. 선점 가중치에 직결                |
| **T**                 | 서로 다른 후속 제보자 수 / 목표치. 판정 점수 (0~1, clip). 시딩·VOID 제외 |
| **reach_level**       | HIT 시 확산 규모 (L1 국지 ~ L4 매스)                     |
| **AS (Active Score)** | 누적 활동 점수 (시간감쇠 반영)                           |
| **TI (Trust Index)**  | 베이지안 평활 적중률. 초기값 0.4                         |
| **VOID**              | 판정 불가. 점수 변동 없고 제보권만 반환                  |

### 점수 공식

```
HIT :  Δ = + c × w_order × (1 + m)
MISS:  Δ = − c × 0.5
VOID:  Δ = 0
AS   = Σ Δ_i × 0.5^(원장 기록일로부터 경과일 / 90)

c        확신도 (10 / 30 / 50)
w_order  선점 가중 (1위 1.0 / 2위 0.6 / 3위 0.4 / 4위↓ 0.2)
m        확산배수 (L1 0.2 / L2 0.5 / L3 1.0 / L4 1.5)
```

**원장(`score_ledger`)에는 감쇠 없는 원값을 기록한다.** 반감기 90일 감쇠는 AS를 계산하는 조회 시점에만 적용한다. 원장에 감쇠를 미리 곱해 두면 "그때 얼마를 받았나"가 불변이 아니게 되고(R2), 반감기를 바꾸려면 과거 원장을 UPDATE해야 해 사실상 변경이 불가능해진다. 조회 시점에도 곱하면 이중 적용이다.

> ⚠ 조회 시점 감쇠를 전역 반감기로 하면 반감기를 바꿀 때 **과거 AS가 전부 재계산된다**(ADM-600 "비소급"과 충돌). 그래서 원장 행마다 `halflife_days`·`decay_anchor_at`을 기록하고 AS는 행별 값으로 감쇠한다(O11 = (b), 2026-09-20 결정) — 반감기를 바꿔도 과거 AS는 그대로다. 재판정·VOID 차액(ADJ)은 원 판정과 같은 감쇠 기준으로 기록해 정확히 상쇄된다.

**실패 페널티가 이득의 절반인 것은 의도된 설계다.** 대칭으로 만들면 유저가 "이미 뜬 것만" 제보하게 되어 조기경보 목적이 무너진다. 임의로 바꾸지 말 것.

### 신뢰도

```
TI = (HIT + 2) / (HIT + MISS + 5)     # 최근 180일 기준
```

소표본 왜곡(1승 0패 = 100%) 방지용 베이지안 평활. α=2, β=3.

---

## 구현 시 자주 틀리는 지점 (중요)

### ⚠ order_rank 는 저장 컬럼이 아니다

병합 검수는 최대 24시간 지연될 수 있다. "기존 최대값 + 1"로 부여하면 **먼저 제보한 사람이 뒤로 밀린다.**
→ `submissions.created_at` 기준 파생값으로 두고 병합 시마다 전체 재정렬.
→ 판정 시점(D+14)에 `verdicts.evidence_json`으로 스냅샷 동결.

```sql
SELECT id, RANK() OVER (ORDER BY created_at ASC) AS order_rank
FROM submissions
WHERE trend_item_id = :target AND result <> 'VOID' AND NOT is_seed
```

### ⚠ first_seen_at 이 바뀌면 order_rank 재계산, 판정 후면 병합 금지

병합으로 `first_seen_at`이 앞당겨지면 `order_rank`와 (SP4 이후) 시간 분포 신호가 바뀐다.
판정 전(PENDING)이면 병합 트랜잭션 안에서 재계산한다 — **빼먹어도 에러가 나지 않고 선점 가중치만 조용히 틀린다. 반드시 테스트 케이스로 걸어둘 것.** 판정 후(RESOLVED)면 **병합 자체를 거부(409)**해야 한다 — 흡수된 제보가 다음 판정에서 원장에 다시 실려 이중 점수가 된다. 판정 후 병합(ADJ 상쇄 경로)은 Phase 2(O9).

> **구현(SP2).** `MergeService`가 두 항목을 id 오름차순으로 `SELECT … FOR UPDATE` 잠근 뒤 `MergeGuard`로 판정 전 상태(DRAFT·PENDING) + 현행 판정 행 없음을 확인하고, 아니면 409(`merge-judging`·`merge-resolved`)로 거부한다. `cluster_merge`는 판정된 항목과 닮으면 병합하지 않고 감사 로그 `MERGE_SKIPPED_JUDGED`만 남긴다. 순위 재계산은 뷰라서 자동이고 `MergeRecomputeTest`로 건다. 병합으로 관측 마감이 병합 시점 + 3일보다 당겨지면 그 시점까지 연장한다(상한 최초 제보 + 21일).

### ⚠ 같은 유저의 중복 제보는 VOID 처리

병합 후 자기 자신과 중복되면, 확신도를 두 번 건 헤지가 되어 베팅 구조가 무너진다. 늦은 쪽은 VOID 처리된다(`MergeService.dedupSameUserSubmissions`). `voided_at`이 속한 주에 제보권 한 장이 반환된다(저장 카운터 없이 제보 행에서 파생, `QuotaService`).

---

## 아키텍처 흐름

```
[제보] → [정규화] → [완전일치 병합] → [임베딩 유사도]
                                        ├ ≥0.85 자동 병합
                                        ├ 0.75~0.85 → 관리자 큐 (ADM-100)
                                        └ <0.75 신규 항목
                                              ↓
                                      [D+14 판정 엔진 (제보 집계 — 현재는 제보자 수 축)]
                                              ↓
                                    [score_ledger 기록]
                                              ↓
                                  [주간 등급 재계산 · 제보권 리필(주 경계)]
```

### 배치 잡

| 주기             | 잡                 | 비고                                 |
| ---------------- | ------------------ | ------------------------------------ |
| 시간당           | `sla_watch`      | 신고 4h(OPEN, 공개 중) → 임시 비공개 + `auto_hidden_at` / 병합 대기 24h → 두 항목 마감을 now + 24h까지(상한 최초 제보 + 21일) / 90일 미접속 관리자 비활성화(마지막 ADMIN 제외). 시간당, 코드 기본 `0 0 * * * *` |
| 일 1회           | `cluster_merge`  | 임베딩 병합 + 관리자 큐 적재. 판정된 항목과 닮으면 병합하지 않고 기록만(SP2). ADM-900에서 수동 실행 가능 |
| 일 1회           | `verdict_runner` | 관측 마감 지난 항목 JUDGING → `JudgeService`가 항목별 트랜잭션으로 판정 → 원장 기록. 판정 로직은 `JudgeService` 하나를 배치·재판정·VOID·미리보기가 공유 |
| 일 1회           | `abuse_scan`     | **미구현(Phase 2)**. 플래그만 생성, 제재는 안 함 |
| 주 1회(월 00:00 **KST**) | `grade_recalc`   | 공식 등급 스냅샷(코드에 zone 명시). TI 최근 180일, 강등 규칙(Phase 2) 전까지 등급을 내리지 않는다. 제보권 리필(이월 없음)은 주 경계 자체라 배치가 없다 |
| 월 1회           | `l4_quota`       | **미구현(Phase 3)**. L4 정원 재산정  |

---

## 관리자 콘솔

**큐 중심 설계.** 진입 화면은 대시보드가 아니라 "지금 처리할 것".

| 큐            | SLA     | 초과 시                    |
| ------------- | ------- | -------------------------- |
| 신고 콘텐츠   | 4h      | **자동 임시 비공개**       |
| 병합 검수     | 24h     | 판정 유예 자동 연장        |
| 어뷰징 플래그 | 48h     | 상위 역할 에스컬레이션     |
| 이의 제기     | 5영업일 | 알림 + 적색 표시           |

> 신고·병합 초과 동작은 `sla_watch`가 한다. 알림 채널(O10)은 Phase 2 — 지금은 감사 로그와 ADM-010 배너. 어뷰징·이의 제기는 큐 자체가 Phase 2다.

**2인 승인 대상**: 유저 제재 확정 / 등급 수동 조정 / 파라미터 적용 / 100점 초과 상쇄 원장 / 관리자 계정 생성·권한 변경(신규 계정은 생성 후 7일간 승인권 없음)

**2인 승인 = 요청자 + 승인자 1명**(승인자: 활성 ADMIN, 요청자 아님, 승인권 유예 7일 경과). 승인할 ADMIN이 요청자 외 0명이면 계정 생성만 단독 허용(부트스트랩 예외, 감사 `ACCOUNT_CREATE_BOOTSTRAP`).

**통제는 서버 한 곳(`ApprovalGate`)에서.** 재판정·판정된 항목 VOID·수동 ADJ가 원장을 100점 넘게(변동 절댓값 합) 움직이면 아무것도 쓰지 않고 승인 요청(202)이 되고, 승인되면 그 시점 입력으로 다시 계산해 실행한다. 관리자 세션은 요청마다 재검증한다(비활성·승인 대기·역할 변경 → 401).

**ADM-900 배치 관리**: `cluster_merge` 수동 실행(OPERATOR/ADMIN). 실행 중이면 409.

**파라미터 스튜디오(ADM-600)**: 시뮬레이션 없이 승인 요청 불가. 기본값은 비소급 예약 적용(반감기는 예외 여부 미결 — O11).

**ADM-700 감사 로그는 나중에 붙일 수 없다.** Phase 0부터 필수.

---

## 구현 순서

### Phase 0 (~4주) — 유저 노출 없음

판정 엔진 + 과거 사례 30건 백테스트. 백테스트는 SP4(판정 신호 재설계)와 함께 수행하며, 판정 결과가 실제로 저장되는 SP1 이후에야 의미가 있다.
**목적: submitterTarget·임계값 보정.** 지금 값들은 근거 없는 초기 추정치다.
필요 화면: ADM-010, 200, 600, 700, 800, 900

### Phase 1 (4~12주) — 클로즈드 베타

운영진 시딩 주 20건 + 초대 유저 50~100명.
시딩분은 `is_seed=true`로 **점수 원장에 반영하지 않고 판정 신호(T)·선점 순위에서도 제외한다**(R5). 시딩은 클러스터 앵커 역할만 한다.
**선행 조건: 제보권 쿼터·리필·VOID 반환(SP1에서 구현).** 쿼터 없는 베팅 구조는 난사를 보상한다.
필요 화면 추가: ADM-100, 110/111, 311, 410, 500
**ADM-311(유저 원장)은 이의 제기 대응의 근거 화면이고, ADM-410(신고 큐)은 법적 리스크 대응이라 Phase 1로 앞당긴다.** ADM-410은 구현돼 있고, ADM-311은 `/admin/users/**`(SP3)로 원장·산정 근거·수동 ADJ를 제공한다.

### Phase 2 (12~24주) — 공개 + 등급 활성화

필요 화면 추가: ADM-300, 310, 320, 400, 610
**이의 제기(ADM-400 + `POST /v1/appeals`)는 공개 전 필수** — 약관에 처리 기한을 명시해야 하는데 현재 유저 측 엔드포인트가 없다(404, 미구현 — Phase 2, SP3 스펙 §0 비범위).

### Phase 3 (24주~) — 리워드 · L4 정원제

---

## 열려 있는 결정 사항

| #  | 항목                                         | 상태                                        |
| -- | -------------------------------------------- | ------------------------------------------- |
| O1 | submitterTarget(목표 제보자 수)·판정 임계값 | **상대값 방향 확정**(`max(하한, 활성 제보자 × 비율)`). 값은 SP4 백테스트 |
| O2 | 판정 유예 14일 적정성                        | 카테고리별 차등 검토                        |
| O4 | 확신도 3단계 간격                            | 유저 행동 데이터 필요                       |
| O5 | 반감기 90일                                  | 카테고리별 유행 주기 반영 여부              |
| O6 | 독립 서비스 / 커머스 부가모듈                | **미정. 인증·회원 모듈 설계에 영향** |
| O7 | 임베딩 임계값 0.85 / 0.75                    | 한국어 트렌드 데이터로 튜닝 필요            |
| O8 | 시간 분포·플랫폼 다양성·제보자 독립성 축의 가중치 | SP4                                    |
| O9 | 판정 후 병합(ADJ 상쇄 경로) 도입 시점        | Phase 2. 그 전까지 RESOLVED 병합은 409      |
| O10 | SLA 통보 채널(메신저 웹훅)                  | Phase 2. Phase 1은 로그                     |
| O11 | AS 조회 감쇠에 쓸 반감기 버전(전역 현재값 vs 원장 기록 시점 파라미터) | **결정됨(b)** — 원장 행별 반감기(2026-09-20) |

> O3(디시 데이터 수집 방식)은 2026-08-13 판정 모델 피벗으로 폐기.

---

## 법무 체크리스트 (구현과 병행)

- [ ] 등급 산정 기준·근거를 약관에 명시 (평가 정보 → 미공개 시 제재 방어 불가)
- [ ] 이의 제기 절차 및 처리 기한 명시 (코드의 SLA와 동일해야 함)
- [ ] 강등·제재 사유 열거주의. "기타 운영진 판단" 단독 의존 금지
- [ ] 이해관계 미고지 시 제재 조항 삽입
- [ ] 디바이스 지문·IP 수집을 개인정보 수집·이용 동의에 별도 명시
- [ ] 리워드 지급 시 세무 처리 확인

---

## 코드 작성 시 규약

- 점수·등급 계산 로직은 **순수 함수로 분리**하고 파라미터를 주입받게 할 것. 파라미터 스튜디오의 시뮬레이션이 같은 함수를 재사용해야 한다.
- 시간 관련 값은 전부 UTC 저장, 표시만 KST.
- 병합 연산은 **단일 트랜잭션 + 행 잠금 + 멱등성 키**. 검수자 동시 처리 충돌은 실제로 발생한다. 큐 결정은 `Idempotency-Key` 필수 — `merge_queue.decision_key` UNIQUE(V29)가 보장, 잠금 순서는 큐 행 → 항목(id 오름차순).
- 배치는 재실행 가능해야 한다(idempotent). `verdict_runner` 재실행이 점수를 두 번 주면 안 됨. 멱등성은 코드 가드가 아니라 DB 제약으로 보장한다 — `verdicts` 부분 UNIQUE(`verdict_one_original_per_item`, V4, 구현됨), `score_ledger(verdict_id, submission_id)` UNIQUE(V28), 재판정 체인 분기 방지 `verdict_superseded_once`(V28).
- 판정 로직은 `JudgeService`(`judge` 모듈) 하나로 모은다. **배치 클래스 안에서 같은 빈의 `@Transactional` 메서드를 직접 호출하지 말 것** — 프록시를 타지 않아 트랜잭션이 무효가 되고 dirty-check 변경이 flush되지 않는다.
- 제약(UNIQUE·CHECK)은 코드 가드 대신 DB에 둔다.
- 관리자 API는 모든 응답에 **산정 근거를 함께 반환.** `"TI 0.42 / 요구치 0.45 / 부족분 0.03"` 형태.

---

## 상세 문서

설계 문서는 저장소 **루트**에 있다(`docs/`가 아님).

- `01-system-design.md` — 시스템 설계서 (도메인 모델, 판정 신호, 점수 알고리즘, 등급 체계)
- `02-admin-console.md` — 관리자 콘솔 설계서 (화면 명세, 권한 매트릭스, 와이어프레임)
- `03-merge-clustering.md` — 병합·클러스터링 상세 (정규화 규칙, 트랜잭션 절차, 동시성, 실패 모드)
- `04-development-plan.md` — 개발 설계서 (모듈 구조, 스키마, 배치, 로드맵)
- `05-screen-endpoint-map.md` — 화면 ↔ 엔드포인트 매핑
- `docs/superpowers/specs/2026-09-17-design-review-design.md` — 설계 재검토 진단·결정·서브프로젝트(SP0~SP4)
