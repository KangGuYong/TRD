# 트렌드 제보 · 유저 등급 시스템 설계서 (v0.2)

> **문서 성격**: 초안. 임계값 등 수치 파라미터는 Phase 0 백테스트로 재보정하는 것을 전제로 함.
> **작성 기준일**: 2026-08-06 · **개정**: 2026-09-17 (판정 모델 피벗 2026-08-13 반영 — 외부 지표 판정 → 제보 시계열 판정. 설계 재검토 결정은 `docs/superpowers/specs/2026-09-17-design-review-design.md`)

---

## 0. 설계 전제 (변경 시 이 문서 전체 재검토 필요)

| # | 전제 | 비고 |
|---|---|---|
| A1 | 유저 제보는 **조기경보 신호이자 판정 데이터**다. 유행 여부의 최종 판정은 **제보 시계열 집계**(4장)가 한다. 외부 사이트 지표는 쓰지 않는다 | 투표·좋아요·동의는 판정에 입력되지 않음 |
| A2 | 등급은 "활동량"이 아니라 **적중률(정확도)** 을 주축으로 산정한다 | 제보 건수 기반 등급은 금지 |
| A3 | 판정 유예기간은 **D+14** (2주). 필요 시 항목 유형별 차등 | 4주 옵션 별도 |
| A4 | 초기 3~6개월은 운영진 시딩 전제. 유저 제보만으로 부팅되지 않음 | 8장 참조 |
| A5 | 독립 서비스 또는 기존 커머스 부가모듈 어느 쪽도 수용 가능한 구조로 설계 | 인증·회원 모듈만 교체 |

---

## 1. 시스템 개요

```
[유저 제보]  →  [중복 병합/클러스터링]  →  [항목(TrendItem) 생성]
                                              ↓
                                    [제보 신호 집계 (시딩·VOID 제외)]
                                              ↓
                                    [D+14 자동 판정 엔진 · JudgeService]
                                              ↓
                          [점수 원장(Ledger) 기록]  →  [주간 등급 재계산]
                                              ↓
                                    [제보권 리필 · 배지 갱신]
```

> 이 다이어그램은 목표 구조다. `JudgeService`는 SP1에서 신설(현행은 `VerdictRunner`/`VerdictAdminService`가 판정 로직을 각각 보유). `verdict_runner`는 매일 실행되어 `verdicts`·`score_ledger`는 정상 기록하지만, self-invocation으로 트랜잭션이 걸리지 않아(4.3 참조) 제보 result·항목 상태는 갱신되지 않고 TI는 0.4에 고정된다(SP1에서 수정). **제보권 리필** 단계는 현행 미구현(3.3 참조, SP1).

핵심 분리 원칙 세 가지.

1. **제보(주관)와 판정(객관)의 분리** — 사람은 후보를 올리고, 기계가 채점한다.
2. **점수 원장과 등급의 분리** — 원장은 불변(append-only), 등급은 원장을 읽어 산출하는 파생값.
3. **활동 축과 정확도 축의 분리** — 승급은 두 축을 **모두** 만족해야 한다.

---

## 2. 도메인 모델

### 2.1 주요 엔티티

| 엔티티 | 설명 | 핵심 필드 |
|---|---|---|
| `users` | 회원 | id, joined_at, verified_at, status |
| `trend_items` | 병합된 트렌드 항목(클러스터) | id, canonical_name, aliases[], category, first_seen_at, state |
| `submissions` | 개별 제보 | id, user_id, trend_item_id, raw_input, confidence, order_rank, disclosure, created_at |
| `endorsements` | 중복 제보의 동의 처리분(현행: 저장만, 점수·판정 미반영) | submission_id, user_id, created_at |
| `merge_queue` | 회색지대 병합 후보 큐 | new_trend_item_id, target_trend_item_id, status, assigned_to, decision_key |
| `verdicts` | 판정 결과(append-only, `supersedes`로 재판정) | trend_item_id, result, reach_level, score_T, judged_at, evidence_json |
| `score_ledger` | 점수 원장(append-only) | id, user_id, submission_id, delta, reason, created_at |
| `user_grades` | 등급 스냅샷 | user_id, grade, trust_index, active_score, computed_at |
| `abuse_flags` | 어뷰징 탐지 기록 | user_id, rule_code, severity, detected_at |
| `appeals` | 이의 제기 | id, user_id, target_type, target_id, status, resolved_at |

### 2.2 상태 전이

**TrendItem.state**
```
DRAFT → PENDING(관측 중) → JUDGING(D+14 도달) → RESOLVED(HIT|MISS) 
                                              → VOID(판정불가·중복·규정위반)
```

**Submission.result**: `PENDING / HIT / MISS / VOID`

---

## 3. 제보 파이프라인

### 3.1 제보 입력 폼

| 필드 | 필수 | 설명 |
|---|---|---|
| 항목명 | ✔ | 자유 텍스트. 정규화 대상 |
| 카테고리 | ✔ | 밈 / 상품 / 인물·채널 / 챌린지 / 슬랭 / 기타 |
| 최초 목격 플랫폼 | ✔ | 디시(갤러리명) / X / 인스타 / 유튜브 / 기타 |
| 근거 URL | ✔ | 1건 이상. 캡처만 있는 제보는 반려 |
| 확신도 | ✔ | 10 / 30 / 50 중 택1 (=베팅 포인트) |
| 이해관계 고지 | ✔ | "본인·소속사·거래처와 관련 있음" 체크박스 |
| 한줄 설명 | ✔ | 200자 이내 |

> 이해관계 고지 필드는 **법적 방어선**이다. 미고지 후 적발 시 등급 초기화 조항의 근거가 된다(11장).

### 3.2 중복 병합

1. 항목명 정규화 (공백·특수문자 제거, 자모 분리 처리, 소문자화)
2. 정규화 키 완전일치 → 즉시 병합
3. 불일치 시 임베딩 코사인 유사도 계산 → **≥ 0.85** 이면 동일 클러스터 후보
4. 0.75 ~ 0.85 구간은 **운영자 큐**로 이관 (자동 병합 금지)
5. 병합 시 `order_rank` 부여: 클러스터 내 시간순 1, 2, 3, …

**중복 제보자 처리**: 제보권을 소모하지 않고 `endorsements`로 전환. 적중 시 소액 점수(선점 4위 이하 가중치 적용), 실패 시 감점 없음. → *"남이 올린 걸 따라 올려 손해 볼까"* 하는 위축을 막으면서 데이터는 확보.
> 현행: `endorsements`는 저장만 되고 점수·판정에 반영되지 않는다. 반영 여부와 방식은 SP4(판정 신호 재설계) 백테스트와 함께 결정한다.

### 3.3 제보권 (Quota)

| 등급 | 주당 제보권 | 비고 |
|---|---|---|
| L0 관찰자 | 2 | 가입 후 7일 + 본인인증 완료 전 제보 불가 |
| L1 제보자 | 3 | |
| L2 탐지자 | 5 | |
| L3 분석가 | 8 | |
| L4 선구자 | 12 | 정원제 |

매주 월요일 00:00 리필. **이월 없음**(비축 후 일괄 투척 방지).

> **Phase 1 선행 조건(P3).** 쿼터가 없으면 "MISS 페널티 = HIT의 절반"이라는 의도된 비대칭이 역효과를 낸다 — 적중률 ~30%만 넘으면 난사가 이득. 현행 미구현(차감·리필·VOID 반환 모두 TODO). SP1에서 판정과 같은 트랜잭션 경계로 구현.

---

## 4. 자동 판정 엔진

판정 입력은 **제보 데이터뿐**이다(R5). 외부 사이트 지표는 수집하지 않는다.

### 4.1 판정 신호 (TrendSignal)

D+14 도달 시 항목의 제보를 집계한다. 집계 대상에서 **시딩(`is_seed=true`)과 VOID 제보는 제외**한다.

| 축 | 정의 | 상태 |
|---|---|---|
| 서로 다른 제보자 수 `n` | 유효 제보의 distinct user 수 | 구현 |
| 시간 분포 | 2주 중 후속 제보가 들어온 날 수, 첫 48h 쏠림 비율 — 이미 뜬 것을 몰아서 적는 행동을 감점 | SP4 |
| 플랫폼 다양성 | 유효 제보의 distinct 플랫폼 수(플랫폼은 enum) | 집계만 구현, 미사용 → SP4 |
| 제보자 독립성 | 가입일·디바이스·IP 군집이 같은 제보자를 1명으로 압축 | SP4 |

`TrendSignal`은 SP1에서 확장 가능한 형태(제보 시각 목록·플랫폼·제보자 메타 포함)로 정의해 SP4가 파이프라인을 다시 열지 않게 한다.

### 4.2 종합 점수 T

```
T = clip( n / submitterTarget , 0, 1 )        # 현행
```

`submitterTarget`(기본 20)은 절대값이라 유저 규모와 무관하다 — Phase 1 유저 50~100명이 카테고리별로 흩어지면 도달 불가능해 전원 MISS로 수렴하고, TI 하락 → 등급 정체 → 이탈의 디플레 루프가 생긴다. 반대로 계정 4개면 HIT(0.20), 15개면 L4다.

**SP4에서 상대값으로 전환한다(P9):**
```
submitterTarget = max( 하한 , 최근 N일 활성 제보자 수 × 비율 )
```
하한·비율·기간은 파라미터 스튜디오(ADM-600)에서 관리하고 값은 Phase 0 백테스트로 정한다(O1). 다축 신호의 가중치는 O8.

### 4.3 판정 규칙

| 조건 | 결과 | reach_level | 확산배수 m |
|---|---|---|---|
| T < 0.20 | MISS | — | — |
| 0.20 ≤ T < 0.35 | HIT | L1 국지 | 0.2 |
| 0.35 ≤ T < 0.55 | HIT | L2 플랫폼 | 0.5 |
| 0.55 ≤ T < 0.75 | HIT | L3 크로스플랫폼 | 1.0 |
| T ≥ 0.75 | HIT | L4 매스 | 1.5 |

판정은 `JudgeService` 하나가 수행하며 배치(`verdict_runner`)·관리자 재판정·파라미터 시뮬레이션이 같은 서비스와 같은 파라미터 소스(`ParameterSetProvider`)를 쓴다(P6) — **`JudgeService`는 SP1에서 신설한다. 현행은 `VerdictRunner`와 `VerdictAdminService`가 판정 로직을 각각 들고 있고, 재판정은 승인된 파라미터 대신 `ParameterSet.defaults()`를 쓴다.** 항목은 `PENDING → JUDGING → RESOLVED`로 전이하고, 판정·원장 기록·제보 result·항목 상태 변경이 한 트랜잭션이어야 한다 — **SP1의 목표 상태다.** 현행 `VerdictRunner`는 `run()`이 같은 빈의 `@Transactional protected judgeOne()`을 직접 호출해(self-invocation) 프록시를 타지 못하므로 **트랜잭션이 걸리지 않는다.** `verdicts`·`score_ledger`는 명시 save라 저장되지만 `submissions.result`와 `trend_items.state`는 dirty-check에 의존해 flush되지 않는다 — 제보는 계속 PENDING이고 TI는 0.4에 고정된다. `JUDGING` 상태도 현재는 설정되지 않는다.

### 4.4 VOID — 판정 공식의 출력이 아니라 사건의 결과

VOID는 `verdict_runner`가 계산하는 값이 아니다(P5). 아래 사건이 발생하면 **그 시점에** 제보가 VOID 처리되고, 원칙적으로 제보권이 반환되어야 한다. 판정 배치는 VOID 제보를 집계에서 제외할 뿐이다.

> 제보권 반환은 3.3의 쿼터 시스템 자체가 **미구현**이라 지금은 일어나지 않는다(코드 주석: "VOID면 제보권 반환은 QuotaService 도입 후 처리"). SP1에서 쿼터 시스템과 함께 구현.

- 항목이 VOID됨(관리자 VOID 처리, 병합 시 흡수) → 해당 항목의 모든 제보
- 병합 후 같은 유저의 제보가 중복됨 → 늦은 쪽 (03 §4.4)
- 규정 위반(허위 URL, 미고지 이해관계)으로 관리자가 개별 제보를 VOID

판정 시점에 유효 제보가 0건이면 항목 자체가 VOID.

---

## 5. 점수 알고리즘

### 5.1 건별 점수

```
HIT :  Δ = + c × w_order × (1 + m)
MISS:  Δ = − c × 0.5
VOID:  Δ = 0
```

| 기호 | 의미 | 값 |
|---|---|---|
| `c` | 확신도 | 10 / 30 / 50 |
| `w_order` | 선점 가중치 | 1위 1.0 / 2위 0.6 / 3위 0.4 / 4위 이하 0.2 |
| `m` | 확산배수 | 4.3 표 참조 |

> **원장에는 감쇠 없는 원값을 기록한다.** 시간감쇠는 5.3 AS 계산에서만 적용. 원장에 감쇠를 미리 곱해 두면 "그때 얼마를 받았나"가 불변이 아니게 되고(R2) 반감기를 바꾸려면 과거 원장을 UPDATE해야 한다. 양쪽에 걸면 이중 적용(현행 코드 상태 — SP1에서 전환). 조회 시점 감쇠에 쓸 반감기 버전은 O11(미결).

> **실패 페널티를 이득의 절반으로 둔 이유**: 대칭 페널티는 유저를 극단적으로 보수화시켜 "이미 뜬 것만 제보"하게 만든다. 조기경보라는 시스템 목적과 정면으로 충돌한다.

### 5.2 신뢰도 지수 (Trust Index)

최근 180일 판정 완료분에 대한 베이지안 평활 적중률.

```
TI = (HIT수 + α) / (HIT수 + MISS수 + α + β),   α = 2, β = 3
```

신규·소표본 유저가 1승 0패로 100%가 되는 왜곡을 막는다. 초기값은 0.4.

### 5.3 활동 점수 (Active Score)

```
AS = Σ Δ_i × d_i ,    d_i = 0.5 ^ (원장 기록일로부터 경과일 / 90)     # 반감기 90일, 조회 시점 계산
```

**AS 와 TI 는 서로 다른 축이며, 승급은 두 값을 모두 요구한다.** (전제 A2)

---

## 6. 등급 체계

| 등급 | 명칭 | 승급 조건 (AND) | 제보권 | 혜택 |
|---|---|---|---|---|
| L0 | 관찰자 | 가입 직후 | 2 | 열람 |
| L1 | 제보자 | 판정완료 ≥ 5, TI ≥ 0.35, AS ≥ 30 | 3 | 배지 |
| L2 | 탐지자 | 판정완료 ≥ 15, TI ≥ 0.45, AS ≥ 150 | 5 | 트렌드 상세 지표 열람 |
| L3 | 분석가 | 판정완료 ≥ 40, TI ≥ 0.55, AS ≥ 500 | 8 | 원시 시계열 다운로드, 우선 노출 |
| L4 | 선구자 | L3 + 상위 1% (정원제) | 12 | 리워드, 운영 자문단 참여 |

> 현행: L4는 `GradePolicy`에 없다(Phase 3). 6.1 강등 규칙은 미구현(Phase 2) — 구현 전까지 강등은 발생하지 않는다.

### 6.1 강등 규칙

- **주기**: 매주 재계산, 단 강등 판정은 **90일 롤링 기준** 미달이 4주 연속일 때만 발동
- **폭**: 1회 1단계까지. 2단계 하락 금지
- **유예**: 강등 2주 전 알림 + 회복 조건 안내
- **L4 정원제**: 매월 재산정, 밀려난 인원은 L3로 자동 이동 (페널티 아님을 UI에서 명시)

### 6.2 노출 정책

- 절대평가 배지(L0~L4)는 **공개**
- 전체 서열 랭킹은 **비공개**. 상위 10위만 명예의 전당 형태로 노출하되 **본인 동의 시에만**
- 각 유저는 자신의 AS·TI·판정 내역 **전체를 열람 가능** (11장 약관 요건과 직결)

> 전체 서열 공개는 상위권의 보수화(등급 지키기)와 하위권 이탈을 동시에 유발한다. 등급 인플레만큼 위험한 실패 모드.

---

## 7. 어뷰징 방어

| 위협 | 대응 |
|---|---|
| 양치기 제보(산탄총) | 제보권 제한 + 확신도 베팅 + MISS 감점 |
| 다중 계정 | 가입 후 7일 쿨다운, 본인인증 필수, 디바이스 핑거프린트 + IP 대역 클러스터링 |
| 상호 추천 링(collusion) | `endorsements` 그래프에서 상호 추천 밀도 이상치 탐지 → 해당 엣지 점수 무효화 |
| 자가 유행 조작 (다중 계정으로 후속 제보 생성) | **제보자 독립성 압축**(4.1, SP4) — 같은 군집의 제보자는 판정 입력에서 1명으로 센다. 사후 제재가 아니라 판정 입력 단계에서 방어 |
| 뒷광고·대행사 제보 | 이해관계 고지 필수 + 미고지 적발 시 **AS 전액 소멸 + 3단계 강등** |

**공통 원칙**: 자동 탐지는 **플래그까지만**, 계정 제재는 사람이 결정한다(R4). 오탐으로 인한 강제 강등은 이의 제기 폭증과 신뢰 붕괴로 직결된다. `abuse_scan` 잡은 미구현(Phase 2).

---

## 8. 콜드 스타트 운영 계획

| 단계 | 기간 | 내용 | 성공 기준 |
|---|---|---|---|
| Phase 0 | ~4주 | 판정 엔진 가동. 과거 사례 30건을 제보 데이터로 재생해 백테스트. 유저 노출 없음 | submitterTarget·임계값 보정(O1) |
| Phase 1 | 4~12주 | 운영진 시딩 (주 20건 이상, T 집계 제외). 초대 기반 클로즈드 베타 50~100명. **선행 조건: 제보권 쿼터(SP1)** | 주간 활성 제보자 30명 |
| Phase 2 | 12~24주 | 공개 전환. 등급 시스템 활성화 | 유저 제보 비중 > 70%, TI 중앙값 > 0.4 |
| Phase 3 | 24주~ | 리워드·L4 정원제 도입 | 4주 리텐션 > 25% |

> **Phase 0 백테스트는 생략하면 안 된다.** 4.2의 submitterTarget과 4.3의 임계값은 근거 없는 초기 추정치이며, 실데이터 보정 없이 공개하면 판정 시비가 서비스 초기에 신뢰를 무너뜨린다. SP4의 다축 신호도 같은 백테스트로 정당화한다.

---

## 9. API 설계 (초안)

```
POST   /v1/submissions              제보 등록 (제보권 차감 — 현행 미구현, 3.3 참조)
GET    /v1/submissions/me           내 제보 목록 + 판정 상태
POST   /v1/trends/{id}/endorse      동의(중복 제보 전환)
GET    /v1/trends                   관측 중/판정 완료 목록, 필터·정렬
GET    /v1/trends/{id}              상세 (제보 시계열 요약, 잠정 T)
GET    /v1/me/grade                 등급·AS·TI·다음 승급 조건
GET    /v1/me/ledger                점수 원장 내역 (전건 열람)
POST   /v1/appeals                  판정·제재 이의 제기
GET    /v1/leaderboard              상위 10 (동의자 한정)
```

**응답 원칙**: 등급 관련 모든 응답에 산정 근거를 함께 반환한다. `"현재 TI 0.42 / L2 요구치 0.45 / 부족분 0.03"` 형태.

---

## 10. 배치 스케줄

| 주기 | 잡 | 내용 |
|---|---|---|
| 일 1회 | `cluster_merge` | 신규 제보 임베딩 병합, 운영자 큐 적재. ADM-900 수동 실행 가능 |
| 일 1회 | `verdict_runner` | D+14 도달 항목 판정(현행 자체 로직, `JudgeService` 통합은 SP1에서 신설) → `verdicts` + `score_ledger` 기록. **제보 result·항목 RESOLVED 전이는 self-invocation으로 현재 반영되지 않음(4.3 참조, SP1에서 수정)** |
| 1시간 | `sla_watch` | **미구현(SP3)**. 신고 4h → 자동 임시 비공개 / 병합 24h → 판정 유예 연장 / 90일 미접속 관리자 비활성화 |
| 주 1회(월 00:00) | `grade_recalc` | AS 재계산(정상, `score_ledger` 기반), 승급. **TI는 `submissions.result` 미반영으로 0.4 고정(4.3 참조, SP1에서 수정)**. 제보권 리필(SP1에서 신설) |
| 일 1회 | `abuse_scan` | **미구현(Phase 2)**. 어뷰징 룰 실행 → `abuse_flags` |
| 월 1회 | `l4_quota` | **미구현(Phase 3)**. L4 정원 재산정 |

---

## 11. 법무 · 약관 체크리스트

- [ ] **등급 산정 기준·근거를 약관에 명시.** 유저 등급은 평가 정보이므로 산정 방식이 공개되지 않으면 제재 시 방어가 어렵다.
- [ ] **이의 제기 절차 및 처리 기한 명시** (`appeals` 엔티티와 연동)
- [ ] **강등·계정 제재 사유의 열거주의** — "기타 운영진 판단" 조항 단독 의존 금지
- [ ] **제보 콘텐츠의 명예훼손·영업방해 대응 프로세스**: 신고 접수 → 임시 비공개 → 소명 → 결정. 특정 인물·상호 포함 제보는 별도 검수 큐.
- [ ] **리워드 지급 시 세무 처리** (기타소득 원천징수 대상 여부 확인)
- [ ] **개인정보**: 디바이스 핑거프린트·IP 수집은 수집·이용 동의 항목에 별도 명시. 보관 기간 설정.
- [ ] **신고 콘텐츠 자동 임시 비공개(4h)**의 절차·해제 기준을 약관에 명시 (P1)
- [ ] **이해관계 미고지 시 제재 조항**을 약관 본문에 삽입 (7장 대응의 근거)

---

## 12. 미결 사항

| # | 항목 | 필요 조치 |
|---|---|---|
| O1 | 4.2 submitterTarget(상대값 하한·비율·기간), 4.3 임계값 | 상대값 방향 확정(P9). 값은 SP4 백테스트 |
| O2 | 판정 유예기간 14일의 적정성 | 카테고리별(밈 vs 상품) 차등 검토. `sla_watch`가 병합 지연 시 연장 |
| O4 | 확신도 3단계(10/30/50) 간격 | 유저 행동 데이터로 조정 |
| O5 | 반감기 90일 | 카테고리별 유행 주기 차이 반영 여부 |
| O6 | 독립 서비스 / 기존 커머스 부가모듈 여부 | 미확정. 후자일 경우 인증·회원 모듈 교체 및 과업 범위 협의 필요 |
| O8 | 시간 분포·플랫폼 다양성·제보자 독립성 축의 가중치 | SP4 |
| O9 | 판정 후 병합(ADJ 상쇄 경로) 도입 시점 | Phase 2. 그 전까지 RESOLVED 병합은 409 |
| O10 | SLA 통보 채널 | Phase 2 메신저 웹훅. Phase 1은 로그 |

---

## 부록 A. 기존 커머스 시스템에 부가할 경우

앞서 검토한 복지종합정보체계 고도화 RFP 기준으로는, 본 기능은 **제안요청서 명시 요구사항 밖**이다. 반영 경로는 두 가지.

1. **제안서 추가 요구사항으로 표기** — RFP p.15 각주 및 COR-012(p.109)가 "제안요청서에 명시되지 않았으나 제안사 판단으로 추가되어야 할 요구사항은 별도 표시하여 추가 작성 가능"을 허용.
2. **과업 변경 절차** — 계약 후 과업심의위원회를 통한 범위 조정.

기술적 접점은 SFR-019(회원 관리), SFR-021(마케팅/판촉 관리), SFR-023(통계 관리)이며, 등급 정보는 개인정보 성격을 가지므로 SER-006(개인정보보호법 준수) 및 DAR-008(암호화 방식 통일) 적용 대상에 포함된다.
