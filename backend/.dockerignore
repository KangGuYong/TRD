# 빌드 산출물·캐시 — 컨테이너 안에서 새로 만든다.
**/build/
.gradle/
**/.gradle/

# 로컬 전용 설정·시크릿은 이미지에 넣지 않는다.
**/.env
**/.env.*
app/src/main/resources/application-local.yml
**/*firebase*adminsdk*.json
**/firebase-service-account*.json

.idea/
*.iml
