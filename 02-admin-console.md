# 관리자 콘솔 설계서 (v0.1)

> **대상 시스템**: 트렌드 제보 · 유저 등급 시스템 (설계서 v0.1 연계)
> **작성 기준일**: 2026-08-06 · **개정**: 2026-09-17 (자동 임시 비공개 정책 확정, 클레임·에스컬레이션, ADM-900 추가. 근거: `docs/superpowers/specs/2026-09-17-design-review-design.md`)
> **문서 성격**: 초안. 화면 ID·권한 매트릭스는 확정, 레이아웃은 와이어프레임 수준.

---

## 0. 설계 원칙

이 콘솔의 성격을 먼저 못 박아 둔다. 여기서 방향을 잘못 잡으면 화면을 아무리 잘 만들어도 운영이 무너진다.

| # | 원칙 | 의미 |
|---|---|---|
| C1 | **큐 중심(Queue-first), 대시보드 중심 아님** | 관리자의 첫 화면은 "현황"이 아니라 **"지금 내가 처리해야 할 것"** 이다. 대시보드는 부차적. |
| C2 | **원장 불변** | 판정 결과·점수 레코드를 **수정하지 않는다.** 잘못된 건은 상쇄 원장을 **추가**해 바로잡는다. |
| C3 | **자동은 플래그와 가역적 보전 조치까지, 결정은 사람** | 어뷰징 탐지·병합 후보 추출은 자동. 제재·비공개 확정은 사람. 병합은 유사도 ≥0.85만 자동 확정, 0.75~0.85는 사람. 임시 비공개(가역)는 SLA 초과 시 자동 허용(R4 개정). |
| C4 | **모든 개입은 감사 대상** | 관리자 행위는 예외 없이 `admin_audit_log`. 조회 행위도 포함. |
| C5 | **파라미터 변경은 즉시 반영 금지** | 드래프트 → 시뮬레이션 → 2인 승인 → 예약 적용. |
| C6 | **권한 최소화** | 판정 검수자가 파라미터를 만질 수 없어야 한다. |
| C7 | **통제는 서버 한 곳** | 2인 승인·역할 검사는 서버 `ApprovalGate`·메서드 시큐리티가 강제. 프론트 `CAN`은 표시용. |

> P1–P10은 설계 재검토 스펙(`docs/superpowers/specs/2026-09-17-design-review-design.md` §2.2)의 정책 결정 번호다. 이 문서의 C1~C7과는 다른 채번이다 — C 번호는 이 콘솔 자체의 설계 원칙, P 번호는 그 원칙을 발동시킨 개별 정책 결정을 가리킨다.

> C2가 특히 중요하다. 관리자가 판정 결과를 직접 UPDATE 할 수 있게 만들면, 유저 이의 제기 때 "운영진이 임의로 바꿨다"는 주장을 반박할 근거가 사라진다. 이건 UI 편의성 문제가 아니라 **분쟁 대응 능력의 문제**다.

---

## 1. 역할 및 권한 (RBAC)

| 역할 | 코드 | 주 업무 |
|---|---|---|
| 검수자 | `REVIEWER` | 병합 큐, 신고 콘텐츠 1차 처리 |
| 운영자 | `OPERATOR` | 판정 재정, 이의 제기, 시딩, 제재 상신 |
| 관리자 | `ADMIN` | 제재 확정, 파라미터 승인, 계정 관리 |
| 감사자 | `AUDITOR` | **전 영역 읽기 전용** + 감사 로그 열람 |

### 1.1 권한 매트릭스

| 기능 | REVIEWER | OPERATOR | ADMIN | AUDITOR |
|---|:--:|:--:|:--:|:--:|
| 병합 확정/분리 | ✔ | ✔ | ✔ | 조회 |
| 판정 VOID 처리 | — | ✔ | ✔ | 조회 |
| 상쇄 원장 추가 | — | 상신 | ✔ | 조회 |
| 유저 제재 | — | 상신 | ✔ | 조회 |
| 등급 수동 조정 | — | — | ✔(2인) | 조회 |
| 파라미터 드래프트 | — | ✔ | ✔ | 조회 |
| 파라미터 적용 | — | — | ✔(2인) | 조회 |
| 임시 비공개(수동) | — | ✔ | ✔ | 조회 |
| 배치 수동 실행 (ADM-900) | — | ✔ | ✔ | 조회 |
| 관리자 계정 생성·권한 변경 | — | — | ✔(2인) | 조회 |
| 개인정보 필드 열람 | 마스킹 | 마스킹 | 사유입력 후(서버 기록) | 사유입력 후(서버 기록) |
| 감사 로그 | — | 본인분 | ✔ | ✔ |

**2인 승인(4-eyes)** 대상: 유저 제재 확정, 등급 수동 조정, 파라미터 적용, 상쇄 원장 100점 초과(재판정이 만드는 ADJ 합계 포함), 관리자 계정 생성·권한 변경. 신규 계정은 생성 후 7일간 승인권 없음(P8) — 계정 하나 더 만들어 자기 요청을 승인하는 경로 차단.
목표는 AUDITOR가 **전 영역 읽기**(신고 큐·파라미터 드래프트 포함)인 것이다. **미구현(SP3)** — 현행 `ReportAdminController`(신고 큐)·`ParamStudioController`(파라미터 드래프트)는 `@PreAuthorize`에 AUDITOR가 없어 둘 다 거부된다.

> 위 표는 목표 권한이다. **유저 제재·등급 수동 조정·독립 상쇄 원장 추가·관리자 계정 2인 승인·개인정보 열람 서버 기록은 현재 뒷단이 없다** — 파라미터 적용(ADM-600)·병합/분리/VOID(ADM-100)·임시 비공개(ADM-410)·배치 실행(ADM-900)·감사 로그(ADM-700)·판정 VOID/재판정/유예 연장(ADM-200)·파라미터 드래프트·관리자 계정 생성(단독 ADMIN — 2인 승인은 미구현)도 구현돼 있다. 자세한 내용은 각 화면 섹션(ADM-311·ADM-800)의 미구현 표기 참조.

---

## 2. 정보 구조 (IA)

```
관리자 콘솔
├─ 🏠 오늘의 작업        ADM-010   ← 진입 기본 화면
├─ 📥 큐
│   ├─ 병합 검수          ADM-100
│   ├─ 어뷰징 플래그       ADM-300
│   ├─ 이의 제기          ADM-400
│   └─ 신고 콘텐츠        ADM-410
├─ 📊 트렌드
│   ├─ 항목 목록          ADM-110
│   ├─ 항목 상세          ADM-111
│   └─ 판정 관리          ADM-200
├─ 👤 유저
│   ├─ 유저 목록          ADM-310
│   ├─ 유저 상세/원장      ADM-311
│   └─ 제재 관리          ADM-320
├─ 🌱 시딩 관리           ADM-500
├─ ⚙️ 정책
│   ├─ 파라미터 스튜디오    ADM-600
│   ├─ 등급 정책          ADM-610
│   └─ 배치 관리          ADM-900
└─ 🔒 시스템
    ├─ 감사 로그          ADM-700
    └─ 관리자 계정         ADM-800
```

---

## 3. 화면 정의

### ADM-010 · 오늘의 작업 (진입 화면)

관리자가 로그인하면 **처리 대기 건수와 SLA 초과 건**이 먼저 보여야 한다.

```
┌──────────────────────────────────────────────────────────┐
│  오늘의 작업                          김운영 (OPERATOR)   │
├──────────────────────────────────────────────────────────┤
│  ⚠ SLA 초과 3건                                          │
│                                                          │
│  ┌────────────┬────────────┬────────────┬────────────┐  │
│  │ 병합 검수   │ 어뷰징     │ 이의 제기   │ 신고       │  │
│  │    24      │     6      │     3 ⚠    │     2      │  │
│  │ 최장 4h    │ 최장 11h   │ 최장 26h ⚠ │ 최장 3h    │  │
│  └────────────┴────────────┴────────────┴────────────┘  │
│                                                          │
│  🔴 시스템 알림                                          │
│  · 이의 제기 큐 SLA 초과 3건                             │
│                                                          │
│  📈 오늘 판정 예정: 17건  |  D+14 도달 임박(24h): 9건    │
└──────────────────────────────────────────────────────────┘
```

**SLA 기준(안)**

| 큐 | 목표 처리시간 | 초과 시 |
|---|---|---|
| 신고 콘텐츠 | 4시간 | 자동 임시 비공개 (정책 확정 P1 · `sla_watch` **미구현(SP3)**) |
| 병합 검수 | 24시간 | 판정 유예 자동 연장 +24h (`sla_watch` **미구현(SP3)** — 현행은 화면 배너뿐, 연장은 수동) |
| 어뷰징 플래그 | 48시간 | 상위 역할 에스컬레이션 (Phase 2) |
| 이의 제기 | 5영업일 | 알림 + 대시보드 적색 (Phase 2) |

큐 카운트·최장 대기·뱃지는 전부 `GET /admin/queues/summary` 하나에서 온다. 목표는 미구현 큐를 0이 아니라 **"미구현"** 으로 표시하는 것이다(정책 P10). ADM-410(신고 콘텐츠)은 실데이터 (OPEN+EXPLAINING 카운트, OPEN 4h SLA 초과 검사), ADM-300/400은 `available=false`로 '미구현' 표시. 사이드바 뱃지는 `useQueueSummary()` API에 연동. **미구현(SP3)** — ADM-410 4h 초과 시 자동 임시 비공개(`sla_watch`)는 아직 미구현.

> 신고 콘텐츠 4시간은 명예훼손·영업방해 대응 시간이라 다른 큐와 성격이 다르다. **미처리 시 자동 임시 비공개**로 넘어가게 설계해야 야간·주말 공백을 메울 수 있다.

---

### ADM-100 · 병합 검수 큐 ★ 일상 업무량 1위

유사도 0.75~0.85 구간(자동 병합 금지 구역)이 여기로 온다.

```
┌──────────────────────────────────────────────────────────┐
│  병합 검수                        대기 24  |  오늘 처리 31│
├──────────────────────────────────────────────────────────┤
│  [ 유사도 순 ▾ ] [ 카테고리 전체 ▾ ] [ 내 담당만 ☐ ]      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ① 유사도 0.82                            제보 2h 전     │
│  ┌──────────────────────┬──────────────────────┐        │
│  │ 신규 제보            │ 기존 클러스터        │        │
│  │ "○○챌린지"          │ "○○ 챌린지" (#1204) │        │
│  │ 카테고리: 챌린지     │ 카테고리: 챌린지     │        │
│  │ 플랫폼: 인스타       │ 제보 4건, order 1~4  │        │
│  │ URL 2건 [보기]       │ 최초 3일 전          │        │
│  │ 제보자: user_8821    │ 상태: PENDING        │        │
│  │   TI 0.51 / L2       │ 현재 T 추정 0.31     │        │
│  └──────────────────────┴──────────────────────┘        │
│  근거 URL 미리보기: [썸네일] [썸네일]                    │
│                                                          │
│  [ 병합 (order 5 부여) ]  [ 별도 항목으로 분리 ]         │
│  [ VOID (허위/규정위반) ]  [ 보류 → 다음 ]               │
│  판단 근거: [_______________________] (선택, 감사 기록)  │
└──────────────────────────────────────────────────────────┘
```

**설계 포인트**

- **미구현**: 위 와이어프레임의 정렬·필터 바(`[ 유사도 순 ▾ ] [ 카테고리 전체 ▾ ] [ 내 담당만 ☐ ]`)는 `MergeQueueScreen`에 없다 — 큐는 생성 순서로만 나열된다.
- **키보드 단축키는 미구현.** `J/K/M/S/V` 뱃지는 화면에 떠 있지만 장식이다 — `onKeyDown` 등 키 리스너가 코드에 없다.
- 제보자의 TI·등급을 함께 노출하되, **이것이 병합 판단에 영향을 주면 안 된다는 점을 안내 문구로 명시**(실제로는 툴팁이 아니라 상시 노출되는 문구). 참고용일 뿐 고등급자 제보를 우대하면 시스템 공정성이 깨진다.
- 병합 결정 시 `order_rank` 재부여 결과를 **확정 전 미리보기**로 보여준다(`GET /admin/merge-queue/{id}/preview` — 실제 병합과 같은 계산을 재사용하는 dry-run. 구현됨).
- [VOID]는 신규 항목의 항목 VOID 사건이다 — ADM-200 VOID와 같은 `JudgeService.voidItem` 경로를 쓴다(제보 전부 VOID·제보권 반환, 이미 판정된 항목이면 VOID 판정과 원장 전액 상쇄까지). 이미 VOID·병합된 항목은 409.
- **미구현**: 클레임(`assigned_to`·`claimed_at`), 상태 모델의 `CLAIMED`/`HELD`/`ESCALATED`, `Idempotency-Key` 헤더 — 전부 없다. 실제 `MergeQueueStatus`는 `PENDING`/`MERGED`/`SKIPPED`/`VOIDED` 4종뿐이고 만료·보류·에스컬레이션 개념이 없다.
- RESOLVED 병합 거부(정책 P4)는 **미구현(SP2)**. `MergeQueueController.merge()`·`MergeService.merge()` 모두 대상 `TrendItem.state`가 `RESOLVED`인지 검사하지 않고(`MERGED`만 검사한다), `ClusterMergeCandidateService`의 후보 탐지 쿼리도 `RESOLVED`를 후보에서 빼지 않는다(`List.of(DRAFT, PENDING, JUDGING, RESOLVED)`) — 지금 RESOLVED 항목을 병합 결정하면 409가 아니라 그냥 병합된다. 상태만 보는 가드로는 부족하다 — SP1 전까지 배치 판정 항목도 `state`가 `PENDING`으로 남으므로 `verdicts.existsByTrendItemIdAndSupersedesIsNull(id)`를 함께 봐야 한다. **SP2에서 가장 먼저 고쳐야 할 간극.** VOID는 사유 서버 검증 없음(§4 참조).

---

### ADM-111 · 트렌드 항목 상세

```
┌──────────────────────────────────────────────────────────┐
│  #1204  "○○ 챌린지"            [PENDING]  D+9 / 14      │
├──────────────────────────────────────────────────────────┤
│  제보 시계열                                             │
│   서로 다른 제보자     12명 (목표 20명)                  │
│   서로 다른 목격 플랫폼  3곳 (인스타 → X → 디시)         │
│                                                          │
│   현재 T = 0.60  →  예상 판정: HIT / L3                  │
│   ※ 확정 아님. D+14 시점 재계산                          │
├──────────────────────────────────────────────────────────┤
│  제보 이력                                               │
│   order 1  user_4410  c=50  TI 0.63  3일 전              │
│   order 2  user_8821  c=30  TI 0.51  2일 전              │
│   order 3  user_0092  c=10  TI 0.38  1일 전              │
│   endorse  6명                                           │
├──────────────────────────────────────────────────────────┤
│  [ 판정 유예 연장 ]  [ VOID 처리 ]  [ 항목 분리 ]        │
└──────────────────────────────────────────────────────────┘
```

**"예상 판정"은 반드시 잠정임을 명시**한다. 관리자가 이 값을 보고 유저에게 미리 알려주는 사고가 실제로 잦다. 예상 판정은 `JudgeService.preview()`가 판정과 같은 신호(시딩 제외)·현재 파라미터로 계산만 하고 저장하지 않는다. 제보 목록에서 시딩 제보는 순위 대신 "시딩"으로 표시한다(선점 순위에서 빠진다).

**미구현**: 위 와이어프레임의 `[ 항목 분리 ]` 버튼은 `TrendDetailScreen`에 없다 — 실제로는 VOID 처리·판정 유예 연장 둘뿐이다. 클러스터 분리(split)는 **미구현(SP2)**이다 — ADM-100의 [별도 항목으로 분리]는 병합 후보를 기각할 뿐(`MergeService.recordSeparateDecision`은 감사 로그 한 행만 남기고 데이터를 건드리지 않는다) 클러스터를 쪼개지 않는다.

---

### ADM-200 · 판정 관리

판정은 자동이 원칙이므로, 이 화면은 **예외 처리 전용**이다.

| 기능 | 설명 |
|---|---|
| VOID 처리 | 규정 위반 / 중복 제보. **사유 입력 필수**(자유 텍스트 — 사유 코드 선택이 아니다. `VerdictAdminService.requireReason`) |
| 판정 유예 연장 | 병합 검수 지연 등으로 대기 시 1회당 1~7일, 원 마감(D+14) 기준 누적 D+21까지 반복 연장 가능(`VerdictAdminService.extendGrace`). 사유 입력란은 있으나 서버 필수 검증은 없다(`requireReason` 미호출) |
| 재판정 요청 | 판정 로직 버그 등으로 결과가 오염된 경우. **기존 판정은 유지하고 상쇄 원장을 추가**(원장 불변 원칙 C2) |

**금지 기능** — 화면에 아예 두지 않는다.
- 판정 결과(HIT/MISS) 직접 변경
- `T` 값 수동 입력
- `verdicts` 레코드 삭제

재판정이 필요하면 새 verdict 를 `supersedes: 이전ID` 로 추가하고, 점수 차액을 상쇄 원장으로 반영한다. 재판정·VOID 모두 배치와 같은 `JudgeService`가 한다(`rejudge`·`voidItem`).
- **재판정**은 원 판정 때 판정 근거에 동결한 파라미터로 다시 계산한다(J2) — 새 파라미터를 과거 판정에 소급하지 않는다. 재판정의 실질은 그사이 VOID된 제보를 빼고 다시 세는 것이다. 원장은 **제보 단위 차액**만 ADJ로 남기고(원 판정과 같은 감쇠 기준), 같은 입력으로 다시 재판정하면 차액 0이라 아무것도 남지 않는다. 시딩 제보에는 ADJ가 생기지 않는다. 확인 다이얼로그에 이 기준을 안내한다.
- **VOID**는 항목 VOID 사건이다 — 그 항목의 비VOID 제보를 전부 VOID하고(제보권 반환), VOID 판정을 쌓고, 원장을 제보 단위로 전액 상쇄한다. 이미 VOID된 항목의 VOID·재판정은 409.

**미구현(SP3)**: 차액 ADJ 합계가 100점을 넘으면 2인 승인 게이트(`ApprovalGate`)를 지나야 하지만, 현행 재판정 ADJ는 금액 상한도 승인 절차도 없이 그대로 기록된다.

---

### ADM-300 · 어뷰징 플래그 큐

```
┌──────────────────────────────────────────────────────────┐
│  user_7731                    플래그 3건  심각도 HIGH     │
├──────────────────────────────────────────────────────────┤
│  🚩 R-002 다중계정 의심                                  │
│     동일 디바이스 지문 3계정 (user_7731, 7742, 7801)      │
│     [ 연결 그래프 보기 ]                                 │
│  🚩 R-005 상호 추천 링                                   │
│     user_7742 와 상호 endorse 14회 / 전체 17회 (82%)     │
│  🚩 R-007 미고지 이해관계 의심                           │
│     제보 6건 중 5건이 동일 브랜드                        │
├──────────────────────────────────────────────────────────┤
│  최근 제보 6건 [펼치기]   원장 요약: AS 412 / TI 0.58    │
├──────────────────────────────────────────────────────────┤
│  [ 오탐 처리 ]  [ 경고 발송 ]  [ 제재 상신 → ADMIN ]     │
│  판단 근거: [_________________________________] (필수)   │
└──────────────────────────────────────────────────────────┘
```

**OPERATOR 는 상신까지, 확정은 ADMIN 2인 승인.** 오탐 강등은 신뢰 붕괴로 직결되므로 단독 결정 권한을 주지 않는다(C3).

---

### ADM-311 · 유저 상세 / 원장

```
┌──────────────────────────────────────────────────────────┐
│  user_4410      L3 분석가      가입 2026-02-11           │
│  AS 512  |  TI 0.63  |  판정완료 47 (HIT 29 / MISS 18)   │
│  제보권 8/8   |   플래그 0                               │
├──────────────────────────────────────────────────────────┤
│  점수 원장 (append-only)                                 │
│  일시        구분      Δ      사유                        │
│  08-05      HIT     +75.0   #1204 order1 c50 m0.5        │
│  08-01      MISS    -15.0   #1188 c30                    │
│  07-28      ADJ     +12.0   로직 버그 재판정 보정 (승인:박관리) │
│  ...                                        [전체 보기]  │
├──────────────────────────────────────────────────────────┤
│  [ 상쇄 원장 추가 ]  [ 제재 상신 ]  [ 등급 수동 조정 ]   │
└──────────────────────────────────────────────────────────┘
```

> **경고 — 현행 ADM-311은 픽스처 화면이다.** `/admin/users/**` 컨트롤러가 없고, [상쇄 원장 추가]·[제재 상신]·[등급 수동 조정] 버튼은 API를 호출하지 않고 성공 토스트만 띄운다(`UserLedgerScreen`의 `flash(...)`). 운영자는 제재를 올렸다고 믿지만 아무것도 남지 않는다. **실서버 연결 전까지 이 화면을 운영에 쓰면 안 된다**(SP3).

- 원장 행은 **수정·삭제 UI가 존재하지 않는다.** 정정은 `ADJ` 행 추가뿐.
- `ADJ` 추가는 **미구현** — [상쇄 원장 추가] 버튼은 사유·금액을 입력받아 클라이언트에서 필수 검사까지 하지만 실제로는 API를 호출하지 않는다. 지금 실제로 쌓이는 `ADJ` 행은 재판정·항목 VOID의 차액(`JudgeService`)뿐이다. 사유 필수·승인자 표시(`score_ledger.approved_by_admin_id`)·100점 초과 2인 승인은 독립 ADJ 엔드포인트가 생기면 같이 구현해야 한다.
- 마스킹 해제는 서버 엔드포인트(`POST /admin/users/{id}/unmask`)를 거쳐 `PII_VIEW`가 기록되어야 한다 — **미구현(SP3)**. 현행 `UserLedgerScreen`은 클라이언트 토글(`setMasked`)일 뿐이라 서버에 아무 기록도 남지 않는다. ADM-311 자체가 `/admin/users/**` 컨트롤러 없이 픽스처로 떠 있다.
- 등급 수동 조정도 **미구현** — 버튼은 같은 방식으로 토스트만 띄운다. "다음 주 자동 재계산 시 원복"이라는 경고는 실제 엔드포인트가 생긴 뒤에나 의미를 가진다.
- 개인정보(실명·연락처·디바이스 지문)는 **기본 마스킹** 원칙이나, 현재 화면엔 마스킹 대상 필드 자체가 없다(§4 참조) — 열람 토글은 사유 입력 없이 클릭 한 번이다.

---

### ADM-400 · 이의 제기 큐

```
접수 → 담당 배정 → 사실 확인 → 결정 → 통보 → (재이의)
```

| 항목 | 내용 |
|---|---|
| 처리 기한 | 5영업일 (약관 명시값과 동일해야 함) |
| 필수 첨부 | 판정 근거 스냅샷 자동 첨부 (관리자가 별도 조회 불필요) |
| 결정 유형 | 인용(상쇄 원장 발생) / 부분 인용 / 기각 |
| 통보 | 템플릿 + **산정 근거 수치 포함** 자동 삽입 |
| 재이의 | 1회 한정, 상위 역할이 처리 |

> 이의 제기 통보문에 **"현재 TI 0.42 / 요구치 0.45 / 부족분 0.03"** 같은 구체 수치를 자동 삽입하는 게 핵심이다. "기각되었습니다"만 보내면 재이의와 커뮤니티 여론전으로 이어진다.

---

### ADM-410 · 신고 콘텐츠 큐

특정 인물·상호가 포함된 제보에 대한 명예훼손·영업방해 신고 처리.

```
신고 접수 → [4h 내 미처리 시 자동 임시 비공개] → 제보자 소명 요청(48h)
         → 결정(복원 / 영구 비공개 / 수정 후 복원) → 양측 통보
```

- **정책 확정(2026-09-17, P1)**: 4h 자동 임시 비공개는 `sla_watch` 잡이 수행한다 — **잡은 미구현(SP3)이며 현재는 자동 비공개가 일어나지 않는다.** 임시 비공개는 가역적 보전 조치라 R4와 양립(R4 개정). 확정(복원/영구 비공개)은 사람. 목표는 수동 임시 비공개를 OPERATOR/ADMIN으로 제한(REVIEWER 불가)하는 것이지만 **미구현(SP3)** — 현행 `ReportAdminController.hide()`는 REVIEWER/OPERATOR/ADMIN 모두 허용한다.
- 신고자 정보는 처리자에게도 **비식별 처리** (보복 방지)
- **미구현**: 동일 항목 3회 이상 신고 시 자동 상위 역할 배정 로직이 `ReportAdminService`에 없다. 지금은 몇 번을 신고해도 배정이 바뀌지 않는다
- 결정 이력은 법적 대응 자료이므로 **영구 보존**

---

### ADM-500 · 시딩 관리 (Phase 1 전용)

운영진 직접 등록분을 유저 제보와 **명확히 분리**한다.

- 시딩 제보는 `is_seed = true` — 점수 원장에 반영되지 않고, 판정 신호(제보자 수)와 선점 순위에서도 빠진다(정책 P2·J3). 판정 결과(HIT/MISS)는 기록되지만 원장 행은 없다. 시딩은 클러스터 앵커 역할만 — 운영진이 HIT를 만들 수 없어야 한다(R1). 시딩 적중률 표(`listAccuracy`)는 적용된 현재 파라미터로 계산한다
- 시딩 비중 지표를 대시보드에 상시 노출 → Phase 2 전환 판단 근거(유저 제보 비중 70%)
- 시딩 담당자별 적중률을 별도 집계 → 판정 임계값 보정에 활용

> 시딩분이 점수 시스템에 섞이면 초기 랭킹이 운영진으로 채워지고, 공개 전환 시 신규 유저가 따라잡을 수 없는 구조가 된다.

---

### ADM-600 · 파라미터 스튜디오 ★ 가장 위험한 화면

목표 제보자 수(submitterTarget)와 4.3 임계값(HIT threshold)을 다루는 화면. **미구현**: 확신도 값·반감기·승급 요구치는 이 화면 범위 밖이다 — `ParamStudioScreen`엔 `submitterTarget`·`hitThreshold` 두 필드만 있다.
외부 지표 가중치는 더 이상 없다 — 판정 근거가 제보 시계열 하나뿐이라 조정 대상도 단순해졌다.
SP4에서 `submitterTarget`은 절대값이 아니라 **상대값 파라미터(하한·활성 제보자 비율·기간)** 로 바뀌고, 다축 신호 가중치(O8)가 추가된다. 시뮬레이션(`ParamSimulation.run()`)은 과거 판정 근거(`verdicts.evidence_json`)에 동결된 `TrendSignal`을 판정과 같은 순수 함수(`VerdictEngine`)로 새 파라미터에 다시 평가한다 — 판정 배치와 계산이 어긋날 수 없다. 적용한 파라미터는 이후 판정부터 쓰이고, 과거 판정의 재판정은 원 판정 파라미터를 쓴다(비소급, J2).

```
┌──────────────────────────────────────────────────────────┐
│  파라미터 드래프트 #17          작성 김운영  상태: 검토중  │
├──────────────────────────────────────────────────────────┤
│  목표 제보자 수(T=1.0)  현재 → 변경                       │
│   submitterTarget      20 → 25   (Phase 0 백테스트 반영)  │
│                                                          │
│  판정 임계값 (HIT)  0.20 → 0.22                          │
│  반감기(일)         90   → 90                            │
├──────────────────────────────────────────────────────────┤
│  🔬 시뮬레이션 (과거 180일 재판정)                        │
│   판정 변동: 342건 중 28건 (8.2%)                        │
│    · MISS → HIT   11건                                   │
│    · HIT → MISS   17건  ⚠ 영향 유저 14명, 평균 -23점     │
│   등급 변동: 강등 3명 / 승급 1명                          │
│   [ 영향 유저 목록 ]                                     │
├──────────────────────────────────────────────────────────┤
│  적용 방식  ◉ 예약 적용 (신규 판정부터)                   │
│             ○ 소급 적용  ⛔ 승인 2인 + 사유서 필수         │
│  적용 일시  2026-08-11 00:00                             │
│                                                          │
│  [ 승인 요청 ]        승인 0/2                           │
└──────────────────────────────────────────────────────────┘
```

**필수 안전장치**

1. **시뮬레이션 없이 승인 요청 불가** — 버튼 비활성화(구현됨: 클라이언트 `disabled` + 서버 `AdminValidationException` 이중 검사)
2. **비소급(구현됨 — 2인 승인 즉시 적용, 이후 판정부터 반영)**. 적용 일시 예약은 **미구현** — `ParamApplyExecutor.execute()`는 두 번째 승인이 확정되는 즉시 `markApplied(clock.instant())`를 호출하고, UI에도 적용 일시 입력 필드가 없다. 소급 적용 라디오·⛔승인 2인 흐름도 **미구현** — 지금은 예약 적용 경로 하나뿐이고, 위 와이어프레임의 소급 적용 옵션은 화면에 없다
3. **미구현**: 버전 이력 열람·원클릭 롤백 UI가 없다. 코드베이스 전체에 rollback 관련 로직이 없다
4. submitterTarget < 1 이면 저장 불가(구현됨: 클라이언트·서버 양쪽에서 검사)
5. **미구현**: 유저 대상 변경 고지 페이지가 없다

> 설계서 O1(가중치·임계값 미보정)이 열려 있는 상태이므로, Phase 0~1 동안 이 화면이 가장 자주 쓰인다. 반대로 Phase 3 이후에는 사용 빈도가 급감해야 정상이다. **파라미터를 자주 바꾸는 서비스는 신뢰를 잃는다.**

---

### ADM-610 · 등급 정책 관리

승급 요구치(판정완료 수 / TI / AS), 제보권, L4 정원 비율을 관리. ADM-600과 동일한 드래프트-시뮬레이션-승인 절차를 따른다.

추가로 **등급 분포 히스토그램**을 상시 노출한다. L2 이상이 전체의 40%를 넘어가면 등급 인플레 신호로 보고 요구치 재검토.

---

### ADM-700 · 감사 로그

| 기록 대상 | 예 |
|---|---|
| 상태 변경 | 병합/분리/VOID(구현됨). 제재·등급조정은 **미구현** — 그 액션 자체가 없어 기록할 것도 없다 |
| 원장 추가 | ADJ 행 생성(재판정 경로만 구현됨. 독립 ADJ 추가는 ADM-311 참조) |
| 정책 변경 | 파라미터 드래프트·승인·적용(구현됨). 롤백은 **미구현** |
| **조회 행위** | **미구현**: `PII_VIEW` 등 개인정보 열람 기록이 없다(`/admin/users/**` 컨트롤러 자체가 없음) |
| 인증 | 로그인(구현됨). 2FA 실패는 2FA 자체가 없어 발생할 수 없다. 권한 상승 감지는 **미구현** |

- **미구현**: 관리자/대상/액션/기간 검색·필터가 없다 — `AuditLogController`는 스스로 "Phase 0 스코프라 필터·페이지네이션 없이 최신 200건만 제공"이라고 주석에 적어 뒀다. 응답(`AuditLogEntryResponse`)에 `detail` 필드도 없다 — 감사 로그 엔트리 자체는 `detail` 컬럼에 기록되지만 이 화면엔 노출되지 않는다
- OPERATOR는 본인 행만, ADMIN·AUDITOR는 전체 열람(구현됨, `AuditLogController`). **ADMIN이 남을 열람했다는 사실 자체를 남기는 기록은 미구현**
- 위변조 방지(해시 체인, `HashChainer`)는 구현됨. 보존 기간·WORM 저장 정책은 별도 결정 필요

---

### ADM-800 · 관리자 계정

- 2FA는 **미구현** — `AdminAccount`에 컬럼은 있지만 검사하지 않는다(`AdminSecurityConfig` 주석: "2FA는 이번 단계 미구현"). 세션 타임아웃 30분은 구현됨(`server.servlet.session.timeout: 30m`)
- 계정 생성·권한 부여·회수의 2인 승인과 신규 계정 7일 승인권 유예(P8)는 **미구현** — `AdminAccountController`는 스스로 "ADMIN 단독 처리(2FA와 같은 급의 의도된 축소, approval_requests 없음)"라고 밝힌다. `ApprovalService`에 등록된 executor는 `ParamApplyExecutor` 하나뿐이다
- 부트스트랩 계정의 첫 로그인 비밀번호 변경 강제도 **미구현** — `AdminAccountBootstrapRunner`는 로그에 "온보딩 후 임시 비밀번호를 변경하세요"라고 경고만 남기고, 강제하는 코드는 없다
- 90일 미접속 자동 비활성화도 **미구현** — 이를 수행할 배치(`sla_watch`)가 없다. 지금은 `POST /admin/accounts/{id}/disable` 수동 클릭뿐
- 퇴사자 즉시 회수 체크리스트 (운영 절차 — 코드로 검증할 대상이 아님)

---

### ADM-900 · 배치 관리

배치를 스케줄 밖에서 수동 실행한다. 현재 `cluster_merge`만 지원.

```
┌──────────────────────────────────────────────────────────┐
│  배치 관리                                               │
├──────────────────────────────────────────────────────────┤
│  cluster_merge   일 1회 · 마지막 실행 03:00 (성공)        │
│  [ 지금 실행 ]                                           │
│  결과: 후보 12 · 자동 병합 3 · 큐 적재 5 · 분리 4 · 실패 0│
└──────────────────────────────────────────────────────────┘
```

- 권한 OPERATOR/ADMIN. 스케줄 실행과 같은 ShedLock 이름을 쓰므로 **실행 중이면 409**.
- 감사 로그 `CLUSTER_MERGE_MANUAL_TRIGGER` + 결과 요약을 `detail` 컬럼에 기록(구현됨). 다만 ADM-700 화면은 이 `detail`을 보여주지 않는다(§4·ADM-700 참조) — 지금은 실행한 화면(이 화면 자체의 결과 카드)에서만 볼 수 있다.
- 판정(`verdict_runner`) 수동 실행은 두지 않는다 — 판정 시점을 사람이 고르면 R1이 흔들린다. 재판정은 ADM-200.

---

## 4. 공통 UI 규약

| 항목 | 규칙 | 현행 |
|---|---|---|---|
| 되돌릴 수 없는 액션 | 확인 모달 + 사유 입력. 5초 내 취소(Undo) 토스트 제공 | **확인 모달·5초 Undo 토스트는 화면마다 다르다** — ADM-200(VOID·재판정·유예연장)은 자체 확인 오버레이, ADM-900은 `window.confirm`. ADM-100 병합/분리/VOID·ADM-311 ADJ는 클릭 즉시 실행(확인 단계 없음). Undo 토스트는 어디에도 없음(전부 알림용 토스트, 2.6~2.8초 자동 소멸) |
| 사유 입력 | 제재·VOID·ADJ·소급적용은 **필수**. 병합은 선택 | VOID·재판정(ADM-200)·파라미터 승인 요청(ADM-600)은 **서버 필수**(`requireReason`). 판정 유예 연장(ADM-200)은 입력란은 있으나 서버 미검증. 병합·분리·병합큐 VOID(ADM-100)는 선택, 서버도 미검증. 제재 상신·등급 수동 조정·독립 ADJ 추가(ADM-311)는 **미구현** — 클릭해도 서버 호출 없이 토스트만 뜬다. 소급 적용 UI는 없음(예약 적용만 구현) |
| 개인정보 | 기본 마스킹, 열람 시 사유 입력 → 접속기록 | 기본 마스킹은 원칙일 뿐, ADM-311엔 마스킹 대상 필드(실명·연락처·디바이스 지문)가 없다. 열람 토글은 사유 입력 없이 클릭 한 번이고 서버 호출도 없다 — **미구현**: `/admin/users/**`, `PII_VIEW` 기록 |
| 목록 | 서버 사이드 페이징, 커서 기반. 전체 선택 후 일괄 처리 시 상한 100건 | **미구현**: 서버 사이드 페이징·커서 파라미터가 없다 — 목록 엔드포인트는 전부 전체 배열을 한 번에 반환한다. 전체 선택·일괄 처리 UI도 없다 |
| 단축키 | 큐 화면 전체에 J/K/Enter/Esc 공통 적용 | **미구현**: `onKeyDown` 등 키 리스너가 코드에 없다. ADM-100의 `J/K/M/S/V` 뱃지는 장식일 뿐 동작하지 않는다 |
| 알림 | SLA 초과는 사내 메신저 웹훅 연동 | **미구현**: `sla_watch`가 없어 SLA 초과를 감지할 배치 자체가 없고, 웹훅 코드도 없다 |
| 반응형 | **데스크톱 전용.** 모바일 대응은 알림 확인 수준까지만 | **데스크톱 전용**(`viewport width=1280` 고정으로 실제 강제됨). 모바일용 알림 확인 화면은 **미구현** — 모바일에서도 동일한 데스크톱 레이아웃이 축소되어 보일 뿐이다 |

---

## 5. 화면 목록 총괄

| ID | 화면명 | 우선순위 | 비고 |
|---|---|:--:|---|
| ADM-010 | 오늘의 작업 | P0 | 진입 기본 |
| ADM-100 | 병합 검수 큐 | **P0** | 일상 업무량 1위 |
| ADM-110/111 | 트렌드 목록/상세 | P0 | |
| ADM-200 | 판정 관리 | P0 | 예외 처리 전용 |
| ADM-300 | 어뷰징 플래그 큐 | P1 | Phase 2 이후 부하 증가 |
| ADM-310/311 | 유저 목록/상세 | P0 | |
| ADM-320 | 제재 관리 | P1 | |
| ADM-400 | 이의 제기 큐 | P1 | 등급 공개 시점부터 필수 |
| ADM-410 | 신고 콘텐츠 큐 | **P0** | 법적 리스크. 오픈 전 필수 |
| ADM-500 | 시딩 관리 | P0 | Phase 1 필수, 이후 축소 |
| ADM-600 | 파라미터 스튜디오 | **P0** | Phase 0 백테스트에 직접 사용 |
| ADM-610 | 등급 정책 | P1 | |
| ADM-700 | 감사 로그 | P0 | 초기부터 없으면 소급 불가 |
| ADM-800 | 관리자 계정 | P0 | |
| ADM-900 | 배치 관리 | P1 | cluster_merge 수동 실행 (구현됨) |

**Phase 0~1 최소 구성**: ADM-010, 100, 110/111, 200, 311(원장 — 이의 대응 근거), 410(구현됨), 500, 600, 700, 800, 900
**Phase 2 공개 전 필수 추가**: ADM-300, 310, 320, 400, 610

> ADM-700(감사 로그)은 나중에 붙일 수 없다. 초기에 빠지면 그 기간의 운영 행위를 소급 증명할 방법이 없다.

---

## 6. 미결 사항

| # | 항목 | 필요 조치 |
|---|---|---|
| A1 | SLA 시간(4h/24h/48h/5영업일) | 운영 인력 규모 확정 후 재산정 |
| A2 | 병합 검수 예상 일일 건수 | Phase 1 데이터로 추정 → 인력 산정 근거 |
| A3 | 2인 승인 대상 범위 | 운영 부하와 통제 강도의 균형점 확인 |
| A4 | 감사 로그 보존 기간 | 법무 검토 (분쟁 소멸시효 고려) |
| A5 | 상쇄 원장 100점 기준선 | 임의값. 실제 원장 분포 확인 후 조정 |
| A6 | 신고 콘텐츠 야간·주말 대응 | **결정(2026-09-17)**: 자동 임시 비공개(`sla_watch`). 당직 인력 계획 없이 야간 명예훼손 신고가 노출되는 리스크가 더 큼 |

---

## 부록 A. 공공·국방 환경에 배치할 경우

앞서 검토한 복지종합정보체계 고도화 RFP 환경에 올린다면 다음이 추가로 걸린다.

| 항목 | 근거 |
|---|---|
| 관리자 접근 시 **개인정보 접속기록 자동 생성** | RFP p.11 개인정보접속기록 생성 서버(BlackBox Suite) 연동 |
| **DB 접근제어·암호화** 경유 필수 | DBSAFER, PrivacyDB (p.13) |
| 관리자 화면 **소스 시큐어코딩 검증** | SER-002, COR-011 |
| 등급·원장은 개인정보에 해당 → **암호화 방식 통일 대상** | DAR-008 (p.84) |
| 웹 표준·호환성(브라우저 3종 이상) | COR-009 (p.108) — 관리자 화면도 예외 아님 |
| 접속기록 소명관리 솔루션 추가 도입 가능성 | ECR-002 (p.64) 언급 |

관리자 화면은 개인정보 열람 빈도가 높아 접속기록 생성량이 상당하다. 서버 용량 산정 시 **일반 사용자 트래픽과 별도로 반영**해야 한다.
